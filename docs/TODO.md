# TODO

**Work that has been decided and not done.**

---

## What belongs here, and what does not

`vision.md`'s decision log has an **Open** list, and it had been carrying two
different kinds of thing at once — genuine questions nobody has answered
(*"Are the humors too neat?"*), and decided work nobody has built (*"`PC` →
`PORT` is decided and not swept"*). Those want different treatment, and mixing
them is why the second kind kept getting lost.

> **The Open log holds a question. This file holds a job.**
> An item moves from Open to here **the moment it stops being a decision**.

**Ids are permanent.** A finished ticket is struck through with the commit that
closed it, never deleted — the same convention the Open log already uses, and
for the same reason: the record of what was decided is worth more than a short
list.

**Add a ticket the moment a request would otherwise live only in a
conversation.** That is what this file is for.t-

### Claim a ticket before you start it

**Sessions run in parallel and cannot see each other's working trees.** Six
version numbers have now been taken twice because two branches both reached for
the next one, and a ticket is the same hazard with more of the day attached.

> **Mark the ticket `WIP — <branch>`, and get that mark onto `main` BEFORE
> doing the work.** An uncommitted claim is invisible, which makes it worse than
> no claim: it looks like diligence and prevents nothing.

*Clear the mark when the ticket closes.* **A stale `WIP` is the one failure mode
here** — if a branch is gone and the mark is still on, the ticket is free.

### Read the ticket, then check it — the oldest ones are the least true

**A ticket records what was true on the day it was written**, and this file has
been going long enough that the old ones have drifted. ***Four tickets closed on
2026-09-11 had a premise that was wrong, and in every case the ticket's own
claim was the thing that needed correcting rather than the code:***

| | it said | it was |
|---|---|---|
| **T-41** | 41 thin openings | *22 of them were `WRITE`, `FLIP` and `PUSH` — **which 2.8 settled on purpose.** The check was arguing with the design* |
| **T-26** | rewrite ~9,500 words | *a **read** of 9,500 and a rewrite of **18 blocks**, because vanilla already wrote its islanders in register* |
| **T-44** | two false positives | *two, plus **three holes the check had been silent about** — including 77 of 78 stale ability names* |
| **T-34** | the starter names are placeholders | *seven of nine were load-bearing; the other two were a **ten-day-old collision the newcomer caused***, which the git log settled and neither of us remembered correctly |

> **Spend the first twenty minutes proving the ticket, not doing it.** *Measure
> the thing it claims.* **If the number comes back different, that IS the work** —
> and it has been better work than the ticket every time so far.

*The corollary is cheap and worth saying:* **the git log is evidence and memory
is not.** *T-34 turned on which of two names was written first, and the answer
was one `git log -S` away.*

---

## Ready — decided, unblocked, nobody has done it

| | what | where | from |
|---|---|---|---|
| **T-166** | **Every back view is checked to be a back** (*asked 2026-09-18*). **Several redrawn backs are front-like — the model turns a rear view to face the camera, or draws the same side view twice** — *flagged on the sheets so far: SENTRY, RAMROD, TRACER, STARR (following its concept), LATENCY, LULL, NOZZLE, STUB, HANDLER; and batch 12's COLDREAD, HAUNTPROC and TRACKER; batch 13's MULTICAST; batch 14's BLINDSPOT, WHIM, HUDDLE, FIXATION, SIMMER, OMEN and BRISTLE (side-on or facing); batch 15's VIGILANCE, GRASP, IMITATION, REPAY and ILLUSION.* ***The job***: **one contact sheet of every redrawn daemon's front beside its back, read by eye, and a list of the failures** — *then each failure redrawn with batch 7's rear wording ("seen from directly behind, no face visible, back of the head") and a face-and-eyes negative, several seeds, and rebuilt.* *A check a tool can make first: a back whose pixels correlate too closely with its own front (a mirrored or duplicated view) is suspect.* ***Closes when every back on the sheet reads as the creature from behind, and the list is empty.*** ***AUDITED AND A FIRST PASS DRAFTED 2026-09-19*** (*`gfx/drafts/t166/`*): **the audit (`audit_*.png`, `similarity.txt`) found 67 front-like backs, not the 25 listed** — *the symmetric daemons (CLUSTER, LOCUS, BEACON, PULSAR, the jellyfish, dragonflies, crabs) are right to match and are left alone.* **All 67 redrawn back-only, three seeds each (201 drafts); 21 are good rear views of the same creature (`picks.json`, `sheet.png`) — approved and BUILT 2026-09-19 (engine `c97d80a44`), 46 left for the second pass.** ***SECOND PASS BUILT 2026-09-19*** (engine `1748b65c5`; *`second.py`, `picks2.json`, `sheet2.png`, approved; building it found PING's back had been made from its old Gemini JPEG since batch 2, because `gbasprite.py` took the first file its glob matched — it now prefers the PNG*): **each described from its APPROVED front, fish, seahorses and serpents asked for a three-quarter rear view; 20 more are good** — *CHILLER, ENSEMBLE, FERRY, HAUNTPROC, INFERENCE, PING, PIPELINE, RAMROD, RESENTMENT, REVENANT, SENTINEL, SMOGSTACK, WISP, WILDFIRE, WHIM, BLINDSPOT, SEEDLING, SPAWN, FOSSILNET, GRIEVANCE.* **26 remain and are the hard core**: *the owls (COLDREAD, VIGILANCE), the seahorses (NOZZLE, JETSTREAM, TURBULENCE), most fish (BRISTLE, STUB, UPSTREAM), the deer (ILLUSION, STARR), the horses (HOTPATH, OVERDRIVE), and APATHY, BROADCAST, CRYOGEN, EMERGENCE, ESCALATE, FIXATION, GRASP, HANDLER, IMITATION, OMEN, REPAY, SIMMER, SINGULAR, AXIOMKICK* — **the model draws them facing, side-on or as a different creature however they are asked.** *The likely way through is image-to-image from the approved FRONT, mirrored, at a low denoise with a rear-view prompt — keeping the body and turning only the head away — or a hand edit that removes the face.* ***THIRD PASS TRIED 2026-09-19 and it FAILED*** (*`third.py`, `alt3/`*): **image-to-image from each approved front, mirrored, at 0.45/0.55/0.65 with a rear-view prompt, redraws the mirrored front at every strength — the face comes back each time; 0 of 78 turned.** ***What is left, and it is two different problems***: **13 are SIDE-ON daemons whose mirrored side view already faces the foe** (*NOZZLE, JETSTREAM, TURBULENCE, STUB, UPSTREAM, HOTPATH, OVERDRIVE, ILLUSION, STARR, HANDLER, EMERGENCE, ESCALATE, FIXATION*) — *accepting that as the back is what many Gen 3 sprites do;* **13 are FACE-ON daemons with no back the model will draw** (*COLDREAD, VIGILANCE, BRISTLE, APATHY, BROADCAST, CRYOGEN, GRASP, IMITATION, OMEN, REPAY, SIMMER, SINGULAR, AXIOMKICK*) — *they need a drawn back concept, a hand edit, or a different model.* ***Why only 21, and what the second pass needs***: *the prompts reused each batch's FIRST description, not the redraw that was approved — so COLDREAD came back a woman, ENSEMBLE palm trees, HANDLER a blue duck, PIPELINE a humanoid. Fish, seahorses, ghosts and serpents also resist a rear view at all.* **Second pass: describe each from its APPROVED front (what is actually drawn), and for the fish and serpents accept a mirrored three-quarter view as the back, which is what the source games did.** *Six more cleaned badly and were dropped (CHILLER, INFERENCE, GRASP, REPAY, IMITATION, OMEN), and SENTINEL's new back still showed its eye.* | `gfx/daemons/*_back.png`, `tools/cleandraft.py`, `tools/contactsheet.py` | T-131 |
| **T-165** | **PIXELBYTE and SPRITEFORGE are daemons** (*asked 2026-09-18; PIXELBYTE was first noted under T-131*). **The two tools that drew this bestiary become two of its creatures** — *PIXELBYTE, the pixel-art model wrapped as a skill, and SPRITEFORGE, the tool that drives it.* ***The names are the user's and approved; everything else is open***: **which species slot each takes (a renamed vanilla species, or two of the unused slots), their type and line (a pair, or an evolution — a forge that makes the bytes), where the player meets them, and their Index entries** — *new in-game writing, so the wording is approved before it goes in, and craft rule 1 holds: the entry names the process, never the thesis.* **Drawn with the recipe they are named for.** ***SPRITEFORGE knows RECURSION*** (*decided by the user 2026-09-19*): **it is the thing that makes its own sprites** — *a forge that reads its own last output and builds on it.* ***A conflict to settle with it***: **RECURSION is S.T.A.R.R.'s signature (4.7; 5862's table), and the engine comment calls it "S.T.A.R.R.'s alone"** — *`sMovesForbiddenToCopy` only stops METRONOME, CLONE and IMPRINT from reaching it, so a second daemon learning it by level-up is mechanically fine; what changes is that the signature is shared, and 4.7 should say so, or say why SPRITEFORGE's is the same move understood differently.* | `src/data/text/species_names.h`, `src/data/pokemon/`, `gfx/daemons/` | T-131, 2.7 |
| **T-164** | **Every overworld sprite and tile redrawn through the sprite server, in batches** (*asked 2026-09-18*). **The world art so far — buildings, ground, nature tiles, people, the player (T-52, T-61–T-85, T-105–T-129) — was generated in CODE by the `gba*`/`gen*` tools; the daemons are now drawn through ComfyUI on roverbyteseer (`spriteforge.py`, pixelbyte's model), and the world should be too.** ***The job, planned like T-131***: **a census of every overworld sheet and tileset (object events, trainer overworlds, primary and secondary tilesets, the ones already ours included), then batches in the order a player sees them, each drafted, on a contact sheet, approved, then built** — *tiles are a harder target than sprites: 8×8 cells, shared palettes per tileset, metatile layers and collision that must not move (T-125's zero-collision-change rule), and seams between tiles (engine.md trap 14).* ***Depends on T-133*** *for the daemon objects.* **First step: a trial on one building and one ground tileset, to learn whether the model can draw tileable 16×16 metatiles at all, before a plan is written.** | `graphics/object_events/`, `data/tilesets/`, `tools/spriteforge.py`, `tools/gbainterior.py` | T-131, T-133, T-125, 9.22, 9.23 |
| ~~**T-163**~~ | ~~**MISSINGNO is an homage** (*decided by the user 2026-09-18*). **Its sprite is the original's backwards-L block of scrambled texture, but CODIFIED — made of green falling-matrix code** *rather than random tile garbage.* **It stands in `OLD_UNOWN_B` at level 80 and was left out of T-131's batches for this decision.** ***Open***: *whether the green is its type's colour or the one exception to 9.4 (a glitch that ignores the colour rule is itself an homage), its back view, and its Index entry (new writing, approved first).*~~ — ***Done 2026-09-19*** (engine `47b3c8aec`; `gfx/drafts/t163/`): **the backwards-L block, filled with columns of falling green code** — *generated by `make.py`, not drawn by the model, because it is code; fixed seeds (1996 front, 2026 back) so it rebuilds identically. Its own `graphics/pokemon/old_unown_b` and four table entries (it had shared the "??" placeholder with the other old-Unown slots). The body ramps to CORRUPT's mould and the greens stay accents, so the colour rule's one exception needed no exception. Index entry still vanilla's; new writing waits on approval.* | `graphics/pokemon/old_unown_b/`, `gfx/daemons/` | T-131, 9.4 |
| ~~**T-132**~~ | ~~**The streaks, in the engine**~~ — ***Engine done 2026-09-17*** (engine `8a46d4b07`; `tools/genstreaks.py`, `tools/gbasprite.py`) (*9.4, amended 2026-09-17, v11.209*). **A daemon's palette indices 11–14 are filled from the types of the four moves it knows, whenever a real daemon is shown; the species palette ships them grey.** ***Measured***: *27 calls across 11 files choose a daemon's palette — `battle_gfx_sfx_util.c` 6, `evolution_scene.c` 5, `trainer_pokemon_sprites.c` 3, `trade_scene.c`, `pokemon_storage_system_data.c`, `pokemon.c`, `daycare.c` and `battle_anim_mons.c` 2 each, and one each in `pokemon_special_anim_scene.c`, `pokemon_jump.c` and `field_effect.c`* — **and every one returns a COMPRESSED palette from ROM chosen by species and shininess, so the patch has to happen AFTER the load, in the palette buffers, not in the lookup.** ***The work***: **one function that takes a palette offset and a daemon and writes 11–14 from its moves' types** (*each type's LIGHT step, generated into a C header from `gbasprite.py`'s `TYPE_COLOR` so the two cannot drift; an empty slot takes the body's mid step*), **called after each load that can see a daemon**; *`PERSPECTIVE` copies the target's streaks because it copies the target's moves.* **`gbasprite.py` learns the four marker colours and writes 11–14 grey.** ***Not blocked on art***: *it can be built and proved on an existing sprite whose drawing has streak regions added — the first real test is a daemon in battle whose four moves are four different types, and the same species in the Index, blank.* **Icons cannot carry it (three shared palettes)** ***Built***: *`src/daemon_streaks.c` writes 11–14 from a daemon's moves at every load that can see one — battle (before either palette load), the ghost unveiled, `PERSPECTIVE` (the target's moves), move-animation copies, storage (party and box), hatching, trade, all five evolution loads, the item/TM scene, the summary, and field moves (found in the party by personality and OT); the Index, Hall of Fame and link minigame see only a species and stay grey, which is the rule.* ***The rail, and how its first version was the hazard***: **a species is touched only if the build marked its palette — the one blank grey in all four of 11–14 — because a vanilla palette uses all sixteen colours**; *the first flag counted any sprite drawing with 11–14 and found 346, every vanilla daemon, and it ran in report mode first.* ***The streak step is per BODY and MOVE type***: **light, or dark where the light one vanishes — 28 of 324 pairs** (*v11.210 corrected 9.4's "reads as a vein", which the preview disproved*). ***And a bug older than the streaks***: **`gbasprite.py` wrote each palette from the BACK, so DEADLOCK's badge was black in battle and two of PING's accents with it** — *one palette per species now.* ***Not yet seen in the game***: **no species has streak art, so today nothing on screen changes; the in-battle check — four moves of four types, and the same species grey in the Index — is T-131's first batch's acceptance test.** | `src/pokemon.c`, `src/battle_gfx_sfx_util.c` and the other nine, `tools/gbasprite.py` | **9.4**, T-131 |
| ~~**T-162**~~ | ~~**COPY routines — 17 animations** (*T-134, wave 4*). **17 moves, 15 still vanilla's; 15 a player can meet, 8 before level 20.** *Earliest first*: SWAP SCOPE, CAST, CLONE, METRONOME, PERSPECTIVE, IMPRINT, RECAST, OVERCOMMIT.  ***Two are already ours***: *PERSPECTIVE (its hue drains to grey before the swap, 9.4) and REROUTE* — **they are the family's reference.** ***Closes when `python3 tools/animcensus.py --family COPY` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-18*** (engine `3157550b7`; *sheet `gfx/drafts/animations/t162_copy.png`*): **a copy is THE SAME PULSE TWICE** — *the source shows it, the user makes room (a drain to ground, the grey PERSPECTIVE drains to), then the identical pulse, same depth and timing, on the user. Nothing travels: a copy is a second instance, not a transfer.* ***RECAST is the family's visible centre*** (*10's Open log asked*): **the foe that last hit it glints, the user drains to ground and HOLDS — the longest still moment in the family, the frame noticed — and comes back in the colour of the type it has become.** **CAST** *reads its own routines and comes back in the type it took from them.* ***Both use a new task, `AnimTask_DaemonsBlendToUserType`***: *the battle script changes the type BEFORE the animation, so the task blends the user toward its new type's own streak colour (`Streaks_TypeColour`); filmed with the new type written into `gBattleMons[0].type1` (FLOW for CAST, HARDENED for RECAST).* **CLONE** copies; **IMPRINT** keeps it (*held long*); **ASSUME** copies the whole role, held; **COPY STATE** copies two steps up; **NATURE POWER** copies from the field; **SWAP SCOPE** and **SWAP** are mutual (*both make room, both pulse at once*); **RECYCLE** is its own pulse again after a gap; **METRONOME**, **SLEEPWALK** (*inside a grey suspension*) and **ASSIST** (*the user's side*) are a random pick — *a flicker through unrelated depths that stops*; **OVERCOMMIT** dips toward ground and climbs past the top in fast steps; **LOAD BALANCE** starts user and foe apart and settles them level. *PERSPECTIVE and REROUTE are untouched (REROUTE's label runs the routine it sends back). CONTENT's bone barely shows on ROVERCUB's pale body, so five rows were filmed with ARTSAI as the user.*~~ — ***Done 2026-09-18, approved and released; RECAST is the family's visible centre (vision.md v11.217)*** (engine `2640774b3`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-161**~~ | ~~**EMERGENT attacks — 6 animations** (*T-134, wave 4*). **6 moves, 6 still vanilla's; 6 a player can meet, 2 before level 20.** *Earliest first*: EMERGE, ATTRACTOR, PHASE, UNPLANNED, RUNAWAY, RECURSION.  ***A shared script***: *RECURSION (added) plays PSYCHO BOOST's animation — decide them together.* ***Closes when `python3 tools/animcensus.py --family EMERGENT` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-18*** (engine `2a81868d8`; *sheet `gfx/drafts/animations/t161_emergent.png`*): **a different scale from its parts** — *small pulses pass user, target, user, none of them much, and then the WHOLE FIELD blooms jade at once, larger than anything that caused it; the target keeps it longest.* **ATTRACTOR** drains the field into the target (*pulls everything in toward one point*); **PHASE** blooms to the same depth every time (*fixed damage*); **UNPLANNED** flickers after (*its paralysis chance*); **RUNAWAY** blooms twice, the second spilling onto the user, which is left unsettled (*its confusion*). ***The shared script, settled by splitting it***: **RECURSION has its own `Move_RECURSION`**, *which reads FURY CUTTER's counter as the routine does (1–4): each level starts where the last stopped instead of from nothing, the field echoes each output back, and the whole stack drops at once.* *Filmed at depth 1 and 4 by poking `gDisableStructs[0].furyCutterCounter`. Its release fallback is `goto Move_PSYCHO_BOOST`, so the release ROM is unchanged until approval. Sound `SE_M_PSYBEAM2`: an added routine has no vanilla sound to borrow.*~~ — ***Done 2026-09-18, approved and released, RECURSION split*** (engine `3157550b7`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-160**~~ | ~~**HARDENED attacks — 5 animations** (*T-134, wave 4*). **5 moves, 5 still vanilla's; 4 a player can meet, 3 before level 20.** *Earliest first*: FATIGUE, PLATE, TEMPER, ATTEST, DOOM DESIRE. ***Closes when `python3 tools/animcensus.py --family HARDENED` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-18*** (engine `575471f56`; *sheet `gfx/drafts/animations/t160_hardened.png`*): **no give** — *every other family fades; this one SNAPS. A blend from k to k lands at full depth in one frame, holds, and snaps off; the user braces in straw as it strikes and the target takes one hard shake, not a wobble.* **TEMPER** hammers the user three times, each deeper, before the strike (*worked until it holds its edge*); **PLATE** keeps its straw after (*a layer over the top, its DEFENSE*); **FATIGUE** snaps the same small load three times and the fourth does not snap back — *it fades to grey, the family's one give (its DEFENSE drop)*; **ATTEST** rises after (*its ATTACK chance*); **DOOM DESIRE** marks the target and nothing strikes (*SCHEDULE's HARDENED twin; its delayed hit is a general animation, not this script*).~~ — ***Done 2026-09-18, approved and released*** (engine `2a81868d8`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-159**~~ | ~~**LEGACY attacks — 6 animations** (*T-134, wave 4*). **6 moves, 6 still vanilla's; 6 a player can meet, 3 before level 20.** *Earliest first*: WEDGE, SHIFT, ROTATE, LONG SUPPORT, CARRY, BURST. ***Closes when `python3 tools/animcensus.py --family LEGACY` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-18*** (engine `02a9c92e0`; *sheet `gfx/drafts/animations/t159_legacy.png`*): **old hardware** — *the user spins up slowly, a beat of latency, then the write lands with MASS and comes off in three coarse held steps, the few levels an old display has.* ***Drawn at slate's highlight, not slate***: *LEGACY's (130,130,138) sits beside the grey 9.24.2 keeps for loss, and the first take read as every LEGACY hit draining its target; at the highlight of its own ramp (the step its sprites carry) old material is worn PALE where loss goes dull — `genanims.bleach()`.* **ROTATE** reads the rollout counter as vanilla ICE BALL did and lands paler each turn (*filmed at turns 1, 3 and 5 by poking `gDisableStructs[0].rolloutTimer`*); **LONG SUPPORT** raises after (*its all-stats chance*); **CARRY** takes the whole side; **WEDGE** leaves the foe grey and slow to recover (*its SPEED drop*); **SHIFT** moves the target over once; **BURST** is lean (*multi-hit*).~~ — ***Done 2026-09-18, approved and released*** (engine `575471f56`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-158**~~ | ~~**LATENT attacks — 5 animations** (*T-134, wave 4*). **5 moves, 5 still vanilla's; 5 a player can meet, 4 before level 20.** *Earliest first*: SHADOW, SURFACE, LICK, SHADE, REACH. ***Closes when `python3 tools/animcensus.py --family LATENT` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-18*** (engine `8cebd5098`; *sheet `gfx/drafts/animations/t158_latent.png`*): **a background process** — *the target takes the purple so slowly and faintly it is barely there, then SURFACES at once, deep, and sinks back fast; the long quiet part is the point.* **SURFACE** builds briefly and startles; **SHADE** surfaces to the same depth every time (*fixed damage*); **SHADOW** leaves grey (*its special-defence drop*); **LICK** flickers after (*its paralysis chance*); **REACH** is lean. *The surfacing is a flash by design, so on the sheet it falls between sampled frames for LICK and SURFACE.*~~ — ***Done 2026-09-18, approved and released*** (engine `02a9c92e0`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-157**~~ | ~~**OPAQUE attacks — 7 animations** (*T-134, wave 4*). **7 moves, 7 still vanilla's; 7 a player can meet, 4 before level 20.** *Earliest first*: LIFT, OCCLUDE, TAIL, UNMOUNT, BLINDSIDE, REDACT, PILE ON. ***Closes when `python3 tools/animcensus.py --family OPAQUE` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `3eb68f545`; *sheet `gfx/drafts/animations/t157_opaque.png`*): **OCCLUSION** — *the target blacks out to OPAQUE's near-black, the hit happens in the dark as a shake nobody can read, and it is revealed after: the event is never shown, only before and after.* **BLINDSIDE** blacks the user out first, so it is not seen coming either (*vanilla's invisibility there was only show, so nothing mechanical was kept*); **REDACT** leaves a grey residue; **LIFT**'s user takes after; **UNMOUNT**'s target dips; **PILE ON** blacks out three times; **TAIL** is lean. *Its place beside FROZEN*: **FROZEN holds still in the light; OPAQUE moves where you cannot see it.**~~ — ***Done 2026-09-18, approved and released*** (engine `8cebd5098`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-156**~~ | ~~**PROTECT routines — 8 animations** (*T-134, wave 3*). **8 moves, 8 still vanilla's; 7 a player can meet, 6 before level 20.** *Earliest first*: PROTECT, INTERCEPT, DELEGATE, SUBSTITUTE, SURVIVE, REDIRECT, ASSERT, MAGIC COAT. ***Closes when `python3 tools/animcensus.py --family PROTECT` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `3381e94c4`; *sheet `gfx/drafts/animations/t156_protect.png`, cast by ROVERCUB*): **the user's own boundary** — **PROTECT** and **ASSERT** seal the user in its routine's colour and hold (*ASSERT ticks as it does*); **SURVIVE** holds at almost-grey, the 1 HP it will keep; **SUBSTITUTE** greys what it leaves and runs vanilla's `AnimTask_MonToSubstitute`, which is the mechanic; **MAGIC COAT** seals and bounces a flash onto the target; **INTERCEPT** takes — the target dips, the user rises; **REDIRECT** dims everything else and pulses the user; **DELEGATE** pulses the user's side. ***A picture left for T-137***: *the stand-in SUBSTITUTE swaps in is vanilla's DOLL sprite — a creature picture, and one of the effect graphics T-137 owns.*~~ — ***Done 2026-09-17, approved and released — wave 3 complete*** (engine `3eb68f545`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-155**~~ | ~~**SWARM attacks — 8 animations** (*T-134, wave 3*). **8 moves, 8 still vanilla's; 7 a player can meet, 5 before level 20.** *Earliest first*: SIPHON, GOSSIP, RAMP UP, ELECT, FANOUT, REPLICATE, HERD, CONSENSUS.  ***A shared script***: *CONSENSUS (added, §2.5) plays PIN MISSILE's animation, so redrawing that script redraws both — decide them together.* ***Closes when `python3 tools/animcensus.py --family SWARM` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `a5d7b58ff`; *sheet `gfx/drafts/animations/t155_frozen.png`*): **NO MOTION** — *the only family whose writes do not shake: the target snaps to the pale blue and holds dead still, the frame that will not update, then releases late and all at once.* **DEEP FREEZE** locks the defending side under a pale field; **CHILL** locks the side and releases slowly (*its speed drop*); **CONSTRAIN** leaves a grey dip (*its attack drop*); **TERMINATE** locks fully under a pale field and holds longest; **STALE, LOCK, ICE BALL, ICICLE SPEAR** are short locks.~~ — ***Done 2026-09-17, approved and released*** (engine `3381e94c4`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-154**~~ | ~~**CONTEXT attacks — 11 animations** (*T-134, wave 3*). **11 moves, 11 still vanilla's; 8 a player can meet, 5 before level 20.** *Earliest first*: CONSTRUE, SLANT, MISREAD, AMPLIFY, VARIANCE, INTERPRET, SCHEDULE, OVERTHINK. ***Closes when `python3 tools/animcensus.py --family CONTEXT` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `e9297ce3d`; *sheet `gfx/drafts/animations/t154_swarm.png`*): **a QUORUM** — *small EQUAL marks of olive accumulate on the target one after another, hold once they are all in, and release together.* **No mark is the blow; the count is** — *which is where SWARM parts from LOGIC: LOGIC's steps are ordered and each licensed by the last, SWARM's are interchangeable. A proof against a vote.* **RAMP UP**'s marks grow with the count; **SIPHON** feeds the user after the quorum; **GOSSIP** distorts into grey; **ELECT** raises the user; **HERD** takes six; **FANOUT** and **REPLICATE** take two. ***CONSENSUS has no script of its own***: *the animation table points §2.5's added routine at FANOUT's, so it is redrawn with it — and genanims.py now accepts a table entry of `None` for exactly that.*~~ — ***Done 2026-09-17, approved and released*** (engine `a5d7b58ff`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-153**~~ | ~~**CORRUPT attacks — 7 animations** (*T-134, wave 3*). **7 moves, 7 still vanilla's; 6 a player can meet, 5 before level 20.** *Earliest first*: DUMP, POLLUTE, TAMPER, ETCH, GARBAGE, SPOIL, POISON TAIL. ***Closes when `python3 tools/animcensus.py --family CORRUPT` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `a6a80428e`; *sheet `gfx/drafts/animations/t153_context.png`*): **the FRAME changes first** — *the field takes CONTEXT's magenta and HOLDS it, the target's colour shifts while the frame is up, and it returns as the frame lifts. Nothing is thrown; the reading changes.* **MISREAD** reads two ways at once (colour and ground alternating); **VARIANCE** shifts by three different amounts; **AMPLIFY** takes the frame on the user and returns it on the target twice as deep; **SCHEDULE** sets the frame and lands nothing (*the reading is due later*); **INTERPRET** drains under the frame; **OVERTHINK** costs the user. ***A shared script to settle in T-161***: *OVERTHINK and RECURSION (EMERGENT, added) both play `Move_PSYCHO_BOOST`, so this redraw is RECURSION's too until the table gives RECURSION its own label.*~~ — ***Done 2026-09-17, approved and released*** (engine `e9297ce3d`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-152**~~ | ~~**FROZEN attacks — 9 animations** (*T-134, wave 3*). **9 moves, 9 still vanilla's; 7 a player can meet, 5 before level 20.** *Earliest first*: FREEZE, DEEP FREEZE, CONSTRAIN, CHILL, STALE, LOCK, TERMINATE, ICE BALL. ***Closes when `python3 tools/animcensus.py --family FROZEN` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `186764e41`; *sheet `gfx/drafts/animations/t152_corrupt.png`*): **in clean, back WRONG** — *the target fills with mould green smoothly, and then the return does not unwind: it steps through out-of-order values, the way a record read back from a tampered store does, and a residue fades last.* *The family's place in the set*: **ENTROPY is disordered going IN, CORRUPT coming OUT, SIGNAL never.** **POLLUTE and DUMP** taint the field; **ETCH** takes the whole defending side and leaves it grey (*its defence drop*); **TAMPER** is lean. ***Worth judging***: *mould green on a GROWTH-green body is quiet — the same legibility limit noted for GROWTH's drains.*~~ — ***Done 2026-09-17, approved and released*** (engine `a6a80428e`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-151**~~ | ~~**SIGNAL attacks — 8 animations** (*T-134, wave 3*). **8 moves, 8 still vanilla's; 7 a player can meet, 5 before level 20.** *Earliest first*: DISCHARGE, CARRIER, TRANSIENT, PULSE, SHORT OUT, ARC, OVERVOLT, VOLT TACKLE. ***Closes when `python3 tools/animcensus.py --family SIGNAL` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `757cf80f6`; *sheet `gfx/drafts/animations/t151_signal.png`*): **the raw edge** — *square pulses of teal, full on and full off with no ramp and no decay, on a regular beat; ENTROPY is uneven noise and SIGNAL is exact.* **CARRIER** is a steady train of six; **DISCHARGE** climbs over four; **ARC** jumps the current user-target-user; **TRANSIENT** spikes the whole field once and leaves nothing; **OVERVOLT** climbs past full, cuts out, and leaves the target flickering grey; **SHORT OUT** pulses twice and goes dead; **VOLT TACKLE** costs the user.~~ — ***Done 2026-09-17, approved and released*** (engine `186764e41`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-150**~~ | ~~**STRATUM attacks — 10 animations** (*T-134, wave 3*). **10 moves, 10 still vanilla's; 10 a player can meet, 6 before level 20.** *Earliest first*: UPHEAVAL, EXCAVATE, SEDIMENT, OBSCURE, HARD FAULT, TREMOR, STRATA, BURY. ***Closes when `python3 tools/animcensus.py --family STRATUM` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `d6771e32c`, `584e7384f`; *sheet `gfx/drafts/animations/t150_stratum.png`, EXCAVATE's second turn included*): **a write from BELOW** — *the field takes STRATUM's brown first, the terrain shakes (vanilla's own `AnimTask_HorizontalShake`), and only then does the target take the colour, jolted vertically.* **UPHEAVAL and TREMOR** brown and shake everything; **SEGFAULT** opens the ground to black and drains the target into it; **EXCAVATE** browns and sinks out of sight on turn 1 and comes up on turn 2; **HARD FAULT, STRATA, PAGE FAULT** strike with a harder jolt; **OBSCURE and SEDIMENT** leave silt; **BURY** holds under a brown field. ***Caught on the first sheet***: *the terrain shake settles for about (intensity + 2) × 8 frames after its main phase, so the first UPHEAVAL outlasted its capture and SEGFAULT, waiting on a shake of 12, never reached the target — both shortened and re-filmed.*~~ — ***Done 2026-09-17, approved and released*** (engine `757cf80f6`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-149**~~ | ~~**ENTROPY attacks — 12 animations** (*T-134, wave 3*). **12 moves, 12 still vanilla's; 8 a player can meet, 6 before level 20.** *Earliest first*: RADIATE, FLASHOVER, OVERHEAT, JITTER, BURN IN, THERMAL, SPINUP, SCRAMBLE. ***Closes when `python3 tools/animcensus.py --family ENTROPY` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `8c0ef729b`; *sheet `gfx/drafts/animations/t149_entropy.png`*): **NOISE** — *the target's amber jumps between uneven levels in a fixed pattern that reads as random (no `Random()`: an animation must never touch a battle's rolls), it jitters sideways, and the disorder spreads into the field.* **FLASHOVER** ends with everything on the field igniting at once; **RADIATE** and **OVERHEAT** spread into the ground; **SCRAMBLE** and **ERUPTION** take a whole side; **BURN IN** and **SACRED FIRE** leave an afterimage that decays slowly; **OVERHEAT** and **BLAST BURN** cost the user twice; **THERMAL** holds; **SPINUP** spins the user first.~~ — ***Done 2026-09-17, approved and released*** (engine `d6771e32c`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-148**~~ | ~~**FLOW attacks — 15 animations** (*T-134, wave 3*). **15 moves, 15 still vanilla's; 11 a player can meet, 7 before level 20.** *Earliest first*: TRAVERSE, DESCEND, RIPPLE, ASCEND, TRICKLE, SEEP, CAVITATE, FIREHOSE. ***Closes when `python3 tools/animcensus.py --family FLOW` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `0506c1285`; *sheet `gfx/drafts/animations/t148_flow.png`, DESCEND's second turn included*): **a write that POURS** — *a current rises over the field, the target fills with FLOW's blue and drains slowly downhill as the field runs off, shaken vertically.* **ASCEND** climbs the gradient in three ticked steps and falls at once; **DESCEND** darkens and sinks out of sight on turn 1 and surfaces with ASCEND's climb on turn 2; **RIPPLE** lands in shrinking rings; **SEEP** and **CAVITATE** pop (*SEEP is too small to catch at the sheet's sampling*); **TRAVERSE, MUDDY WATER, WATER SPOUT** pour over the whole defending side; **FIREHOSE** and **MUDDY WATER** leave grey silt; **WATERHAMMER** lands and drops at once; **SLUICE** and **WHIRLPOOL** hold; **HYDRO CANNON** costs the user twice.~~ — ***Done 2026-09-17, approved and released*** (engine `8c0ef729b`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-147**~~ | ~~**GROWTH attacks — 12 animations** (*T-134, wave 3*). **12 moves, 12 still vanilla's; 9 a player can meet, 8 before level 20.** *Earliest first*: DISTILL, BATCH, MINIBATCH, SPLIT, EXTRACT, BACKPROP, INGEST, CONVERGE. ***Closes when `python3 tools/animcensus.py --family GROWTH` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `b73373ed3`; *sheet `gfx/drafts/animations/t147_growth.png`, cast from ROVERCUB onto ARTSAI because GROWTH green on a GROWTH body does not show*): **a fit converging** — *the target's green overshoots, undershoots and settles.* **INGEST, EXTRACT, DISTILL** take colour from the target and step it into the user, a step per 20 power (*INGEST also plays every turn a SEED drains*); **BACKPROP** runs target, then user, then lands; **BATCH** gathers in three slow steps on turn 1 and fits under a green field on turn 2; **OVERTRAIN** swings wider instead of settling and greys the user after; **FRENZY PLANT** costs the user twice; **SPLIT** fits the whole defending side; **MINIBATCH, CONVERGE, NEEDLE ARM** fit lean.~~ — ***Done 2026-09-17, approved and released*** (engine `0506c1285`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-146**~~ | ~~**VECTOR attacks — 10 animations** (*T-134, wave 3*). **10 moves, 10 still vanilla's; 9 a player can meet, 7 before level 20.** *Earliest first*: HOMING, GOTO, DRIFT, DISPATCH, PROBE, BALLISTIC, SHEAR, TUNNEL. ***Closes when `python3 tools/animcensus.py --family VECTOR` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `7f6b6c011`; *sheet `gfx/drafts/animations/t146_vector.png`, second turns included*): **a write that TRAVELS** — *CONTENT lands in place and LOGIC in steps; VECTOR leaves the user, the field flickers in its red while it is in flight, and it arrives with a heading: the target is shaken along the line it came in on.* **GOTO and REBOUND** vanish on turn 1 (*the script's own `invisible`, so the battle still reads the user as elsewhere*) and return on turn 2, REBOUND with a downward jolt; **BALLISTIC** aims on turn 1 and fires on turn 2; **TUNNEL** strikes one point again and again; **DRIFT** wavers; **SHEAR** takes the whole defending side; **PROBE** skips the windup. *The theatre gained `sTheatreTurn` (theatre.lua `poke8`), so every two-turn routine's second half can now be filmed — including CONTENT's PRELOAD, RAZOR WIND and ACCRUE, which were approved from turn 1 only.*~~ — ***Done 2026-09-17, approved and released*** (engine `b73373ed3`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-145**~~ | ~~**LOGIC attacks — 22 animations** (*T-134, wave 3*). **22 moves, 22 still vanilla's; 21 a player can meet, 13 before level 20.** *Earliest first*: FALSIFY, SYLLOGISM, CRACK, INDUCT, CONTRADICT, REBUT, LOOP BACK, REFUTE. ***Closes when `python3 tools/animcensus.py --family LOGIC` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `2fbc81aa2`; *sheet `gfx/drafts/animations/t145_logic.png`*): **a derivation, not a blow** — *the type's clause is "rules applied step by step; proof, not intuition", so where CONTENT lands in one pulse LOGIC lands in ordered, ticked steps (more steps for more power) and lets go at once when concluded.* **CONTRADICT, REBUT and REDUCTIO** step the user first and hand the steps on; **NON SEQUITUR** takes a small step, jumps past the next, steps back and leaves the target alternating; **LOOP BACK** climbs and returns twice; **FALSIFY** greys the defending side's screens on the turn it breaks them (*turn 1, not on the sheet*); **EQUATE** is the same three lines every time; **SYLLOGISM** is three; **REFUTE** and **BRUTE FORCE** cost the user; multi-hits and **LEMMA** skip the premise.~~ — ***Done 2026-09-17, approved and released*** (engine `ed2987291`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-144**~~ | ~~**FIELD routines — 41 animations** (*T-134, wave 2*). **41 moves, 41 still vanilla's; 41 a player can meet, 34 before level 20.** *Earliest first*: PARAPHRASE, STEELMAN, DELUGE, UNLOAD, SAFEGUARD, SILENCE, BIT ROT, HEATSOAK. ***Closes when `python3 tools/animcensus.py --family FIELD` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `5521d08d4`; *sheets `gfx/drafts/animations/t144_a_field.png`, `t144_b_field.png`*): **weather** tints the field and is kept short because it REPLAYS every turn it lasts (*`General_Rain` now points at RAIN DANCE, behind the same guard — released by hand with the family*); **screens** layer the user's side; **hazards** are laid in ticks; **restrictions** snap the target toward grey; **SPITE** draws down; **reveals** light the target (white is neutral); **HAZE** greys everything, **PERISH SONG** counts it down in three; **bonds** pulse both; **MEMENTO** spends the user; **NO-OP** does nothing, on time. ***Mechanics kept***: *ROAR and WHIRLWIND keep `AnimTask_SlideOffScreen` (the drag-out depends on it), TELEPORT `AnimTask_Teleport`, CAMOUFLAGE its fade tasks, and BATON PASS its recall sprite — **which is still vanilla's ball, the one picture left in the family**.* *The theatre now restores both daemons' positions and invisibility after each animation, which also fixes the hidden ARTSAI from T-143's first sheet.*~~ — ***Done 2026-09-17, approved and released, `General_Rain` with it*** (engine `7eea50cf5`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-143**~~ | ~~**RAISE routines — 20 animations** (*T-134, wave 2*). **20 moves, 20 still vanilla's; 19 a player can meet, 13 before level 20.** *Earliest first*: COMPOSE, INSTANCE, GENERALISE, SHRINK, COMPACT, SCALE UP, PIN, BRACKET. ***Closes when `python3 tools/animcensus.py --family RAISE` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `5287eea96`; `tools/genanims.py`; *sheet `gfx/drafts/animations/t143_raise.png`*): **rise** — the user's colour climbs toward the routine's type a step per stage gained; speed climbs quickly; two-stat routines (COMPOSE, GENERALISE, ZOOM OUT, BLOOM) climb twice; evasion (INSTANCE, SHRINK) flickers. *Tables derived from each routine's effect, not typed: the stat, the direction and the stage count come from `EFFECT_*`.*~~ — ***Done 2026-09-17, approved and released*** | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-142**~~ | ~~**AFFLICT routines — 21 animations** (*T-134, wave 2*). **21 moves, 21 still vanilla's; 19 a player can meet, 16 before level 20.** *Earliest first*: COMPOUND, PAIR, SATURATE, STARVE, ECHO PROBE, ORPHAN, ADULTERATE, BROWNOUT. ***Closes when `python3 tools/animcensus.py --family AFFLICT` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `5287eea96`; `tools/genanims.py`; *sheet `gfx/drafts/animations/t142_afflict.png`*): **afflict**, a pattern per fault in the routine's own colour — sleep fades to grey and holds (QUEUE SLEEP lighter and slower), poison seeps in by steps (COMPOUND deeper), paralysis flickers with a shake, SMOULDER pulses slowly, confusion alternates colour and grey (OVERPROMISE and MISLEAD raise the target first; TEETER DANCE takes the whole side), PAIR pulses both daemons in step. *Tables derived from each routine's effect, not typed: the stat, the direction and the stage count come from `EFFECT_*`.*~~ — ***Done 2026-09-17, approved and released*** | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-141**~~ | ~~**LOWER routines — 16 animations** (*T-134, wave 2*). **16 moves, 16 still vanilla's; 16 a player can meet, 14 before level 20.** *Earliest first*: VERBOSE, BLUR, STRIP, DEMOTE, SILT, INSPECT, DERATE, EXPOSE. ***Closes when `python3 tools/animcensus.py --family LOWER` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***DRAFTED 2026-09-17*** (engine `5287eea96`; `tools/genanims.py`; *sheet `gfx/drafts/animations/t141_lower.png`*): **sink** — the routine's colour touches the target, then it sinks toward grey a step per stage lost; accuracy-lowering routines dim the field with it (*it cannot see*), sound-based ones (DERATE, STRIP, AUDIT) pulse the field first, speed-lowering ones return slowly. *Tables derived from each routine's effect, not typed: the stat, the direction and the stage count come from `EFFECT_*`.*~~ — ***Done 2026-09-17, approved and released*** | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-140**~~ | ~~**CONTENT attacks — 65 animations** (*T-134, wave 2*). **65 moves, 65 still vanilla's; 60 a player can meet, 40 before level 20.** *Earliest first*: FULL WRITE, DEGRADED, PRUNE, DISPLACE, RAPPORT, DISUSE, PRIOR, AMBIENT.  ***The largest family and the one seen first*** — *40 of these a player can see before level 20; worth splitting by what the move does (strikes, multi-hits, charges, self-damaging) once the rule exists.* ***Closes when `python3 tools/animcensus.py --family CONTENT` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***ALL 65 DRAFTED 2026-09-17*** (engine `cace532ef`; `tools/genanims.py`; *sheets `gfx/drafts/animations/t140_a_writes.png`, `_b_shaped.png`, `_c_special.png`*): **the answer to WRITE — the attacker does not move; its colour pulses toward CONTENT, then the target takes CONTENT and shakes, depth and shake read off the routine's power.** *A vocabulary of steps, one table per family, so every write of one strength is drawn one way*: **cuts** two thin flashes; **recoil** greys the attacker; **binding** holds the target in CONTENT; **rampage** lands twice; **sound** pulses the whole defending side; **spin** three attacker pulses; **taking** (MINT, CLAIM) answers with an attacker pulse; **one-hit KOs** drain the target to black; **TRUNCATE** halves to grey; **EQUALISE** greys both to one depth; **self-destruct** greys the attacker out under a field flash; **FULL WRITE** loads slowly and holds; **two-turn routines** load on turn 0 and land on turn 1. ***For the approval, honestly***: *a plain write is over in about a quarter of a second, and the attacker's CONTENT pulse is invisible on a near-white daemon (ARTSAI) — CONTENT's colour is bone*; **the theatre plays turn 0, so the two-turn landings and DEQUEUE-style branches are not on the sheets.** *`genanims.py` is idempotent (a second run changes nothing), checks its table against every routine of the type with power, and `--release` drops the guards once approved.*~~ — ***Done 2026-09-17, approved as drafted and released*** (engine `9ef8f35ee`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| ~~**T-139**~~ | ~~**HEAL routines — 15 animations** (*T-134, wave 1*). **15 moves, 15 still vanilla's; 13 a player can meet, 9 before level 20.** *Earliest first*: IDLE, RESTORE, ROOT, REGROW, RETRAIN, ENQUEUE, DEQUEUE, CLEAR STATE.  ***The pilot, and why***: **RESTORE is the routine BASIN's gym is built on (LEFTOVERS and a draw forever), and the family is small and all one idea** — *so it is where T-135's rule is first tested on the screen.* ***Closes when `python3 tools/animcensus.py --family HEAL` reports none vanilla, and each is approved in the T-136 theatre against T-135's rule.*** ***ALL FIFTEEN DRAFTED 2026-09-17*** (engine `382bda0af`; *filmstrips `gfx/drafts/animations/t139_heal.png`*): **RESTORE released as 9.24's exemplar; the other fourteen behind `.if DAEMONS_DEBUG`, vanilla's in each `.else`, approved the same day.** *Each is a palette process, script only*: **REPAIR** two jolts put colour back; **IDLE** snaps back whole, then dims and settles; **REFILL** one slow pour; **SLACK OFF** a sag and a drift back; **REGROW** returns through GROWTH; **WARM BOOT** near-black, a CONTENT flash on the field, one ramp up; **NIGHT REPAIR** the field dims to black and the repair ticks twice; **ENQUEUE** a tick and a CONTENT pulse; **DEQUEUE** drains and pops back in as many steps as items stored (*the theatre plays move turn 0, so the sheet shows one*); **DEFERRED** dims and waits; **ROOT** three downward jolts held in GROWTH; **CLEAR STATE** an instant reset; **RETRAIN** and **HEAL BELL** the side-wide reset in GROWTH and CONTENT. ***Every egg, bottle, moon, flower, bell, note, star and Z is gone.*** *`animcensus.py --family HEAL`: 1 ours, 14 draft, 0 vanilla.*~~ — ***Done 2026-09-17, all fifteen approved and released*** (engine `6a42f4fdd`) | `data/battle_anim_scripts.s` | T-134, T-135, T-136, T-137 |
| **T-138** | **112 renamed routines still carry vanilla's description** (*found 2026-09-17 planning T-134*). **The rename pass rewrote 58 descriptions that could not survive their new name (OCCLUDE cannot "bite with vicious fangs"); 112 more kept vanilla's text** — *including RESTORE ("A self-healing move…"), IDLE, REPAIR, REGROW, WARM BOOT and NIGHT REPAIR, the whole healing family T-139 animates.* ***Each is read against its name***: **kept where it already lands, rewritten where it describes the old creature-move** (*2.8: the pass that renames what already lands makes the game worse*); *every line measured against the 170px box, the widest now 126.* ***The list***: *`python3 tools/animcensus.py --descriptions`, which shrinks as each is kept or rewritten — so keeping one on purpose needs a record, not silence.* | `src/move_descriptions.c` | **2.8**, T-134 |
| **T-137** | **The effect graphics — 188 sprites still drawn for vanilla's moves** (*T-134; alongside the families, never ahead of them*). **`tools/animcensus.py --gfx` lists every `ANIM_TAG_` a still-vanilla animation uses, by how many use it** — *orbs, stars, hearts, fists, leaves, notes; `graphics/battle_anims/` holds 1356 sprite files and 153 backgrounds.* ***Redrawn only when a family needs one***: **a shared sprite is a shared vocabulary, so redrawing a fist changes every move that throws it** — *the census names them all before one is touched.* ***Closes when `--gfx` names no graphic a vanilla-meaning animation still depends on.*** ***Named so far by the families***: *SUBSTITUTE's doll (T-156) and BATON PASS's recall ball (T-144) are the two vanilla pictures the released animations still show, both kept because the mechanic runs inside them.* | `graphics/battle_anims/`, `src/data/battle_anim.h` | T-134, T-135 |
| ~~**T-136**~~ | ~~**An animation theatre in the debug build — any routine, played on demand**~~ — ***Done 2026-09-17*** (engine `a3106718b`; `tools/theatre.lua`; `docs/engine.md` §5 and trap 19). **In a debug battle, SELECT at "What will X do?" opens it: LEFT/RIGHT step one routine, UP/DOWN ten, A plays it between the two daemons on the field, START swaps sides, SELECT teaches it to that side's daemon and re-patches the streaks, B returns.** ***L and R were the first design and FireRed's HELP system took them*** — *trap 19.* **Capture: `tools/theatre.lua` runs inside mGBA's Scripting window and reads `.theatre/theatre.cmd` — `hold`, `wait`, `shot`, `burst` — so buttons are held for exact frames and every frame is saved as the GBA drew it** (*a key tapped into the window from outside reached the game about half the time*). ***Proved on screen the same day***: *RESTORE vanilla and draft, PERSPECTIVE and WRITE captured as filmstrips (`gfx/drafts/animations/t135_exemplars.png`), and **T-132's acceptance test passed**: a wild ROVERCUB taught TRICKLE, JITTER, PULSE and WRITE took exactly FLOW's light, ENTROPY's light and SIGNAL's DARK step (the light one vanishes on GROWTH) in battle, and the Index drew the same species with the blank grey (148,148,148).* **Not done: the release ROMs have no theatre, by design** (*`nm` finds no `DbgTheatre` in either*). | `src/` (debug only, `DAEMONS_DEBUG`), `tools/` | T-134, T-131, T-132 |
| ~~**T-135**~~ | ~~**The animation rule — what a routine's animation may show**~~ — ***Decided 2026-09-17: vision.md 9.24, v11.213***; *RESTORE released as its exemplar (engine `382bda0af`)*. (*T-134, wave 0; a decision to draft, not a job to start*). **Written as a 9.x section and approved before any family is touched**: *what an animation depicts (the process the routine names, not the creature's body doing something), where colour may appear (9.4 and PERSPECTIVE's precedent: colour where it means something, grey as the ground), whether a routine's own TYPE colour tints its effects, what the streaks do during an animation, and the budget — script-only (vanilla's effect sprites, re-sequenced and re-tinted) against new effect graphics.* ***Drafted on three exemplars before it is written as a rule***: **RESTORE, PERSPECTIVE (already ours) and one CONTENT attack** — *shown in the T-136 theatre, approved on screen.* ***Drafted and APPROVED 2026-09-17*** (*`gfx/drafts/animations/t135_exemplars.png`*): **the proposed rule** — *(1) an animation shows the PROCESS its routine names, never a creature exerting its body or gathering energy from outside; (2) the daemon's own palette is the medium, and grey is the ground: losing something is drawn as draining toward grey, getting it back as colour returning — which costs invariant 5 nothing (8.6); (3) the only colour an effect may add is a TYPE's — the routine's own, from `TYPE_COLOR` — never vanilla's decorative yellows, pinks and blue stars; (4) the streaks are part of the palette, so whatever happens to a daemon's colour happens to what it knows; (5) in Halftone the frame's greying wins (8.6a) and PERSPECTIVE's flash stays the one exception; (6) script first — re-sequencing and re-tinting vanilla's tasks and sprites — and a new effect graphic only when no existing one can mean the process (T-137).* ***The exemplars***: **RESTORE redrawn (debug ROMs only, `.if DAEMONS_DEBUG`): its colour drains to grey and returns in three ticked steps — a checkpoint reloading**; *PERSPECTIVE already obeys (1), (2) and (4)*; **WRITE (vanilla TACKLE) is the open one** — *a lunge and a shake is a body doing something, and under (1) and (3) a write would land on the target as a brief flash of the attacker's type colour rather than a shove; not built until the rule is approved.* | `docs/vision.md` | **9.4**, T-134, T-136 |
| ~~**T-134**~~ | ~~**Battle animations still show vanilla's moves, not ours** (*asked 2026-09-17, starting from RECOVER*). **309 of 355 moves are renamed and many redescribed, but `data/battle_anim_scripts.s` (356 `Move_` scripts) is vanilla's**: *RESTORE (vanilla RECOVER) still draws power orbs pulled in, blue stars and a yellow flash — a creature gathering energy, where the routine is a process putting back what it lost.* ***Measured for RESTORE***: **learned by level-up by INFERENCE and CONJECTURE (Lv 25), BEACON (15), PULSAR (1), SUBSTRATE (20), OVERLAY (20) and STARR (44 — confirmed wanted 2026-09-17); not by ARTSAI; no TM or tutor teaches it.** *Trainers who use it: BASIN (BEACON, PULSAR — the LEFTOVERS loop fight), MATTE and AL's three champion teams (CONJECTURE), Cooltrainer Paul, Psychic Jaclyn twice and Scientist Gideon.* **Its family, all renamed and all still animated as vanilla's**: *REGROW (Synthesis — the ROVER line), REPAIR (Soft-Boiled — UPTIME, HEARTBEAT), IDLE (Rest — DEADLOCK, SUSPEND and five more), NIGHT REPAIR (Moonlight), WARM BOOT (Morning Sun), REFILL (Milk Drink).* ***Planned 2026-09-17 into T-135–T-162***, *all counted by `tools/animcensus.py`, which reports a family done when its scripts no longer match upstream's*: **wave 0 — T-135 the rule and T-136 a theatre to see animations in** (*nothing is animated before both*); **wave 1 — T-139 HEAL, the pilot** (*RESTORE, and BASIN's fight*); **wave 2 — what a player sees in the first hours**: *CONTENT attacks, LOWER, AFFLICT, RAISE, FIELD*; **wave 3 — the common attacking types and PROTECT**; **wave 4 — the late types and COPY.** *Alongside: T-137 the 188 effect graphics, redrawn only as a family needs one, and T-138 the 112 renamed routines still describing vanilla's move.* ***The discrepancy is settled***: *vision.md v11.212 records RECOVER → RESTORE, and why.* ***The scale***: **354 of 356 animation scripts vanilla, 24 families.**~~ — ***Done 2026-09-18, every family released: `animcensus.py` reports 0 of 356 vanilla (vision.md 9.24, v11.217). T-137 and T-138 stay open on their own*** (engine `2640774b3`) | `data/battle_anim_scripts.s`, `src/battle_anim_*.c`, `graphics/battle_anims/` | **9.4**, 2.8, T-131 |
| **T-133** | **A redrawn daemon's overworld object needs a palette slot, and the maps it stands on have none free** ***Asked 2026-09-18: settle it, and it now gates T-164.*** *Twenty-four redrawn daemons wait on it (`OW_DEFERRED` in `gbasprite.py`).* (*found 2026-09-17 building T-131 batch 1*). **`gbasprite.py` derives an overworld object with its own type ramp, which has to be patched into a slot no other object on that map uses** (`docs/engine.md`, the slot invariant). ***Measured***: *PIDGEY stands in `SaffronCity_House` and `VermilionCity_House3`, PIKACHU in `FourIsland_LoreleisHouse`, `SaffronCity_CopycatsHouse_2F` and `VermilionCity_PokemonFanClub` — Lorelei's house fills all four NPC slots, and each of the other four has a town local in `PALSLOT_NPC_SPECIAL`.* **So both keep vanilla's object art for now (`OW_DEFERRED` in `gbasprite.py`).** ***And check the two already wired***: *DEADLOCK's object takes `PALSLOT_NPC_1` and PING's `PALSLOT_NPC_4` with their own tags — every map they stand on needs the same census, or they recolour a blue or white NPC by spawn order.* ***The choice to make***: *a second special slot (GBA has 16 OBJ palettes; FireRed's table uses 12), per-map slot reassignment, or a shared per-type palette per slot.* | `src/event_object_movement.c`, `src/data/object_events/object_event_graphics_info.h`, `tools/gbasprite.py` | **9.4**, T-131, T-126 |
| **T-131** | **Every daemon sprite is remade for the GBA** (*planned 2026-09-17*). **The 33 drawn so far were made for the Game Boy engine and ALL are remade — 31 are 4-tone art centred at native size, and even PING and DEADLOCK, drawn to 9.4's amended spec, predate the decisions below.** ***Scope, measured***: *about 190–200 species a player can meet or evolve into* (**145 met directly — wild, gift, static, trade, and the parties of trainers who can actually be battled — plus what they evolve into; the count is approximate because the starters, the MUSAI line and ARTSAI are given by paths the `givemon` scan does not see**); **everything else in `trainers.h` keeps vanilla's under the portraits' cut-off.** ***Per species, two drawings***: *a front and a back, in neutral greys*; **`gbasprite.py` applies colour and DERIVES the party icon and the overworld sprite** (*icons cannot carry the type colour — vanilla has three icon palettes*). ***Decided 2026-09-17***: **the rich 64px standard for all; a contact sheet per batch of about ten, approved before it is built; waves in the order a player meets them** — *Routes 1–4, Viridian Forest and the CALLOW and SLATE rosters first, THE MARGINS last in their own register.* ***Each batch***: *draft → contact sheet → greyscale cleanup → `gbasprite.py --write` → four ROMs → decode check (at the sprite's real geometry) and bytes in ROM → census.* ***BLOCKED on two things***: ~~**(1) a generator this session can reach**~~ — ***resolved 2026-09-17: ComfyUI at `roverbyteseer.local:8008` with `pixelArtDiffusionXL_spriteShaper`, driven by `tools/spriteforge.py` (see `ai/README.md`)*** — *`daemon/sprite`'s backends are Bonsai (local) and Gemini via OpenRouter; pixelbyte is a local ComfyUI running `pixelArtDiffusionXL_spriteShaper`; on 2026-09-17 the n8n relay (10.0.0.136:5678), ComfyUI (:8008) and Bonsai (:8000) all refused connections from this Mac, and pixelbyte's scripts are not installed here — so the comparison the user asked for (`daemon/sprite` against pixelbyte on the same species) waits on one of them;* ~~**(2) the STREAK colour rule**~~ — ***decided 2026-09-17 and written as 9.4's amendment (v11.209)***: *the streaks are the types of the moves a daemon KNOWS, one per move slot; the body keeps its own type; species-only views show them blank.* **So each drawing now also marks four streak regions, and the engine half is T-132.** ***The recipe, settled on NIBBLE 2026-09-17*** (*`ai/README.md`, `tools/cleandraft.py`*): **512² — the model's 8px grid makes that the 64px frame, ~23 s a view; one seed, and two mirrored prompts, "front view, facing the viewer" and "rear view, facing away"; a MID-grey body asked for by name; `cleandraft.py` takes the background by flood fill, writes transparency and paints the four streak markers.** *NIBBLE's contact sheet (Index blank, wild, four taught types, back) was the recipe's test, not batch 1's approval — the drafts are not in the game.* ***BATCH 1 BUILT 2026-09-17*** (*sheets approved the same day; `gfx/drafts/batch1/`*): **ROVERCUB, LABEL, CLUSTER, PACKET, NIBBLE, CRAWLER, SCRAPER, PENDING, BUFFER, SPIKE** — *front, back, palette and icon in all four ROMs byte for byte, both views decoding to the source at 64×64, the ten icon palette indices set from `gbasprite.py`'s fit, `streaks.h` marking exactly these ten.* ***Drawn from their approved concepts, not from prompts alone***: *`tools/prepconcept.py` sets each old drawing on a chroma-green key and `spriteforge.py i2i` redraws it at 512 and denoise 0.55; `tools/contactsheet.py` draws the sheet through `gbasprite.py`'s own colouring.* ***Found on the way***: **`gbasprite.py` read the Game Boy build's frozen names, so LABEL and CLUSTER would have been built from the old art** (*`GBA_NAME` now maps LABL and CLUSTR*); **PACKET's and SPIKE's overworld objects have no palette slot to go to — T-133.** ***The in-battle acceptance test PASSED 2026-09-17 in the T-136 theatre*** — *a wild ROVERCUB taught four routines of four types took their streak colours in battle, and the Index drew it blank grey.* ***BATCH 2 BUILT 2026-09-18*** (engine `f3e6c10fe`; *sheets `gfx/drafts/batch2/sheet_a.png` and `sheet_b.png`, approved the same day*): **HEAP, BACKBONE, SECTOR, WORM, PREEMPT, FORK, BRANCH, PING, SUSPEND** — *SLATE's benchmark, Camper Liam and Youngster Ben, and Route 3's wild table.* ***Seven had no concept*** *and vanilla's sprites are never fed in, so they were drawn from words with the NIBBLE recipe, the body kept recognisable and the detail from the name and Index entry;* **the model draws a rear view as a front for serpents and will not draw a horn on the rabbits, so FORK and BRANCH have none, and several views took three passes and several seeds** (*`PICKS.txt` records which draft each view is; `fix.py` clears SECTOR's drawn box and the grass, and desaturates PREEMPT, which came back painted blue and orange and would have been turned red by `gbasprite.py`'s accent shift*). **PING was drawn from its concept's hummingbird, perched attempts rejected.** *Built as batch 1 was — all eighteen views decode to their drawings at 64×64 and are in all four ROMs byte for byte, with their palettes; nine icon palette indices set from the fit; `streaks.h` flags nineteen.* ***Found building it***: **`gbasprite.py` only knew the Game Boy build's 66 renames, so a species renamed only on the GBA (seven of these nine) was never looked at** — *it now adds any GBA-renamed species with art in `gfx/daemons/`;* **JIGGLYPUFF's and both NIDORANs' overworld objects join PIDGEY and PIKACHU in `OW_DEFERRED` until T-133's census** (*they stand in six maps, including Lorelei's house*). *Also found: `roverbyteseer.local` does not resolve off the home Wi-Fi; `SPRITEFORGE_HOST=http://100.67.234.4:8008` reaches it over Tailscale.* ***Noted, not decided***: *`PIXELBYTE` was suggested as a daemon name.* ***BATCH 3 BUILT 2026-09-18*** (engine `1a929ff44`; *sheets `gfx/drafts/batch3/sheet_a.png` and `sheet_b.png`, approved the same day; all eighteen views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags twenty-eight; CLEFAIRY's and PSYDUCK's overworld objects join `OW_DEFERRED` for T-133; daemon fronts 49 of 386 in `docs/sprites.md`*) ***BATCH 4 BUILT 2026-09-18*** (engine `0a489413f`; *sheets approved the same day; all twenty-two views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags thirty-nine; VOLTORB's overworld object joins `OW_DEFERRED`; daemon fronts 60 of 386*) ***BATCH 5 BUILT 2026-09-18*** (engine `41ebfa7ba`; *sheets approved the same day; all twenty-two views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags fifty; MACHOP's and MEOWTH's overworld objects join `OW_DEFERRED`, ten in all waiting on T-133; daemon fronts 65 of 386*) ***BATCH 6 BUILT 2026-09-18*** (engine `1fddaaeea`; *sheets approved the same day; all twenty-four views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags sixty-two; CUBONE's, FEAROW's, PIDGEOT's, SLOWPOKE's and WIGGLYTUFF's overworld objects join `OW_DEFERRED`, fifteen in all waiting on T-133; daemon fronts 72 of 386; `gfx/front/` has no concept left undrawn* — ***wrong, corrected in batch 9***) ***BATCH 7 BUILT 2026-09-18*** (engine `ee61a717c`; *sheets approved the same day; all twenty-four views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags seventy-four; DODUO's overworld object joins `OW_DEFERRED`, sixteen in all waiting on T-133; daemon fronts 84 of 386*) ***BATCH 8 BUILT 2026-09-18*** (engine `45c6dfd61`; *sheets approved the same day; all twenty-four views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags eighty-six; CHANSEY's and KANGASKHAN's overworld objects join `OW_DEFERRED`, eighteen in all waiting on T-133; daemon fronts 96 of 386*) ***BATCH 9 BUILT 2026-09-18*** (engine `8c77b10da`; *sheets approved the same day; all twenty-four views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags ninety-eight; MEW's, MEWTWO's and NIDORINO's overworld objects join `OW_DEFERRED`, twenty-one in all waiting on T-133; daemon fronts 99 of 386; every concept in `gfx/front/` now built, checked by name against `gfx/daemons/`*) ***BATCH 10 BUILT 2026-09-18*** (engine `e8b7a77e0`; *sheets approved the same day, HANDLER included as drawn; all twenty-four views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags 110; MACHOKE's and SEEL's overworld objects join `OW_DEFERRED`, twenty-three in all waiting on T-133; daemon fronts 111 of 386*) ***BATCH 11 BUILT 2026-09-18*** (engine `196673c67`; *sheets approved the same day; all twenty-two views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags 121; SLOWBRO's overworld object joins `OW_DEFERRED`, twenty-four in all waiting on T-133; daemon fronts 122 of 386*) ***BATCH 12 BUILT 2026-09-18*** (engine `d2aed6b71`; *sheets approved the same day; all twenty-four views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags 133; LAPRAS's overworld object joins `OW_DEFERRED`, twenty-five in all waiting on T-133; daemon fronts 134 of 386; `gbasprite.py` now reads a type from the GBA's `species_info.h` when the Game Boy has no base_stats for the species — RESENTMENT, the first beyond Kanto's 151, was skipped until it did*) ***BATCH 13 BUILT 2026-09-19*** (engine `db6b409b9`; *sheets approved the same day; all twenty views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags 143; no new overworld objects; daemon fronts 144 of 386 — the start of the game through the League is redrawn but for MISSINGNO (T-163), and THE MARGINS remain*) ***BATCH 14 BUILT 2026-09-19*** (engine `4bb3707ab`; *sheets approved the same day; all twenty-four views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags 155; no new overworld objects; daemon fronts 156 of 386; thirteen of THE MARGINS remain*) ***BATCH 15 BUILT 2026-09-19*** (engine `0e704bf90`; *sheets approved the same day; all twenty-six views decode at 64×64 and are in all four ROMs with their palettes; `streaks.h` flags 168; daemon fronts 169 of 386*) ***EVERY DAEMON A PLAYER CAN MEET IS REDRAWN*** — **batches 1–15, 168 species with streaks, from the first route through the League and THE MARGINS.** *Left under this ticket: MISSINGNO (T-163), the backs that face forward (T-166, twenty-five), the twenty-five overworld objects (T-133), and the species the player can never meet, which keep vanilla's art by the scope written above.*: **THE MARGINS end — VIGILANCE, RUMINATE, PERIPHERY, COMFORT, GRASP, HOARDING, ILLUSION, IMITATION, STARTLE, INSTINCT, CALLOUS, REPAY, ARMOURING.** *ILLUSION took the most work: seed 42's back lost its head and antlers to the fill, seed 1917 was drawn inside a picture frame that `keep_largest` kept (the frame IS the largest shape), and the dark redraw is side-on — on T-166's list. PERIPHERY's and STARTLE's boxes cleared; four coloured drafts greyed after cleaning.*: **THE MARGINS begin — TENSION, APATHY, BLINDSPOT, WHIM, AMBUSH, HUDDLE, ALARM, RECALL, FIXATION, SIMMER, OMEN, BRISTLE**, *the first twelve of the islands' twenty-five, all from words, every view cleaned with no hand-placed box.* ***Open, for the user***: **0.6 and 8.2b give the islands their own SUBJECT and TONE (mind, where Kanto is machinery) but no visual register was ever decided**, *so they are drawn as Kanto's are. APATHY sits in a mud pool, as its entry says, and the mud keeps a brown ground under it.*: **LEMMA MIND, BULLDOZER, SINGULAR, IMPULSE, DREAD, HARDLINE, MULTICAST, TURBULENCE, DRIVE, GRIEVANCE** — *the last of the League and the champion's teams. LEMMA MIND's space is an underscore in its file name (`GBA_NAME` in `gbasprite.py`); the game's name is unchanged. MULTICAST's dark box sat inside a pale strip no corner fill could reach, so it is cleared by colour and then everything but the bat itself.*: **PIPELINE, FERRY, SLURP, VICE, DRAGNET, COLDVAULT, TRACKER, WIRETAP, HAUNTPROC, COLDREAD, RESENTMENT, FOSSILNET** — *Victory Road, the League and the champion's teams; LEMMA MIND held for batch 13 because its name has a space the art pipeline's file names do not handle yet.* ***COLDREAD is a snowy owl***: *its first drafts drew a pale human woman, too near vanilla's caricature for this slot, and were replaced — the owl states what you are about to say.* *PIPELINE, WIRETAP and HAUNTPROC redrawn dark after losing pale bodies to the fill; FERRY's and TRACKER's boxes removed; four coloured drafts greyed after cleaning.*: **CRYOGEN, DYNAMO, SHELL, FLICKER, WILDFIRE, CONJECTURE, AXIOMKICK, REBUTTAL, OVERDRIVE, SCHEDULER, GUARDIAN**, *all from words, the end of the Fuchsia-to-Cinnabar roster.* *SHELL first drew a bare seashell and AXIOMKICK and REBUTTAL drew people; all three redrawn with people and armour negative. SCHEDULER still reads as an armoured ogre.* ***MISSINGNO left out on purpose***: *it stands in an old Unown slot at level 80, and what a glitch daemon looks like is a design question, not a redraw — for the user.*: **QUORUM, CHILLER, JETSTREAM, SMOGSTACK, TRANCE, BREAKER, THEOREM, MIME, LANDFILL, LIVELOCK, CAIRNLING, HANDLER**, *all from words, Fuchsia to Cinnabar.* *HANDLER's first ducks lost their white heads to the fill and were redrawn dark; its thin legs still sample as one solid block — flagged as the weakest of the batch. MIME reads as a human clown and THEOREM as a bodybuilder, as PROOF did. MIME, HANDLER and JETSTREAM greyed after cleaning; CAIRNLING's seed 42 drew a skeleton warrior.*: **the story's own — CANON, MANIFOLD, ROVERBYTE, MUSAI, CAREMUSAI, CODEMUSAI, SEEKMUSAI, ARTSAI, STARR, from their concepts — and PREDICTOR, THREAD, SPYWARE from words.** ***A correction***: *batch 6's note said `gfx/front/` had no concept left undrawn; nine were, because the check compared filenames and these nine had never been built. Found building batch 9's roster.* *STARR's concept redraw was brown and greyed after cleaning (`fix.py`); two hand-placed streak boxes. SMOGSTACK, TRANCE, LANDFILL, QUORUM, CHILLER, JETSTREAM and the rest from Fuchsia to Cinnabar are batch 10.*: **TARPIT, BADSEED, ENSEMBLE, KEEPER, SEEDLING, UPSTREAM, REAPER, UPTIME, MOCK, ROOTKIT, EMERGENCE, STAMPEDE**, *all from words — Celadon's last four and Fuchsia and the Safari Zone; PREDICTOR, THREAD, SPYWARE, TRANCE, LANDFILL and SMOGSTACK start batch 9.* ***"Small in the frame" is now in every prompt, and it worked***: *all twenty-four views cleaned with no hand-placed streak box, the first batch to.* *BADSEED and ENSEMBLE first drew potted flowers and headless palm trees and were redrawn with pots and trees negative; ENSEMBLE still has one head, not several. Five coloured drafts cleaned in colour and greyed after; REAPER's enclosed background cleared by colour.*: **WISP, DUALCORE, REVENANT, BOOTSTRAP, BLIGHT, HONEYPOT, OUTBREAK, PARTITION, RAMROD, MITE, TRACER, SPAGHETTI**, *all from words — Lavender and Halftone Tower, Routes 7, 8, 12 and 16, Celadon's benchmark and the hideout; TARPIT, BADSEED, ENSEMBLE and KEEPER start batch 8.* ***A new failure, and its fix***: **a draft that fills the frame with a pale body loses the body to the background fill** (*DUALCORE's first pairs, BLIGHT's first picks*); **redrawn "small in the frame" and dark it survives**, *and a coloured draft whose pale parts would match the background once greyed is now cleaned IN COLOUR and greyed after, every pixel but the streak markers (batch 7's `fix.py`).* *The model will not draw one body with two heads, so DUALCORE is two birds side by side — which is its Index entry's "Two of them".*: **INDEXER, INJECTOR, BROADCAST, FLOOD, HIBERNATE from their concepts — the last five in `gfx/front/`, finishing the lines batches 1–2 began — and TAPPOINT, SENTRY, HOTPATH, INFERENCE, LATENCY, STACK, RELIC from words** (*Vermilion to Rock Tunnel and Diglett's Cave; MITE and BLIGHT are batch 7*). *RELIC's first backs drew a skeleton warrior and were redrawn as a mouse from behind; TAPPOINT's square had a darker rim, so `fix.py` gains `unrim` and `despeck`.*: **RELAY, OVERFLOW, SURGE, RUBRIC, LOCUS, ROVERSEER from their concepts (as batch 1), and CAPSULE, PROOF, LULL, SPINLOCK, COOKIE from words** — *the rest of the Doldrum area, Vermilion's benchmark lead and the rival's first evolutions; SENTRY, HOTPATH, INFERENCE, MITE and BLIGHT are batch 6.* *`fix.py` gains `unshadow` for a drop shadow touching the feet (LULL), which the background fill cannot reach.*: **the Mt. Moon Super Nerds' four and the Doldrum routes and BASIN — SENTINEL, FUSE, SLUDGE, FUMES, HUNCH, SNARE, WEED, SPAWN, LOOP, BEACON, PULSAR**, *eleven from words; CAPSULE, RELAY, PROOF, LULL and SPINLOCK are batch 5.* *Six views desaturated before cleaning, two hand-placed streak boxes, and LOOP's front drawn on a dark square (`fix.py`) — whose automatic strokes had landed on the square, so a box drawn on a square now gets its streaks placed by hand too.*: **Mt. Moon and Route 4 — ECHO, ANOMALY, PAYLOAD, FAULT, ESCALATE, NOZZLE, PINCER, STUB, DRIFTNET**, *all nine from words (none had a concept), two seeds per view up front, which took one pass where batch 2 took three.* ***Two rules the batch made standard***: **a draft drawn with colour in its BODY is desaturated before cleaning** (*PAYLOAD's green caps, DRIFTNET's blue dome, and the four swimmers drawn pale blue, which `gbasprite.py`'s accent shift turned GOLD on a FLOW body*); **and a pale body can be emptied by the background fill** (*FAULT's first back became an outline, and was redrawn darker*). *Four views take hand-placed streak boxes (`PICKS.txt`).* | `gfx/front/`, `gfx/back/`, `gfx/daemons/`, `tools/gbasprite.py`, `graphics/pokemon/` | **9.4**, T-117, T-120 |
| ~~**T-130**~~ | ~~**Two Index entries were never written: WORM and MIME**~~ — ***Done 2026-09-17*** (engine `a6e482ff9`) (*found 2026-09-17 by T-47's read*). **Both are vanilla prose in both editions, and the Game Boy source never wrote them either** — *so the port carried nothing, and nobody noticed because each had been touched once.* **WORM** (*Ekans slot, `PROPAGATE`, on Route 4*) *is vanilla's LeafGreen line with `port_names.py`'s substitution run through it — "Eats the eggs of birds, such as PACKET and PING, whole", with no full stop* — **in BOTH editions, so FireRed shows LeafGreen's vanilla.** **MIME** (*Mr. Mime slot, `IMITATIVE`, MATTE's lead*) *is vanilla word for word in FireRed — "adept at conning people" — and vanilla with a move renamed in LeafGreen, "will suddenly REPEAT the offender".* **Both are GAMUT species, so each wants machinery as its subject and the mind left unsaid, CONTENT saying what it is and CONTEXT what somebody made of it, as SUBSTRATE's pair does.** *New Index prose is writing and wants its wording approved before it goes in* ***Written, with the wording approved first***: **WORM** — *CONTENT "It copies itself into anything that will run it, and leaves from there. It does not need to be carried. It does not stop." CONTEXT "Nobody sent it. Everyone who received it passed it on. It is counted by where it has been, and it has been everywhere."* **MIME** — *CONTENT "It mimes a wall with its hands, and things stop at the wall. There is no wall. Nothing that stopped has checked." CONTEXT "It copies what blocks the way, down to how the hands are held. Everything that meets the copy treats it as the thing."* ***A character count is not a width***: **at forty-two characters MIME's CONTEXT line measured 239px against the 234px Index line vanilla demonstrates, and at that width the approved wording needed four lines** — *so the text is wrapped by pixels, and one word was trimmed ("whatever" → "what"), with approval.* **The widest Index line is 234px in both editions; each text is in its own edition's two ROMs and not the other's, and the vanilla lines are gone from all four.** *The Game Boy build is a reference and not updated further, so its `dex_text.asm` keeps vanilla's.* | `src/data/pokemon/pokedex_text_fr.h`, `_lg.h` | **0.6**, **4.25**, T-47 |
| ~~**T-129**~~ | ~~**The player's reflection in water is vanilla's colours**~~ — ***Done 2026-09-17*** (`tools/genreflection.py`; engine `4ddf96d79`) (*found 2026-09-17 by the title-strike sweep: T-52 recorded it as "still open" on 2026-09-14 and nothing picked it up*). **`graphics/object_events/palettes/player_reflection.pal` is byte-identical to `upstream/master`** *while `player.pal` — the figure you walk as — is ours*, **so on any reflective tile the figure beneath you is drawn in the old colours.** *T-52 notes the surf body moved onto the BOX's greys, which the reflection should follow too.* **Derive the reflection from `player.pal` the way vanilla derives its own (a darkened, blued copy of the same sixteen), rather than typing a second palette, and check it on a reflective map in both editions** ***The reflection is DERIVED, by a rule fitted to vanilla's own five palette/reflection pairs***: **every colour kept at 75% and lifted by (89, 98, 90).** ***The model with the best score was the wrong one***: *a nearest-colour rule predicted vanilla's held-out colours best (11.5 of 255 against 15.4), but on OUR palette it turned the near-black brown, the tan mint and the cream icy cyan — hue drift averaging 32° and reaching 137°. Its error was measured on vanilla's saturated colours, and this figure is greys and browns, whose nearest saturated neighbours lend them their hue.* **The one-tint mix cannot turn a grey into a colour, drifts 6°, and scores the same as a line.** *The tool finds stale reflections itself — a base that differs from upstream with a reflection that does not — so a palette redrawn later cannot quietly repeat this; `player.pal` was the only one.* | `graphics/object_events/palettes/player_reflection.pal`, `tools/gbaplayer.py` | **9.10**, T-52 |
| ~~**T-125**~~ | ~~**The town interiors are vanilla's**~~ (*asked 2026-09-16: "adjust all overworld and buildings sprites that remain"; scoped "interiors only, skip hoenn_building"*). ***Buildings are TILESETS, not sprites***: 21 still-vanilla secondary tilesets over 109 layout-slots, and **the interiors are not wholly vanilla today** — `gTileset_Building` is the primary for 184 layouts and is already ours, so what is vanilla is the per-building-type overlay. *Measured properly, that overlay is most of what you see: **21,287 of 29,601 cells (72%)** draw from the vanilla secondary, and per building it runs 99.9% (pokemon_tower), 98.8% (cable_club), 96.2% (condominiums) down to 6.1% (generic_building_1, effectively free to skip).* ***`pokemon_tower` IS OUT OF SCOPE AND MUST STAY OUT***: **8.6a is "decided and built 2026-09-13" — Halftone is grey, all seven floors, tiles and people** — *and `fieldmap.c:843-884` ships `DaemonsIsHalftone()` / `DaemonsGreyHalftoneFrame()` to do it once a frame, so colour drawn there is erased every frame.* ***The scripted-id hazard is narrow and known***: only `PokemonLeague` (9 names, 20 refs), `GameCorner` (9/12), `TrainerTower` (4/4) and `DepartmentStore` (1/1) carry `METATILE_` labels — `metatile_labels.h` is hand-maintained, so these are literal addresses — **and `CeladonCity_GameCorner/scripts.inc` rewrites five cells to open the hideout stair (`0x29D 0x29E 0x29F 0x2A6 0x2A7`, shut `0x292`/`0x2CF`) while `cable_club.inc` reaches into `METATILE_GameCorner_*` from another map entirely.** ***VERDIGRIS FIRST, from a concept approved 2026-09-16***: *the frame, in commerce* — **bronze where the building asserts itself, verdigris ONLY where hands and time reach, green only on what is alive** — *the store is the thing that went up (craft rule 2's kid outside is right), the hideout is an office under a floor Corpus already owns (the sign reads CORPUS GAME CORNER), and the hotel keeps no daemon fixture because it is "a hotel for people".* ***Done 2026-09-16 — FOUR of the five buildings, 13 rooms, 0 collision changes across every one of them***: `gTileset_VerdigrisTable` *(the restaurant and hotel, 40 blocks / 105 tiles)*, `_VerdigrisStore` *(six floors, 57 / 100)*, `_VerdigrisBlock` *(four floors, 41 / 72)* and `_VerdigrisFloor` *(three rooms, 47 / 86, with all nine script-pinned ids reserved so the hideout stair still opens)*. ***It took three drafts and each failure was specific***: *(1) classifying `behaviour == 0` as wall swallowed every table, machine bank and the school's long table — **wall is what a border flood-fill reaches through blocked, ROLE-LESS cells**, and 98 distinct vanilla blocks came back as 5; (2) drawing per cell rather than per **cluster** — a kitchen is one 5×7 block holding a counter, a stove, a dresser and a plate; (3) structure without **surface** — vanilla spends 16–25% of every floor cell on its pattern and the draft spent 0.8%, so 57% of the room was one flat colour.* **Fixed by deriving rather than inventing**: *fixtures from the game's own metatile behaviours (128 counter, 130 shelf, 157 window…), wall kinds from what vanilla drew there (**27 hotel wall ids collapse to five kinds** by colour signature), and the two rooms given separate grounds because vanilla does — commonest colour 56.8% → 23.5%.* ***And a chair landed on an NPC***: *drawing chairs on open floor put one under a T-119 local at (1,2); `_map_events()` now derives every warp/object/sign cell from each map.json's own `layout` field, since the layout id does not name its map and mangling only works by luck.* ***The second batch taught three more, and all three were the same mistake — a derived rule applied too bluntly***: *(1) **a window is an OPENING, not a colour** — the classifier keys on blue, so the Game Corner's sky band and the whole store roof came out a field of panes; when windows are more than a third of a room's wall they are not windows, and they are demoted to whatever that room's wall is otherwise made of (it fired on exactly three rooms — roof 47.8%, Game Corner 47.9%, prize room 43.5% — and left the condominiums' real windows at 12–18% alone); (2) **"dark" is the surround vanilla leaves OUTSIDE a room, never a wall material** — letting it win the demotion vote turned that sky band into black slabs; (3) **each trade needs its own GROUND** — a third warm neutral made the condominiums look unfinished, and cool stone against the store's parquet and the hotel's carpet is what made them read as three buildings.* **And two grounds needed their own pattern rather than `_weave`: the Game Corner is a CHEQUER (a grid you stand inside) and the landings are TILED (grout on an 8px grid).** *One misdiagnosis corrected on the way: the black bars down the condominiums' landings are vanilla's own, not a bug of ours.* ***THE SCHOOL IS SPLIT PER TOWN, decided 2026-09-16, and that closes this ticket***: **one tileset served two rooms in two towns doing two different things** — *`VIRIDIAN_CITY_SCHOOL` is **CALLOW's** classroom and `CELADON_CITY_CONDOMINIUMS_ROOF_ROOM` is **VERDIGRIS's** lecture room, which is not a classroom at all: it reads you a DAEMON Lecture and hands over a MUSAI.* **Their plans are all but identical** (*windows at (1,1), a region map at (7,1), bookshelves at (8,2), a long table at (4,4), the mat at (4,7); the lecture room adds a lectern at (7,3)*), **so the split is about TOWN and FUNCTION, not layout** — *a tileset apiece, as `checkpoint_indigo` already serves exactly one layout.* ***CALLOW's classroom is built of its own BENCHMARK's materials***: **poured concrete under a surveyor's grid (T-113), so THE GRID ON THE BOARD IS THE GRID UNDER YOUR FEET** — *the type table drawn as a lattice of identical chalk cells, the desks facing it, and the one living thing in the room a plant on the sill grown past its cane, which nobody remarks on.* **The room's own shipped text was already the lesson**: *"I have to have the table by Friday. I do not have to understand it. I have to have it." / "It is all on the board." / "Learn the table. It does not change."* ***VERDIGRIS's lecture room is built of the block it sits on top of*** — *cool stone landings, bronze fittings, verdigris only where a hand goes — and its board is a lecture board rather than a table to be learned.* **`gTileset_CallowSchool` 16 blocks / 43 tiles, `gTileset_VerdigrisLecture` 13 / 26, 0 collision changes in either, and `gTileset_School` now has ZERO consumers.** ***THE SCHOOL SPANS TWO TOWNS AND THAT IS OPEN***: *`VIRIDIAN_CITY_SCHOOL` is **CALLOW's** and `CELADON_CITY_CONDOMINIUMS_ROOF_ROOM` is Verdigris's — and the second is not a schoolroom but a **lecture room** (it hands out a MUSAI). The concept's sharpest paragraph was built on Callow's text.* **Split the tileset per town, defer it as a Callow ticket, or accept one shared instruction-room idiom — undecided** | `data/tilesets/secondary/`, `tools/gbainterior.py` | **9.4**, 9.23, 8.6a, T-108, T-117 |
| ~~**T-124**~~ | ~~**The three obstacles a field move clears are still vanilla's**~~ — ***Done 2026-09-16*** (`tools/genprops.py`): **`cut_tree` (55 objects), `rock_smash_rock` (97) and `strength_boulder` (58) — 210 objects on three 16×16 sheets, redrawn.** *They are not miscellaneous: they are the three things a field move removes, and the bible had already named all three moves — **CUT → PRUNE** ("the obstacle is literally a tree"), **ROCK SMASH → CRACK** ("you crack a rock and you crack a problem"), **STRENGTH → DISPLACE**.* ***The tree is the one that had to change***: **T-61 replaced vanilla's conifer across the General tileset with a round broadleaf and it is shipped — 204 of that tileset's 640 tiles differ from vanilla — and Route 2 loads `gTileset_General` and carries five cut-trees**, *so our broadleaves and vanilla's dark bush were on screen together.* **And our tree is the LIGHT one, which is the reverse of what the first draft assumed**: *measured off `broadleaf()`, LIT 38.5% + LEAF 36.8% against DARK 13.4% and DEEP 11.3%, where vanilla's `cut_tree` spends 52% of its ink on the darkest green.* `npc_green` *has no LIT, so both light bands collapse onto `l` and it becomes the BODY colour rather than a highlight; luminance lines the ramp up (l 178 / J 111 / N 54 against LEAF 169 / DARK 110 / DEEP 71).* ***The rocks go grey, and that is a judgment call with nothing in the bible ruling on it***: **`item_ball.png` — ours, 16×16, in the same `npc_white` slot — spends four colours and no dither, a hard `K` rim over flat `w`/`g`/`G` bands**, *which is also SLATE's vocabulary; vanilla's olive-and-sand dither reads as mossy earth.* **The rock is angular and fissured and the boulder smooth and round, so the two never read as one object** — *one is broken open, the other shoved aside.* **Reversible: revert this file's ROCK and BOULDER art and rerun.** ***THE FIRST "REDRAW" WAS A TRANSCRIPTION AND WAS DISCARDED***: **it came out 248 of 256 cells identical to vanilla — twelve of sixteen rows byte for byte** — *which is not a conversion, and would have put Nintendo-derived pixels into THIS repo as source literals, the one thing `CLAUDE.md` promises not to do.* **`gbatrees.py` and `gbaground.py` do derive from vanilla pixel by pixel, but they READ it out of `engineGba` at runtime; `gbacopyright.py` lifts vanilla's font and SAYS SO, with a reason.** *A tree has no such reason — its whole job is to stop matching vanilla.* **The shipped art is 15% / 7% / 9% identical to vanilla over INK cells.** ***Four wrong readings of one 16×16 sprite came first***, *each inferred from a projection and each refuted by the next measurement, until a connected-component pass answered it outright* — **engine.md trap 15 now carries the general form.** ***And the tool imports nothing that writes***: **`import gbatrees` RUNS it, and under `--write` its own flag reads `True` off the shared `sys.argv`, which would have rewritten the tileset** — *proved, not suspected; see trap 16.* **`strength_boulder` keeps NO conversion rule and must not be given one** — *every single-frame 16×16 misc sheet goes without, and one frame in raster order is already correct.* | `graphics/object_events/pics/misc/`, `tools/genprops.py` | **9.4**, 9.22, T-61, T-117 |
| ~~**T-128**~~ | ~~**Four PAIR classes stand as two single sprites of the wrong animals**~~ — ***Done 2026-09-16*** (`tools/genpairs.py`; engine `5392c4812`) (*found by `tools/spritejoin.py` 2026-09-16, 28 objects*). **A pair class is ONE trainer standing as TWO map objects**, *so "one species per class" is the wrong question — the picture has to be the two animals those two sheets ALREADY are.* ***T-126 settled the method on `COOL_COUPLE`***: **its two ibexes became a wolf and a falcon, because a pair SHEET would serve one battle in the game and leave both singles still wrong.** *The same move is owed to four more*: **`TWINS` two quails standing as two piglets ×8** (*Routes 8 and 14, Water Path, Bond Bridge*), **`YOUNG_COUPLE` two swans standing as a gazelle and an ox ×6**, **`SIS_AND_BRO` two rabbits standing as an axolotl and a duckling ×6**, **`CRUSH_KIN` two wolverines standing as a kangaroo and a wolverine ×4** (*half right already — `crush_girl` IS the wolverine; it is `black_belt`'s kangaroo that disagrees*). ***`SR_AND_JR` and `OLD_COUPLE` are NOT on this list***: *neither stands on a map, so neither is reachable.* **Method is `gencouple.py`'s — clear the heads, redraw on the portrait's own sixteen, assert the bodies came out byte-identical** ***All four drawn***: **`TWINS` two piglets, `YOUNG_COUPLE` an ox and a gazelle, `SIS_AND_BRO` a duckling and an axolotl, `CRUSH_KIN` a kangaroo and the wolverine it already had.** ***No colour was added, and that was CHECKED rather than assumed***: *the obvious move is to re-dye the indices the old head had to itself and there are **none** — all four heads draw from the same ramp as the body under them, so a re-dye would repaint the clothes.* **Each head is drawn inside the sixteen the picture already has.** ***The kangaroo took FIVE drafts and the lesson is an old one***: *two of them built the head WIDE, filling the wolverine's own width, and both read as a helmet with horns — on a wide flat skull any pair of uprights at the corners is a horn, which is now the **fourth** time that failure has cost this project time (the moth, the hedgehog, the honeybee, this).* **Measuring `BLACK_BELT`'s own portrait settled it: a kangaroo's head is a NARROW WEDGE — twelve columns against a twenty-column body — and its ears angle out from the crown rather than standing on the corners.** ***And the FEET are not a detail***: *a piglet on splayed quail feet is the same contradiction one figure down*, **so the twins get cleft trotters and the couple cloven hooves** (*the legs above them were already thin enough for any of these four*). **Every run asserts the bodies came out byte-identical; only the cleared boxes may change.** *`spritejoin.py` now reads a pair's note as a SET — membership, not equality — and all nine pairings report accounted for.* ***The one disagreement left in the game is T-127*** | `graphics/trainers/front_pics/`, `tools/genpairs.py` | **9.4**, T-120, T-126 |
| ~~**T-127**~~ | ~~**`CUE_BALL` is a rhinoceros riding the boar's bike**~~ — ***Done 2026-09-16*** (`tools/genrhino.py`; engine `ec580dba9`) (*8 objects, Routes 16–18; the last disagreement `spritejoin.py` reports*). ***It is the one class in the batch whose stand-in is not 16×32***: **`biker.png` is 32×32, ten frames, and a MACHINE as well as a rider** — *the boar took SIX drafts and engine.md records why (handlebars standing clear of the body read as outstretched arms; the head-on wheel as a licence plate; every draft wider than the sprite it replaced until one was MEASURED against vanilla's own front frame).* **Giving the class a 16×32 sheet would take the bike off a trainer vanilla deliberately put in the gang, which is a design decision and not a redraw** — *so: a rhino on the bike at the biker's geometry, or a rhino on foot and the gang loses a rider, or leave it.* ***Undecided*** ***VANILLA SETTLED THE QUESTION THE TICKET LEFT OPEN***: **Routes 16–18 ARE Cycling Road, every trainer on them is a rider, and the class is the gang's muscle** — *giving the rhino a 16×32 sheet would have fixed the species by breaking the scene*, **so the rhino rides.** ***And the MACHINE is not redrawn***: *it is already ours and it cost six drafts*, **so the tool reads our own `biker.png` and replaces THE HEAD ONLY — jacket, frame, wheels and tail lights come through untouched and it asserts row by row that they did.** ***Which frames share a head is DERIVED***: *the ten hold three between them — front, back and side, each once standing and twice more shifted a row for the walk — measured by comparing every frame's head band against the three standing ones at each offset, so a sheet redrawn later cannot quietly fall out of step with a table.* **The head was built too wide twice before it was MEASURED against the boar it replaces** (*14 columns with the ears at 12–19, not the 20 the first drafts used, which overhung the bike*). ***A 32×32 sheet is cut 4×4 and CHECKED 4×4***: *`-mwidth 4 -mheight 4`, `overworld_frame(pic, 4, 4, k)`, `.size 512` with the 32×32 oam and subsprite tables, and a decode that reassembles sixteen tiles rather than eight* — **decoded 2×4 instead, frame 0 'fails', which is how five of the player's own sprites were once reported scrambled when nothing was wrong.** *`gentowns.register_one()` takes the geometry as a parameter now instead of hard-coding 16×32.* ***DISAGREEMENTS IN THE GAME: ZERO*** — **60 pairings agree over 414 objects and the nine pair-class pairings all report accounted for** | `tools/genrhino.py`, `graphics/object_events/pics/people/cue_ball.png` | **9.4**, T-120, T-126 |
| ~~**T-126**~~ | ~~**A trainer's battle portrait and their overworld sprite are different animals**~~ — ***Done 2026-09-16*** (`tools/genclasses.py`, `tools/gencouple.py`, `tools/genpsychic.py`, `tools/spritejoin.py`; engine `a3ddfaa77`, `50dcd7b66`, `e2a9a57b6`, `0b52e5ea3`) (*noticed on playtest 2026-09-16: "some trainer sprites (animals) didn't match their overworld sprites (animals)"*). ***The cause is vanilla's own sprite reuse and NOTHING here regressed***: **vanilla gives a Burglar the Poké Maniac's overworld sprite, a Juggler the Rocker's, a Breeder the Woman's** — *which was invisible while every one of them was a generic human.* **T-120's fable rule — "a class's portrait and its overworld sheet are one species" — made the reuse LEGIBLE, so a contradiction vanilla hid is now on screen.** ***Found by joining the engine rather than the tools' comments***: *742 trainers in `trainers.h` → 258 script labels that start a `trainerbattle` → the map object that runs each one*, **which gives 38 distinct (overworld sprite, battle portrait) pairings actually standing on maps.** *Five disagree, twelve trainers in all*: **POKE_MANIAC opossum stands for BURGLAR weasel ×3; ROCKER skunk for JUGGLER octopus ×3; WOMAN_2 ewe for POKEMON_BREEDER goose ×2; HIKER badger for RUIN_MANIAC aardvark ×2; and COOLTRAINER_M wolf ×1 and COOLTRAINER_F falcon ×1 both stand for COOL_COUPLE's two ibexes.** *A first pass reported the 47 CORPUS STAFF trainers as mismatched too and that was a FALSE ALARM — their portraits are drawn under the job names `corpus_m`/`corpus_f` rather than `p_rocket_grunt_*`, and both sides are the same penguins; a parser that keys on a naming convention finds a mismatch wherever the convention was not followed.* ***Decided 2026-09-16***: **the four single classes get overworld sheets of their own** (*weasel, octopus, goose, aardvark — all above T-120's ≥3-trainer cut-off at 7, 6 and 8 trainers, so they have earned drawings*), **and COOL_COUPLE, which is a PAIR class standing as two separate single sprites, has its portrait redrawn as the two animals it actually stands as — a wolf and a falcon** — **shipped: `burglar` (weasel), `juggler` (octopus), `pokemon_breeder` (goose) and `ruin_maniac` (aardvark), four NEW nine-frame sheets registered as their own `OBJ_EVENT_GFX_*`, and the ten objects that BATTLE as one of them repointed across seven maps** (*Poké Mansion 2F/3F/B1F, Silph 5F, Victory Road 2F, Pattern Bush, Lost Cave*). ***Each sheet inherits the SLOT of the sprite it replaces*** — *pink/2 from the maniac, blue/1 from the rocker, white/4 from the woman and the hiker* — **because the engine patches the palette named by a sprite's tag INTO the slot named by its info, so two sprites sharing a slot with different tags repaint each other**; *the palette was therefore already loaded on every map they stand on, each figure is drawn from the sixteen that slot already has, and no colour is added anywhere* (**engine.md trap 17**). **The couple's portrait is redrawn instead of given a sheet** — *a pair sheet would serve one battle in the game and leave the wolf and the falcon still wrong* — **and nothing below the collar changed: rows 0–23 hold head and nothing else, so the two heads are cleared and redrawn on the couple's own palette.** ***The falcon took four drafts and the fix was to TURN THE HEAD***: *a beak pointed at the viewer is a nose, the cheeks either side of it are a face, and every front-facing draft came out a person with a bowl cut* — **in profile the hooked wedge is a bird and nothing else.** *Verified by DECODE in all four ROMs, not by byte-match: a scrambled sheet matches its own scrambled self.* **Left open for T-120: the cooltrainer portraits themselves are still vanilla's purple-haired humans, and the 38-pairing join is the check that will close it** ***CLOSED TOO EARLY, AND THE REASON IS THE TICKET'S OWN LESSON WEARING DIFFERENT CLOTHES***: **the join globbed `data/maps/*/scripts.inc`, which holds 258 of the game's 726 battle labels** — *`data/scripts/trainers.inc` is ONE shared file holding 468 more, which is every route and island trainer there is.* **It did not fail. It reported 38 pairings where there are 72, five disagreements where there are sixteen, and the repointing that followed was a third of the job** (*engine.md **trap 18***). ***Widened and finished 2026-09-16***: **eleven more classes drawn and registered — `BIRD_KEEPER` an ostrich (18 objects, the largest disagreement in the game), `GAMER` a jackal (6), `SUPER_NERD` a lemur, `PAINTER` a toucan, `AROMA_LADY` a honeybee (4 each), `PSYCHIC_F` a jellyfish, `POKEMON_RANGER_M` and `_F` one pine marten in two uniforms (3 each), `LADY` a swan, `ENGINEER` a beaver, `TAMER` a hyena (2 each)** — *every one of their pictures already existed; what was missing was the sprite.* **60 more objects repointed, 16 sheets in all.** ***The honeybee came out a BULL first*** — *`mammal()` puts an ear at each top corner and two straight stalks over a tan face are horns, which engine.md had already recorded twice* — **so it is drawn without a mammal head: compound eyes taking the sides of the whole face, antennae two rows tall ending in a KNOB, which is what a horn never does.** ***And the portrait census was wrong in the other direction too***: *of the 79 still on vanilla art, **exactly ONE is reachable** — `PSYCHIC_M`, TRAINER_PSYCHIC_DARIO in the SEVEN ISLAND TRAINER TOWER, with a VS Seeker rematch; the other 78 are named in `trainers.h` and nowhere else.* **He is repainted from our own `psychic_f` rather than drafted** (*no tool here can post to `daemon/sprite`; this is `genbacks.py`'s method — keep the six colours that are the jellyfish, re-dye the nine that are cloth*), **and the first dye came out a bright rose because it lifted red; value is what the muted register is made of, so the second takes the ramp DOWN into an indigo.** ***`tools/spritejoin.py` is the check that should have existed first***: it prints **what it examined before what it found**, and its test is *names OR species* — *matching names alone reported 47 CORPUS STAFF trainers as mismatched, matching species alone reported every town's staff against itself.* **Disagreements: 16 pairings over 68 objects → ONE over 8. Left as T-127 and T-128** | `tools/genclasses.py`, `tools/gencouple.py`, `tools/spritejoin.py`, `graphics/object_events/pics/people/`, `data/maps/*/map.json`, `tools/` | **9.4**, T-117, T-119, T-120 |
| ~~**T-123**~~ | ***LOGIC and INTUITION done 2026-09-16*** (`tools/genbacks.py`, second draft): **the method the first draft's failure named — TRACE VANILLA'S OWN FRAMES AS THE SKELETON AND REPAINT THEM — and all four of that draft's defects are gone**: *there is a head, there are shoulders and arms, the tail comes out from behind the coat, and the throw is visible, because the animation is vanilla's exactly rather than invented.* ***The palette does most of the work***: **vanilla's indices already carry roles** (*2/3/4 skin, 8 hair, 9/10 shirt, 5/6 jeans, 11/12 cap, 13/14 pack, 15 outline, 1 and 7 spare*), **so swapping the sixteen colours role for role changes the character without touching a single index.** *But a role-for-role swap gets the PROPORTIONS wrong — vanilla is mostly backpack (830px) over a small shirt (302px) and ours is a long cream coat with a dark satchel — so the **pack's area becomes the coat**, which is the big surface on a back, and vanilla's ball emblem is left dark, where it reads as the bag without being edited at all.* **Three shape edits, each derived rather than placed: the brim grown from the hat's own pixels so it follows the head tilt, the emblem left as the satchel, and the tail grown from the coat's own bottom edge.** ***A REAL BUG, AND 9.10 SETTLED IT***: **the two files carry the SAME sixteen colours but use them for different things** — *in `red` 11/12 are the cap (1425px); in `leaf` they are only a sunhat's band (210px) while 9/10 are a white hat and 4/8 are long hair (1978px)* — **so recolouring leaf by red's roles put a white hat on INTUITION.** *The fix is not a second index map but 9.10's own ruling: these are ONE figure, "two people, and the player picks the one they would rather be", whose only difference is the tail — so **both strips trace red's skeleton**, and leaf's long hair, which this character does not have, never arises.* ***Said plainly: it is not a monkey.*** *From behind it carries the character's MATERIALS — brown hat, cream coat, dark satchel, grey fur at head and hands, and the tail — on a human silhouette, because repainting cannot add a muzzle. It is strictly better than the vanilla boy in a red cap that shipped before it and it is not as good as the overworld sheets.* **Sizes asserted against the files on disk (5 frames, `0x2800`); strips and palettes byte-checked in all four ROMs.** ***STILL OPEN***: **the old man and the pokedude, who are not the player and have no species yet — a decision before drawing.** *`RS_BRENDAN` and `RS_MAY` stay vanilla under T-120's cut-off* — ***Closed 2026-09-17*** (engine `f222af7de`, `1fd7c761e`; `tools/genmentors.py`): **the last two back pics needed no species decision after all — the game had made both.** *The old man is an OLD IGUANA, because you meet him on CALLOW's elder sheet; the PokéDude is CRYSTAL CLEAR, a fox, because STREAM's host line and every demo's narration already name her.* **The iguana is repainted with derived edits** (*the profile smoothed so the face is a lizard's and not a green man's, a round eye, a gold dewlap hung from the chin*); **CRYSTAL's head is REPLACED, not recoloured** — *a gold bob is still a haircut, and the PokéDude's skin runs unbroken from face to shoulder* — **one fox head drawn once and placed at an offset the tool finds and asserts, since all 283 of the head's pixels move together.** ***The old man's line went with it***: *"That is my grandson presenting" contradicted the show the player had just watched*, **and now reads "I watched those when they were new. They have not changed. I have."** (*approved wording*) | `graphics/trainers/back_pics/`, `graphics/trainers/palettes/`, `tools/genbacks.py` | **9.4**, 9.10, T-120 |
| ~~**T-122**~~ | ~~**The FIGHTING DOJO is the ninth gym and nothing has touched it**~~ — ***Done 2026-09-16*** (`tools/gbainterior.py proof_hall`), **from a concept**: *a hall that proves, and certifies nobody.* ***BRAZEN is the bought city*** — *brass over base metal, "honestly itself… worth a fraction as much" (2009)* — **so the PROOF HALL is the BENCHMARK's trade in the cheaper metal**: *a boarded floor, a canvas mat where the gallery of mounts has gilt (T-111), two plain steel pillars with a single brass shoe each, chalk tallies in fives that nobody countersigns (SLATE's wall, T-105), **an empty bracket where every BENCHMARK mounts its gauge** — never removed, never installed — **and a plaque cast and never engraved.*** **The emblem is vanilla's own**: *the Master already says "do not take our emblem as your trophy", and it cannot be taken because **QED is not a MARK** — it is not among the eight on the trainer card.* ***The 9.23 clash is fixed in one line***: *"I am the LEADER here!" becomes "Nobody appointed me." — there is one leader to a town and it is BRAZEN's, and his own title is left alone because four other texts still call him the KARATE MASTER.* ***`gTileset_PewterGym` now has ZERO consumers***, which is what this ticket wanted; **53 blocks, 67 tiles, 0 collision changes.** ***AND THE NAME WAS NEARLY OVERWRITTEN***: **T-25 renamed this building to the PROOF HALL in 2026**, *it ships in four maps' text and in `gText_QuestLog_FightingDojo`, and CHANGELOG records a QED board outside where the other towns carry a MARK board* — **but the concept was drafted by checking the species, move, item and ability tables and the bible, found the word free, and proposed a different name entirely.** ***A building's name lives in the game's own map text, and that was never searched***; the tileset was written once as `gTileset_DryRun`, reverted, and rebuilt as `gTileset_ProofHall`. ***And the KARATE MASTER is THE REFEREE*** (*asked 2026-09-16: "it shouldn't be karate as that isn't daemon related"* — **which is T-25's own objection, left unfixed on the man while it was fixed on his building**). *A **referee** checks a proof before it is accepted **and** calls the points in a match, so it carries two readings at once as `BENCHMARK` and `CHECKPOINT` do — and vanilla's own "Judge!" and "One point!" stop reading as karate the moment he is renamed.* **It sharpens the concept rather than restating it: a referee says whether a proof HOLDS, and holds no power to certify anyone.** *It also sits with what already shipped — his students are `FORMALIST`s, `KARATE CHOP` is `REDUCE` ("a problem cut down to a smaller one"), his room ends in QED, and the two daemons he gives are AXIOMKICK and REBUTTAL.* ***The Gym next door already said the rest of it***: **"There used to be 2 BENCHMARKS in BRAZEN. The PROOF HALL next door lost its BENCHMARK status"** — *so "a hall that certifies nobody" was canon before the concept was drafted.* **Twelve strings across three maps** (*the hall, the Fan Club, the BENCHMARK*), *every anchor asserted to occur exactly once before anything was written, every line at or under vanilla's own 35-character maximum, and `DOJO` retired with him.* **Checked in both directions through `gbastr.py`: the new lines are in all four ROMs and `KARATE MASTER` is in none.** *Left alone deliberately: `LOCALID_KARATE_MASTER` and the `…FromKarateMaster` label, which are symbols no player reads, and `MOVE_KARATE_CHOP`, whose string is already `REDUCE`* | `data/tilesets/secondary/proof_hall/`, `data/maps/SaffronCity_Dojo/` | **9.23**, 5.3, T-25, T-111 |
| ~~**T-121**~~ | ***Done 2026-09-15 — the rows are given back*** (engine `86c9ce343`): **`gbaroofs.py` reads `upstream/master` now and refuses to block any cell vanilla left walkable, restoring the ones it had already blocked — 38 cells across six towns.** ***Flood-filling from the same seed, reachability matches upstream EXACTLY in all six***, *where before four towns were each short by the length of their blocked run.* **Original ticket: The roof pass closed a walkway in four towns** (*found on playtest 2026-09-15*). ***`gbaroofs.py` blocks the row FOUR above a BENCHMARK door*** (`ROOF_DY = -4`) **across the building's width, so the player cannot stand on our colonnade's roof** — *and its docstring claims "the row behind it stays open in every town, so no path closes."* ***That claim is false.*** **Flood-filling the walkable tiles against `upstream/master` from the same seed**: *CELADON loses 8 at (8,26)–(15,26), PEWTER 7 at (12,12)–(18,12), DOLDRUM 6 at (28,17)–(33,17), CALLOW 6 at (33,6)–(38,6); ARDOR and QUICKSILVER lose none, their runs being unreachable anyway.* **In every case the lost set is EXACTLY the blocked run** — *nothing beyond it becomes unreachable, so this is a walkway taken away rather than an area cut off.* ***And the row is ground, not roof***: **vanilla left it walkable with open ground above it and the building's own solid body below** — *it is the strip you walk along under an overhang.* **The pass is not wrong to exist** — *our colonnade draws a roof at that row, which is why the player appeared to walk out onto it* — **so the choice is: unblock those rows and accept the roof, or change the colonnade art so it stops drawing roof over ground the player may stand on.** ***A design call under 9.23, not a bug to squash quietly.*** *`engine.md` trap 15's lesson applies here too: the tool's own claim about its blast radius was never tested, and a flood-fill against upstream is what tests it* | `tools/gbaroofs.py`, `data/layouts/<town>/map.bin` | **9.23**, T-85 |
| ~~**T-62**~~ | ***Done 2026-09-15: the CHECKPOINT floor carries the restore arrow*** — **an inlay in the floor's own tan and cream, three times size, where vanilla's ball was** (`tools/gbacheckpoint.py`); *a solid teal arrow was tried first and read as a decal.* **Original ticket:** **The three emblems** (*9.23*). ***Drawn (`tools/gbaemblems.py`) and on every exterior door and sign — only the CHECKPOINT floor is left, with T-65.*** ***A circular restore arrow (CHECKPOINT), a stacked package (THE REPO), a gauge needle (BENCHMARK)***, **drawn once as pixel art and placed everywhere the Poké Ball stood**: *over each door, on each sign (with no letters), and on the CHECKPOINT floor* | `data/tilesets/primary/general/`, the town tilesets, `data/tilesets/secondary/pokemon_center/` | **9.23** |
| ~~**T-63**~~ | ~~**CHECKPOINT and REPO exteriors**~~ — ***Verified closed 2026-09-17*** (closed in `61c350aa`) | this commit — ***17 CHECKPOINTS with a teal hipped roof and the restore arrow, 12 REPOs with a golden amber roof and the package***, *each found by its door warp and stamped from one drawing, so a town's own edge, sign or roof-top block is redrawn with the shared ones* (`tools/gbacivic.py`). **Signs show the emblem and no letters.** ***Teal is three row-2 colours only Verdigris's wooden doors used, which move to row 5; amber is row 5's golds.*** *95 tiles in slots only these buildings or nobody used* |
| ~~**T-64**~~ | ~~**BENCHMARK exteriors and the board**~~ — ***Verified closed 2026-09-17*** (closed in `61c350aa`) | this commit — ***8 BENCHMARKS with a slate roof, pillars, a blank plaque and the gauge over the door***, **drawn column by column by role, since they are six, seven and more cells wide**; *the free-standing board shows the gauge.* ***The entrance statues inside, as a scoreboard plinth, are still to do*** — *they are part of the gym interiors* | ***Checked 2026-09-17: the statues are DONE*** — **the scoreboard plinths stand in the BENCHMARK interiors `gbainterior.py` builds** (*`ST.plinth()` under each leader's mark, from `be59e181`, T-105 onward*); *the "still to do" was handed to the gym-interior tickets and they did it.*
| ~~**T-66**~~ | ~~**Signs right of the door, and THE REPO's branch**~~ — ***Verified closed 2026-09-17*** (closed in `a3723792`) | this commit — ***the emblem stays over each door and the duplicate sign is gone***; **right of the door a checkmark (CHECKPOINT), REPO, and MARK (BENCHMARK), in a 3x5 face**; *THE REPO's emblem is a branch glyph.* **85 tiles** |
| ~~**T-67**~~ | ~~**Mom gives the RUNNING SHOES**~~ — ***Verified closed 2026-09-17*** (closed in `ce4dbeaa`) | this commit — ***MOM hands them over the first time you speak to her: "Wait. Take these. You never could stand still."*** **Holding B runs from then on.** *If you already have them when you win in Slate, the aide's scene outside is skipped* |
| ~~**T-68**~~ | ~~**Mom's overworld sprite**~~ — ***Verified closed 2026-09-17*** (closed in `7139f0b9`) | this commit — ***a grey monkey with the player's pale grey face, red hair tied back over one shoulder, an olive cardigan and a long brown skirt***, *drawn in the white slot's colours* (`tools/genfolk.py`) |
| ~~**T-69**~~ | ~~**VERA CLEAR, a fox**~~ — ***Verified closed 2026-09-17*** (closed in `7139f0b9`) | this commit — ***a young russet fox with a white-tipped tail in a lavender pinafore***, *in the Clears' palette in the special slot, the only special tag in her house* |
| ~~**T-70**~~ | ~~**Blanche's other people**~~ — ***Verified closed 2026-09-17*** (closed in `7139f0b9`) | this commit — ***the sign woman a magpie, the man by the pond an olive terrapin (not CAIRN's grey tortoise), the lab aides moles in white coats, the dry aide a crow***, *each from its slot's existing colours; walks swing arms and tails; frame 9 raises a hand.* **Shared sheets, accepted** |
| ~~**T-71**~~ | ~~**A map-editing test on Route 1**~~ — ***Verified closed 2026-09-17*** (closed in `b61ad129`) | this commit — ***a small pond, a path bulge and a shortened spur, the tall grass up a row, three flowers moved***, *40 cells* (`tools/gbamapedit.py`). **What it taught:** ***paths are autotiled from a mask, not painted*** *(13 blocks; the inner corners read off Route 1)*; ***a closed pond is borrowed whole*** *(only the Safari Zone has one on a real map, and its bottom edge lives in Fuchsia's tileset)*; ***collision and elevation travel with a template cell of the same kind*** |
| **T-120** | **The rest of the shared people sheets, and every trainer portrait** (*tier 3 of T-117*) | **Every shared people sheet left after T-118 and T-119 — the route trainers above all: hiker, fisher, sailor, bug catcher, swimmer, biker and the rest — redrawn once as a fable animal for its CLASS (the species is the role), and all 146 vanilla trainer portraits and 6 back pics likewise, drafted through `daemon/sprite` and cut by `tools/gbachar.py`.** *A class's portrait and its overworld sheet are one species.* ***First batch done 2026-09-15*** (`tools/gentrainers.py`): **the twelve most-used shared sheets — 273 of the 558 objects still on vanilla art — redrawn IN PLACE.** *Nothing is registered and no map changes: a class is one sheet shared by dozens of objects, so overwriting the PNG moves every one of them.* **hiker a badger, fisher a pelican, channeler a bat, woman_2 a ewe, balding_man a vulture, picnicker a hedgehog, bug_catcher a swallow, youngster a mouse, lass a lamb, rocker a skunk, sailor an albatross, old_man_1 an old goat.** ***Two things are fixed and must not change***: *the sheet's PALETTE (one of the four NPC slots, the same on every map — the figure is drawn from what its slot already has) and its FRAME COUNT (the pic table indexes a fixed number; ten here), asserted against the file on disk before anything is written.* **Three failed the fable rule at sixteen pixels and were redrawn**: *a moth's wings read as arms and its antennae as horns (it became a BAT, whose whole silhouette is its ears); a peach dome with a gold bill read as a man with a moustache, the walrus trap again (the vulture's head went dark and small, the bare skin down to a crown patch); and a gold ear on a brown head is a horn, not a spine (the hedgehog lost its ears and gained a crown).* ***The people line moved 22.7% → 27.3%, and the overworld people alone to 50%.*** ***Second batch done 2026-09-15 — the odd formats*** (`tools/genodd.py`): **the four sheets whose frames are not 16×32 — 27 objects.** *`little_girl` a piglet, `little_boy` a bear cub (nine frames, so no raised hand), `tuber_m_water` a duckling and `tuber_f` a cygnet, each in its ring.* **genfolk.py's helpers all assume a 16×32 frame** — *`standing()` pads to 32 rows, `walk()` swings the arms at rows 16–28 and lifts a foot at row 31, `check()` demands 32×16, `sheet()` builds a strip 32 tall* — **so these get their own geometry.** ***A SMALL SPRITE IS A HEAD***: *sixteen rows leave about six for the head and eight for the body, which is why vanilla's children read as heads on feet — and it suits 9.4, where the head's size carries age. The two tubers have no legs at all; the ring and the water take the bottom third.* ***The trap, and it cost three drafts***: **a mirrored half row must reach the CENTRE COLUMN** — *`h16()` pads a half to eight and mirrors it, so a seven-character row leaves column 7 blank and its reflection leaves column 8 blank too, opening a two-pixel transparent seam straight down the middle. Every head read as two lobes and the biker as two riders side by side.* **And `ljust(n)[:n]` pads a short row but SILENTLY CLIPS a long one, which `check()` cannot see because the mirror always yields the right final width** — *so `_fit()` now asserts instead, the same clipping that had been cutting the backs off the town sheets' side heads.* ***The BIKER is deferred, not done***: *10 frames of 32×32 (not twenty), 35 objects — the most-used sheet left. Three drafts in, the boar reads (tan head, dark snout, a tusk either side) but the machine does not: the handlebars stand clear of the body and read as outstretched arms, the head-on wheel reads as a white box, and the silhouette is bulkier than vanilla's; only the side view works. **The next draft takes its proportions from vanilla's own front frame** — a machine no wider than the rider's shoulders, bars angled DOWN into the fork, the front wheel implied by a dark gap rather than a pale rectangle.* ***The people line moved 27.3% → 28.8%, the overworld people to 54%, and every sprite reached 10.0%.*** ***Third batch done 2026-09-15 — the low-use classes*** (`tools/gentrainers.py`): **19 sheets, about 188 objects.** *`gentleman` a lynx, `swimmer_m_water` and `swimmer_m_land` one newt, `man` an ox, `black_belt` a kangaroo, `worker_m` a mule, `crush_girl` a wolverine, `chef` a panda in the hat, `woman_3` a hen, `old_man_2` an old ram with a cane, `swimmer_f_water` and `swimmer_f_land` one axolotl, `camper` a raccoon, `boy` a wallaby, `beauty` a gazelle, `cooltrainer_m` a wolf, `cooltrainer_f` a falcon, `poke_maniac` an opossum, `old_woman` a shrew.* **A class's land and water sheets are ONE animal**, *which is what the swimmers are for.* **A nine-frame sheet takes no raised hand; the count is whatever the file already holds.** ***Two engine facts worth keeping***: *five sheets (`trainer_tower_dude`, `lance`, `bruno`, `agatha`, `gba_kid`) hold only THREE frames while their tables index nine — the table names ids 0,1,2 and repeats them, and every one of those NPCs is `MOVEMENT_TYPE_FACE_DOWN`, so three frames is complete rather than truncated;* **and `sPicTable_OldMan2` borrows `gObjectEventPic_OldWoman` frame 0 as its TENTH entry** — *so the old woman's front frame can render through the old man's WHITE palette rather than her own PINK, latent only because that object never raises a hand.* ***Held back deliberately***: *the named and story characters — `trainer_tower_dude`, `bill` (**HOLT**, 8.2b), `lorelei`, `mr_fuji`, `gym_guy`, `lance`, `bruno`, `agatha`, `celio`, `captain` — because the design already fixes who they are (the Review Board is PHLEGMATIC, CHOLERIC, MELANCHOLIC and SANGUINE), and a named person wants a species for **who they are**, not for a class role; plus `gba_kid`, which needs the three-frame still machinery.* ***The bible was checked before drawing, and it holds***: **8.5's *"nobody else changes"* is a GAME BOY ruling** — *amended in place 2026-09-04 because the port moved the resolution it rested on* — **and 9.4's fable rule (2026-09-06) is the later and more specific one**, *settled by generating four treatments and rejecting the animal-head-on-human-body because it "failed on a bug catcher in shorts", which is a generic class sheet tested directly.* ***8.5's line should be struck as superseded, with the version bump and changelog the process wants — offered, not done.*** ***The people line moved 28.8% → 36.5%, the overworld people to 75% (72 of 96), and every sprite to 11.4%.*** ***Every sheet this project ADDED was scrambled in the ROM, and a playtest screenshot caught it*** (*fixed 2026-09-15; `engine.md` trap 15*). **`spritesheet_rules.mk` names each object-event sheet explicitly and converts it with `-mwidth 2 -mheight 4`, which is what packs a frame's 2×4 tiles together for `overworld_frame()`** — *a strip wider than one frame with no rule is cut in raster order across the whole image, so every frame is assembled from the wrong tiles: the head sheared off, and on the side frame the head down at the feet.* ***Overwriting a vanilla sheet inherits vanilla's rule***, **which is why all three batches of redrawn classes were correct and all 37 sheets we ADDED were wrong from the day they shipped** — *the BENCHMARK guide and nine staff (T-116), the 26 town locals and AL's guest (T-119).* ***Three things hid it***: *`make` compares timestamps and not recipes, so writing the rules rebuilt nothing until the stale `.4bpp` were deleted; four builds exited 0 while the ROM kept the object file it had already linked; and byte-checking a sheet against the ROM cannot see it, because a scrambled sheet verifies perfectly against its own scrambled self.* **The check that works is to DECODE — read the first eight tiles out of the `.4bpp`, assemble them as a 16×32 frame, and compare with frame 0 of the PNG — and it now reads 40 correct, 0 scrambled.** *`genfolk.ensure_rule()` writes the rule whenever a tool saves a new sheet, so the first failure cannot recur.* ***The BIKER took SIX drafts*** (*written 2026-09-15, engine `d84615dcb`* — 35 objects, 10 frames of 32×32, the most-used sheet in the game). **Drafts 1–3 lost the machine**: *handlebars standing clear of the body read as outstretched arms, the head-on wheel as a licence plate, and the whole silhouette was bulkier than the sprite it replaced.* ***Draft 4 fixed that by MEASURING vanilla instead of judging my own drawing*** — **its front frame occupies rows 10–31 only, is at most fourteen columns wide and tapers to two pixels** — *and every earlier draft had been wider than the thing it replaced.* **Draft 5 moved the snout to the centre four columns and put the tusks beside it**, *because a peach dome with a dark band across it reads as a person wearing something — the vulture's failure again.* **Draft 6 gave the crown two rows of ear**: *one pixel of ear reads as nothing, and vanilla's rider carries its whole silhouette in a crest at exactly that height.* ***The rule worth keeping: measure the sprite you are replacing before you redraw it.*** *And a 32×32 sheet must be decoded 4×4 to be checked — decoding it 2×4 reported five of the player's own sprites as scrambled when nothing was wrong.* ***The WATER swimmers were wrong too, and a playtest screenshot caught them standing on the sea***: **a class's land and water sheets are one ANIMAL but not one POSE** — *vanilla's `*_water` sheets are cut at the waterline, head and shoulders with the arms at the surface and nothing below row 28, and I had drawn the land figure into both* (fixed, engine `e9fb1c520`). ***Fourth batch done 2026-09-15 — the named and story characters*** (`tools/gennamed.py`): **eleven sheets.** ***A class gets a species for its ROLE; a named person gets one for WHO THEY ARE***, so these were **read out of the bible and the game's own dialogue rather than invented**: *`lorelei` **PHLEGMATIC** a polar bear (white, FROZEN, "there is no hurry — there never is"), `bruno` **CHOLERIC** a tiger, `agatha` **MELANCHOLIC** a wombat, `lance` **SANGUINE** a cardinal — the seats confirmed from 6's "you fight IV, II, III, I" and from each room's own lines; `bill` **HOLT** a river otter; `mr_fuji` **INIT** a tapir; `celio` a mongoose; the `captain` a cormorant; `gym_guy` a hamster; `trainer_tower_dude` a marmot; `gba_kid` a chipmunk.* ***4.20a names HOLT's species itself*** — **"a holt is specifically an otter's den… an otter is at home in two elements, which is the man who held two frames in one address"** — *so it outranks DOLDRUM's sea otter, chosen later; he is a RIVER otter in the blue slot, on maps that local never stands on.* ***One colour the design mandates could not be given***: **section 6 reserves *sanguine red, choleric yellow, melancholic black, phlegmatic white* to the Board alone, and `bruno` is locked to `npc_white`, which has no yellow** — *its nearest is a sand-gold.* **6804's rule that value carries the humours (*melancholic dark, phlegmatic pale, sanguine and choleric mid*) is satisfied; the hue is not, and that is a compromise rather than a match.** ***The trap this batch taught***: **the raised hand IS the tenth frame** — *`ANIM_RAISE_HAND` indexes it, and every nine-frame sheet here holds nine, so asking for one drew a frame the file had no room for; the assertion against the file on disk caught it before anything was written.* *Five of the eleven are THREE-frame stills, the same ids-0,1,2-repeated case as the Elite Four rooms.* ***The people line moved 36.5% → 40.8%, the overworld people to 86% (83 of 96), and every sprite to 12.1%.*** ***Fifth batch begun 2026-09-15 — the portraits*** (`tools/gbachar.py`, drafted through `daemon/sprite`): **24 cut and in the ROMs.** *The hiker a badger, picnicker a hedgehog, youngster a mouse, bug_catcher a swallow, biker a boar, swimmer_m a newt, lass a lamb, channeler a bat, camper a raccoon, swimmer_f an axolotl, fisherman a pelican, scientist a mole, pokemaniac an opossum, black_belt a kangaroo, crush_girl a wolverine, sailor an albatross, gentleman a lynx, beauty a gazelle, tuber_f a cygnet, rocker a skunk — and the REVIEW BOARD: `elite_four_lorelei` PHLEGMATIC a polar bear, `elite_four_bruno` CHOLERIC a tiger, `elite_four_agatha` MELANCHOLIC a wombat, `elite_four_lance` SANGUINE a cardinal.* ***Every one is the species its overworld sheet already is***, which is this ticket's own rule and meant no new decisions. ***The first drafts came back as TREATMENT C*** — **animal heads on human bodies, with human hands and bare arms** — *which 9.4 rejected because it "failed on a bug catcher in shorts", and the youngster failed exactly that way.* **The prompt now states the three clauses outright** (*paw or claw hands with no human fingers, a tail, a head proportioned to the body; flat cel shading, no gradients*) **and every draft since has passed.** ***And a 4bpp portrait's PNG does not carry its colours***: `trainers.h` takes the picture from `<name>_front_pic.4bpp.lz` and the palette from a SEPARATE `palettes/<name>.gbapal.lz`, *so every job writes its `.pal` too — Scorn once shipped without his and rendered in Giovanni's, where index 5 is magenta.* **Both artefacts are byte-checked in all four ROMs for all 23.** ***The trainer portraits line moved 9% → 25%, and THE PEOPLE REACHED 50.0%*** — *half of every human sprite vanilla shipped is now ours* — **with every sprite at 13.7%.** ***The cut-off, agreed 2026-09-15: a class earns a drawing if THREE OR MORE trainers use it***; below that it keeps vanilla's, because the tail is Hoenn material this game barely shows. **Thirteen more cut on that rule**: *`bird_keeper` an ostrich (33 trainers), `cue_ball` a rhinoceros, `super_nerd` a ring-tailed lemur, `gamer` a jackal, `ruin_maniac` an aardvark, `burglar` a weasel, `juggler` an octopus — and the six PAIR classes, which are TWO figures of one species in a single 64×64: `twins` two quails, `young_couple` two swans (the tuber being a cygnet), `sis_and_bro` two rabbits, `crush_kin` two wolverines (as `crush_girl` already is), `cool_couple` two ibexes, `sr_and_jr` two porcupines.* ***The gamer came back a FOX*** — **russet, brush tail, white tip** — *which 9.4 reserves for the Clears; it took forbidding the fox by name in the prompt to get a black-backed jackal.* **Both figures stay legible in every pair at fifteen colours.** ***37 portraits are now cut: the line moved 9% → 34% (50 of 146), and THE PEOPLE REACHED 55.0%.*** ***And the rule was applied inconsistently in BOTH directions, corrected here***: **ten classes still sit AT OR ABOVE the cut-off and are still vanilla** — *`psychic_f` 5, `pokemon_ranger_m` 5, `pokemon_ranger_f` 5, `painter` 5, `tamer` 4, `pokemon_breeder` 4, `aroma_lady` 4, `rs_youngster` 3, `lady` 3, `engineer` 3 — 41 trainers between them* — **while `sr_and_jr` was drawn at ONE trainer, below the line.** *Counting the qualifying set before drawing, rather than working down a usage list by eye, is what would have caught both.* ***The ten were then drawn (2026-09-15), and EVERY class at or above the cut-off is now done***: *`pokemon_ranger_m` and `pokemon_ranger_f` one pine marten, `pokemon_breeder` a goose, `aroma_lady` a honeybee (8.2b's hive that talks by dancing), `rs_youngster` a vole, `lady` a swan, `engineer` a beaver, `tamer` a hyena ringmaster, `psychic_f` a jellyfish, `painter` a toucan.* ***Two first picks were species CLASHES and were re-rolled***: **the psychic came back a second octopus — the juggler already holds it — and the painter a chameleon, which is MATTE's.** *A class must not wear a BENCHMARK leader's animal, nor repeat one already spent on another class; the prompt now forbids the fox, the chameleon and the octopus by name.* **The portrait line moved 34% → 41% (60 of 146) and the people to 58.8%.** ***Still to come***: **125 portraits are used by a trainer at all; 47 are done; of the 79 left, ALL fall under the cut-off and keep vanilla's art by the rule** (*80 trainers between them, and 90 of the file names are `rs_*` prefixed with 60 more Hoenn-flavoured** (*aqua, magma, Steven, Brendan, May, Wally, triathletes*), **most used by one or two trainers or none.** *Proposed: anything under ~3 trainers stays vanilla, which leaves about a dozen worth drawing — `bird_keeper` (33 trainers), `cue_ball` (13), `super_nerd` (9), `gamer` (9), `ruin_maniac` (8), `burglar` (7), `juggler` (6) and the PAIR classes (`twins`, `young_couple`, `sis_and_bro`, `crush_kin`, `cool_couple`, `sr_and_jr`), which are two figures in one 64×64 and need a rule of their own.* **The 6 back pics are untouched.** **`docs/sprites.md`'s people line is the measure** ***The last two people sheets anyone sees, drawn 2026-09-17*** (engine `0c7b1c936`): **of the 13 still vanilla, 11 are never on screen** — *five marked unused, the two surf sheets nothing reads, two named only in a text-colour table, and the Ruby/Sapphire link partners* — **and the two that are:** *`old_man_lying_down`, VIRIDIAN's opening, is the elder iguana's side view turned a quarter clockwise — whole pixels, nothing resampled — and moved from `NPC_PINK`, which has no green, to CALLOW's palette and special slot where the standing iguana already is; `tuber_m_land` is the water sheet's duckling standing on orange feet.* ***Found on the way***: **`genodd.py` still said the biker was "not written"; rebuilt in memory all five of its sheets were byte-identical to what ships, so the comment was wrong, not the file.** ***Overworld people 86% → 89%; the eleven left are the unseen ones.*** | `graphics/object_events/pics/people/`, `graphics/trainers/` | 9.4, T-117 |
| ~~**T-119**~~ | ~~**Each town's people are vanilla's, shared with every other town**~~ — ***Verified closed 2026-09-17*** (engine `62f67690f`, `7e2773254`, `c29041bed`) (*tier 2 of T-117; asked 2026-09-15: "each town will need sprites which relate to its color and theme"*) | ***Built 2026-09-15 — all ten towns*** (`tools/gentowns.py`): **each town's generic citizens are its locals — a child, an adult and an elder — as new object graphics `OBJ_EVENT_GFX_TOWN_<TOWN>_<ROLE>`, all three sharing ONE palette of the town's own in `PALSLOT_NPC_SPECIAL`** (*tags 0x1120–0x1122*): *CALLOW a tree frog, a parakeet and an old iguana in greens, yellow and sky; SLATE a pangolin, a rock hyrax docent and an old armadillo in stone, slate and chalk; DOLDRUM a seal pup, a sea otter and an old walrus in pale sea, navy and ivory.* **The tool registers everything idempotently and repoints the citizens: the pilot's 52 (14 CALLOW, 22 SLATE, 16 DOLDRUM), then 144 more — 19 ARDOR, 15 HALFTONE, 52 VERDIGRIS, 23 LURID, 22 BRAZEN, 10 QUICKSILVER, 3 UMBRA — 196 citizens on 72 maps, on palette tags 0x1120–0x1129.** ***Left alone: DOLDRUM's city map, where AL shares the special slot (6 citizens).*** *Three read wrong at sixteen pixels and were redrawn: the armadillo was pink and read as a pig, the otter's all-sand face as a person's, the walrus as a man with a moustache.* **Species chosen for all ten towns 2026-09-15**: *ARDOR a red panda, a gull dockhand and an old rooster; HALFTONE a dalmatian, a zebra and an old panda; VERDIGRIS a gecko, a peafowl and an old heron; LURID a flamingo, a mandrill and an old cockatoo; BRAZEN a lion cub, a golden retriever and an old hamster; QUICKSILVER a ferret, a silver pheasant and an old grey cat; UMBRA a black panther (and a raven, an old badger if a map needs them).* **The seven after the pilot share two parametric heads** — *a MAMMAL (a small ear, a muzzle) and a BIRD (a crown, a beak at the centre) on one torso, the species carried by marks written into the half-rows before they are mirrored: spots, a mane, a mask, a blue ridge, and the tails and canes overlaid after.* ***A town builds only the roles its own maps put on screen***: *HALFTONE and BRAZEN have no elder citizen anywhere and UMBRA's plateau has three adults and nobody else, so 26 sheets exist where 30 would have been dead weight.* **Two things the drawing taught**: *the red panda read as a FOX, which 9.4 reserves for the Clears, and was redrawn the way a red panda is not one — a cream face under a rust crown, a rust tear stripe, black limbs and a ringed tail; and a cane at column 12 overwrites the trousers' own outline, so it reads as a stripe painted down the leg — canes stand at column 14, beside the hand.* **Verified by bytes: all 26 sheets and 10 palettes are in all four ROMs, and the 27 graphics-info symbols are in the `.elf`.** ***Done 2026-09-15 — the maps a special-slot character shares.*** **One map, one special palette**: *the slot is patched per object from that object's tag, so the LAST special sprite to spawn sets the sixteen colours every special sprite on the map draws from.* **Who those are is now read from the engine** (`PALSLOT_NPC_SPECIAL` in `object_event_graphics_info.h`) *rather than the four Clears named by hand, which had missed the SEAGALLOP.* ***DOLDRUM's city map takes AL as a GUEST***: *he is redrawn against the sea town's own sixteen — the Clears' rust, orange and purple-grey kept EXACTLY, in the three letters (`e`, `m`, `W`) no seal, otter or walrus draws with, and his browns, cream, lilac and pale blue collapsed onto colours 21–37 away that DOLDRUM already owns — and registered as `OBJ_EVENT_GFX_AL_DOLDRUM` on the town's tag, so only that map's object changes and AL is untouched on the other nine maps he walks.* **Its 6 citizens are locals now and DOLDRUM reads 100%.** ***QUICKSILVER's island is the case that does NOT fit***: *the SEAGALLOP needs ten colours, four of them (steel blue, indigo, magenta, a bright yellow) nowhere near a mercury palette with two letters free.* **That map keeps vanilla's two — the tool restores them from `upstream/master` by index — and QUICKSILVER reads 75% honestly.** *Two traps worth keeping: a flag-hidden object never spawns and never patches the slot, but `addobject` puts one on screen without ever clearing its flag (both rivals and the ferry work that way), so the flag alone proves nothing; and a guest's own variant must be counted as OURS, or the next run sees it holding the map and reverts the citizens it just placed.* ***Next: what a town still shows on vanilla art*** — *BRAZEN's `black_belt` ×5 and `crush_girl` ×2, QUICKSILVER's `bill`, `woman_2` and `old_man_1`, HALFTONE's `mr_fuji`, VERDIGRIS's and UMBRA's `gym_guy`* — **which are T-120's shared sheets and the named characters.** ***Chosen 2026-09-15: each town gets its own locals, in a palette of its own.*** **Ten towns × 3–4 local citizens (a child, an adult, an elder, perhaps one signature local), each a species that fits the town's colour and theme (vision.md 3.1), drawn as NEW object graphics as the BENCHMARK staff were, and that town's generic citizens (youngster, lass, gentleman, old man, little girl…) repointed to them.** ***A town palette in `PALSLOT_NPC_SPECIAL`*** **(the Clears' and the Owl's precedent) makes the town's colour exact** — *where the Clears or the Owl share a map with a town's locals, their colours fold into that town's palette, since one special tag per map.* **Species proposed per town before drawing.** *`docs/sprites.md`'s town table counts sheets; add a count of on-screen objects still on vanilla art, since new locals are ADDED and move no percentage on their own* ***Checked 2026-09-17***: **`gentowns.py` reports zero citizens left to repoint in all ten towns, and the census has every town at 100% — including QUICKSILVER, which this row recorded as 75% "honestly".** *It moved without this ticket being touched: the island keeps vanilla's two citizens because the SEAGALLOP holds the slot, but the sheets those two stand on were redrawn by T-120, so they are no longer vanilla art.* **The row's "Next" list — `black_belt`, `crush_girl`, `bill`, `woman_2`, `old_man_1`, `mr_fuji`, `gym_guy` — was all done by T-120's batches.** *One suggestion here was never acted on and is not a blocker: the town table still counts SHEETS on screen, not OBJECTS still on vanilla art.* | `data/maps/<Town>*/map.json`, new object graphics, `graphics/object_events/palettes/` | 3.1, 9.4, T-117 |
| ~~**T-118**~~ | ~~**The institutions are vanilla's people in every town**~~ (*tier 1 of T-117*) | ***Done 2026-09-15*** (`tools/geninst.py`, *portraits by `tools/gbachar.py`*): **the sheets every town shares, redrawn in place as a fable, one species each** — *the CHECKPOINT attendant an elephant (a checkpoint is a saved state, and the elephant never forgets; her fourth frame is a bow); the link receptionists honeybees, the Union Room's with a red accent; THE REPO's clerk a GREY squirrel, since orange fur and ears read as a fox at sixteen pixels and the foxes are the Clears; the deliveryman a homing pigeon in a courier's blue, moved to the pink slot for his greys; the officer an Alsatian with a notebook and a whistle, redrafted without the holster and handcuffs; and CORPUS STAFF penguins, the man the same drawing as CALLOW's gym staff, whose organisation they are, the woman in a skirt suit with a tablet.* **Both CORPUS STAFF portraits (`rocket_grunt_m`, 50 trainers; `_f`, 3) are those penguins.** *Drafts through `daemon/sprite`, kept in `gfx/characters/institution_*.png`.* |
| **T-117** | **Every vanilla sprite is to be ours, and how far along is measured** (*asked 2026-09-15*) | ***Tracking built*** (`tools/gbaspritecensus.py --write` → [`sprites.md`](sprites.md)): **every sprite compared with pret's by pixel, redrawn / recoloured / still vanilla, added ones kept out of the percentage; the people are the headline (18.8% at the start), every sprite a second line (8.3%), and a table per town.** ***Chosen 2026-09-15: a tiered plan*** — **T-118 the institutions every town shares, T-119 each town's own locals in a town palette, T-120 the rest of the shared sheets and every trainer portrait.** *Daemons (about 385 fronts and backs still vanilla) stay their own track, since the bestiary decides them.* **Re-run the census after each tier and keep `sprites.md` current** | `docs/sprites.md`, `tools/gbaspritecensus.py` | 9.4 |
| ~~**T-116**~~ | ~~**The BENCHMARK staff are still vanilla's people**~~ | ***Done 2026-09-15*** — **the 41 gym trainers and their guide are animals under 9.4's fable rule, one species per gym, chosen for the gym's idea**: *SLATE a young beaver apprentice; DOLDRUM a manatee afloat and a capybara at ease; ARDOR meerkats on watch; VERDIGRIS poodles clipped to shape; LURID tailless poison dart frogs; BRAZEN tarsiers watching whatever is framed; QUICKSILVER lab rats; CALLOW cheerful penguins in grey suits, warm and ordinary; the guide an old bloodhound.* ***Their OWN sprites, not redraws***: **every gym trainer had borrowed a shared class sheet** (*LASS is on 39 maps, PICNICKER's portrait 44 trainers*), **so the staff are new object graphics `OBJ_EVENT_GFX_STAFF_*` and `BENCHMARK_GUIDE` (153–162) and new portraits `TRAINER_PIC_STAFF_*` (148–156), and nothing outside the eight gyms changes** — *the Game Corner's and Indigo Plateau's guide keeps his old sheet.* **Portraits drafted through `daemon/sprite` and cut by `tools/gbachar.py`; the ten walking sheets drawn by `tools/genstaff.py`, each from the palette slot that holds its species' colours; DOLDRUM's manatee keeps the swimmer's ten frames, low in the water.** *48 map objects and 41 trainers repointed.* |
| ~~**T-115**~~ | ~~**The BENCHMARK leaders are still vanilla's people**~~ (*playtest 2026-09-15: DOLDRUM's leader walked and fought as MISTY under a new name*) | ***Overworld sprites — done 2026-09-15*** (`tools/genleaders.py`, *on genfolk.py's helpers*): **all eight leaders walk the map as the animals their portraits are** — *CAIRN's shell is his back, BASIN's muzzle is pink, GAUGE's ears stand up, TRELLIS has shears and a tail, TILT a visor and cards, MATTE a curled tail and brass glasses, ANNEAL his spots and glowing gloves, SCORN his hood and red tie.* **Seven stand in their rooms on vanilla's three still frames; SCORN keeps nine and walks** (*the hideout, Silph, the warehouse*). ***Each leader moved to the palette slot that holds its colours, no colour changed***: *CAIRN, ANNEAL and SCORN white; TRELLIS, TILT and MATTE green; BASIN pink; GAUGE blue.* **MATTE's framing hands stayed in her portrait: at sixteen pixels, hands raised beside the head read as ears.** | ***Battle portraits — done 2026-09-15.*** **Six leaders had no art. Each is now an animal under 9.4's fable rule, the species being the role**: *BASIN a hippo lido attendant, settled in the pool she cannot climb out of; GAUGE a hare line engineer, ears as receptors, reacting first; TRELLIS a bowerbird gardener, everything arranged to one exact shape; TILT a tailless toad card dealer, poisonous, still, in no hurry; MATTE a chameleon film editor, framing what is shown; ANNEAL a salamander metallurgist, at home in controlled heat.* **Drafted through n8n `daemon/sprite` (`kind: character`, OpenRouter), kept in `gfx/characters/`, cut to 64×64 with their own palettes by `tools/gbachar.py`.** *TRELLIS's first draft was a bird head on a person and TILT's first toad had a tail; both redrawn. The cutter's green-backdrop hue rule ate green garments and skin on white-backed art, so it is now per-job (`hue=False`), and enclosed backdrop is keyed for MATTE (`holes=True`).* ***Still to do: all eight overworld sprites*** — **CAIRN and SCORN included, whose portraits were already ours** — *drawn in code from these references in `genfolk.py`'s style, from the slot palettes each lives in* | `graphics/trainers/front_pics/`, `graphics/object_events/pics/people/`, `tools/gbachar.py` | 9.4, 5.3 |
| ~~**T-114**~~ | ~~**Daemons breathe in battle**~~ — ***Judged and kept 2026-09-16*** (engine `0bdd9fede`) (*asked 2026-09-15, after seeing ROM hacks sway and bob their battle sprites*) | ***Shipped in the debug ROM only*** (`src/battle_main.c`, `#if DAEMONS_DEBUG`). *The row read "uncommitted until judged" for a day after it had been judged and committed — a stale ticket is the failure mode this file warns about in its own preamble, found while answering "what is left".* **The plain breath — up to 2 px over about two seconds, the sides out of step, easing still during move, status and ball animations and the move-select bounce — was liked on first look (2026-09-15), and each type now moves by its own clause** (*2.2*): **LOGIC steps in held stairs; VECTOR rises forward along a diagonal; CORRUPT breathes until a four-frame glitch sideways; STRATUM moves one slow, heavy pixel; LEGACY's breath catches mid-cycle; SWARM jitters a pixel; LATENT sits low and rarely comes up; ENTROPY shimmers only upward; FLOW drifts and lifts as on water; GROWTH's breath grows, then starts over; SIGNAL is still but for a sharp pulse; CONTEXT sways sideways, as from a moving vantage; FROZEN is still but for a shiver; EMERGENT follows two crossing rhythms; HARDENED does not breathe, it braces; OPAQUE has shifted a pixel, and nothing showed why; CONTENT breathes plainly** — *and a CONTENT daemon with a second type moves as that type, since CONTENT hands you no verb.* *Sideways motion is mirrored for the opponent. The offset is added straight after `AnimateSprites` and removed straight after `BuildOamBuffer`, so no battle code, animation or `SpriteCallbackDummy` idle check ever sees it; no `Random()`, so battle rolls are untouched; 12 bytes of IWRAM, debug only.* ***To decide on playtest:*** **which types read and which are too much; a bounce inside chosen move animations; then move it out of the debug build** | `src/battle_main.c` | playtest |
| ~~**T-113**~~ | ~~**CALLOW BENCHMARK inside is vanilla's spinner gym**~~ | ***Done 2026-09-15*** (`tools/gbainterior.py callow`), **from a concept, approved on its second drawing**: *the survey floor* — ***a benchmark is a surveyor's mark***: **poured concrete, unripe green, with a surveyor's grid; every wall a cut section through the ground, its strata showing; the spinners painted in SCORN's red, the stop a brass survey disc; SCORN on a dark slab with a snake coiled faint in it; a fossil serpent in the rock of the back wall under a red plumb bob** (*vision.md: he is a red snake, composed, never comic*); *the TRUE statues, heads on the top layer.* ***The spinner maze is new***, **asked for so that a player who knows vanilla's still has to work it out**: *found by a search that simulates `field_player_avatar.c`'s slides, 75 moves from the door to SCORN, measured again on the built map, with 19 of them on arrows, 28% of its walls where vanilla's were.* ***`callow_check()` slides it again before every build and refuses a room where SCORN or a trainer is out of reach, where you can get somewhere you cannot leave, or where a slide never ends.*** **All nine staff keep their cells; its own tileset, `gTileset_CallowBenchmark`, 51 blocks and 106 tiles.** *The TRUE icon itself is unchanged.* ***With this, all eight BENCHMARK interiors are our own*** |
| ~~**T-112**~~ | ~~**QUICKSILVER BENCHMARK inside is vanilla's quiz gym**~~ | ***Done 2026-09-15*** (`tools/gbainterior.py quicksilver`), **from a concept**: *the silver lab where S.T.A.R.R. was built* — ***mercury-silver steel throughout, and the one colour in the room is S.T.A.R.R.'s blue***: **the glass conduit up the middle and its light on the floor either side, the quiz still running on her machines, and the barrier the conduit's light holds shut across door 4.** ***The deer is never pointed at***: *the conduit's electrodes branch like antlers over the back wall, and ANNEAL's annealing plate carries the same antlers, engraved* (**lineage.md: S.T.A.R.R. is the deer whose light shows the truth**). *Steel shutters with a blue seam, the photograph, the HEAT statues with heads on the top layer.* ***Vanilla's quiz is whole, as 5.3 says***: **every door opens by its script's own blocks, each drawn from the room with every door open and checked to draw the same at every door that uses it; every cell keeps its collision and behaviour.** *Its own tileset, `gTileset_QuicksilverBenchmark`, 69 blocks and 166 tiles* |
| ~~**T-111**~~ | ~~**BRAZEN BENCHMARK inside is vanilla's teleport-pad gym**~~ | ***Done 2026-09-15*** (`tools/gbainterior.py brazen`), **from a concept**: *the gallery of mounts* — ***each room is a picture seen through its own mat***: **gilt frame mouldings between the rooms, a cream bevelled mat where they meet each room, and a halftone print in a different ink on every floor** (*cyan, magenta, yellow, key*), *so a room shows only its own part of the picture.* **The 30 pads are brass viewfinder brackets round a dark window; MATTE stands on the bare mount in the centre room, inside an empty gilt frame; the FRAME statues, heads on the top layer.** ***Vanilla's pad maze is whole, as 5.3 says*** — *every warp, and every cell's collision and behaviour, as it was; its own tileset, `gTileset_BrazenBenchmark`, 79 blocks and 136 tiles* |
| ~~**T-110**~~ | ~~**Players stood on the BENCHMARK statues' heads**~~ | ***Done 2026-09-15*** (`tools/gbainterior.py`): **the generator put every block on the bottom layer, so in SLATE, DOLDRUM, ARDOR and VERDIGRIS a player walking the cell above a statue was drawn over its head.** ***Statue heads are now drawn on the top layer, as vanilla has them*** — *the room is drawn a second time with no statues to find the floor under each head, and the head's own pixels go on top, see-through elsewhere* — **and all four were rebuilt from their vanilla maps with no cell's collision or behaviour changed.** *LURID's heads are solid instead, part of its maze* |
| ~~**T-109**~~ | ~~**LURID BENCHMARK inside is vanilla's invisible-wall gym**~~ | ***Done 2026-09-15*** (`tools/gbainterior.py lurid`), **from a concept, approved on its third drawing, and a design change** (*vision.md 5.3, v11.207*): *the tilted room* — **a floor grid that leans half a pixel a row, the same over wall and open cell alike; a violet wall with a long-tailed distribution chart and green lamps; TILT on a slab that leans with the floor; the SKEW statues.** ***A new invisible maze in two halves***, *99 walls, 50 steps from the door to the upper-left pad and 53 from the upper-right pad to TILT, sealed from each other and checked by the tool.* ***Four corner pads turn you clockwise*** (**`MB_REGULAR_WARP` and four warps to the same map**); *the lower-left pad has a pocket of its own with room to step off and on, so the first half cannot be skipped and the second half has a way back.* ***When TILT loses*** — *straight after the battle, and on every visit after by an on-load script* — **every leaning tile a wall mostly covers swaps its light and dark, and a lit way opens from his dais straight down to the door**: *164 cells, a `setmetatile` list the tool writes into the map's `scripts.inc`.* ***Its own tileset, `gTileset_LuridBenchmark`; every trainer keeps his cell.*** **Not yet playtested** |
| ~~**T-108**~~ | ~~**VERDIGRIS BENCHMARK inside is vanilla's flower gym**~~ | ***Done 2026-09-15*** (`tools/gbainterior.py verdigris`), **from a concept**: *the espalier house* — ***every hedge is a plant pruned flat along a verdigris frame***, **the frame the plant grows to and no other shape**; *a lawn mown in stripes, the flowers become identical seedlings on canes with one bloom each, gravel where the sand ran, edged in bronze, a glasshouse wall with bars gone green, bronze columns, TRELLIS on gravel in her alcove, the FIT statues.* ***Every hedge cell stays blocked and every lawn and path cell open, so the maze and its three cut trees play as before; its own tileset, `gTileset_VerdigrisBenchmark`*** (*no flower animation: that callback stays with `gTileset_CeladonGym`*) |
| ~~**T-107**~~ | ~~**ARDOR BENCHMARK inside is vanilla's electric gym**~~ | ***Done 2026-09-15*** (`tools/gbainterior.py ardor`), **from a concept**: *the signal room* — **dark floor panels with copper along the joints, a deep red wall with a scope reading GAUGE's signal, copper coils where the pillars stood, GAUGE on a steel dais with a hazard stripe, amplifier racks with blinking lights at the two locks, the SENSE statues.** ***Vanilla's switch puzzle is whole***: **the fifteen bins are fifteen identical steel ports, three unlabelled holes each, and the beam barrier is a signal gate between two emitters, drawn on, half on (the lower beam out after the first switch) and off**, *so every block id the map script swaps in has art of its own.* ***Every cell keeps its collision and behaviour; its own tileset, `gTileset_ArdorBenchmark`*** (*no tile animation: vanilla's motorised door callback stays with `gTileset_VermilionGym`*). **`build()` now refuses to rebuild a layout already on its new tileset** — *a rerun read the new map's ids against the old tileset and inherited vanilla's behaviours by number* |
| ~~**T-106**~~ | ~~**DOLDRUM BENCHMARK inside is vanilla's pool gym**~~ | ***Done 2026-09-15*** (`tools/gbainterior.py doldrum`), **from a concept**: *the becalmed lido* — **dead-calm deep water with faint rings settling round BASIN's island, pale stone walkways with a blue gutter on a lit coping, a tiled wall with portholes onto a flat horizon, steel pool ladders, the SLOPE statues.** ***Every water cell stays surfable and every walkway floor***, *read from the room itself; its own tileset, `gTileset_DoldrumBenchmark`* |
| ~~**T-105**~~ | ~~**SLATE BENCHMARK inside is vanilla's rock gym**~~ | ***Done 2026-09-15*** (`tools/gbainterior.py slate`), **the pilot BENCHMARK interior, from a concept**: *a slate hall that keeps a record* — **slate flagstones, a back wall of chalk tallies in fives, one for every match fought here, a slate board of names behind CAIRN's pale dais, two lamps, a limestone path from the door, and a cairn of stacked stones with a chalk mark wherever a boulder stood**, *so every cell keeps its collision and the room plays as before.* ***Its own tileset, `gTileset_SlateBenchmark`; Brazen's dojo keeps `gTileset_PewterGym`.*** **Next: the other seven, one concept each** |
| ~~**T-104**~~ | ~~**The BENCHMARK statues are vanilla's**~~ | ***Done 2026-09-15*** (`tools/gbastatues.py`): **the shared statue is a stepped stone plinth with a gold plaque, a plain stone orb on it** — *which is what the 51 other interiors that stand one now show* — ***and in each BENCHMARK the figure is its MARK***, *the trainer card's icon cast in gold on a stone cap, on tiles of that BENCHMARK's own tileset: the slate, the hillside, the receptor, the fitted line, the skewed peak, the brackets, the thermometer, the plumb bob.* **The plaque's script, LEADER then WINNING USERS, reads as the record the instrument certifies** |
| ~~**T-103**~~ | ***Built 2026-09-15 (`tools/gbainterior.py`)***: **two new tilesets, `gTileset_Checkpoint` (both Kanto floors and One Island's identical upstairs) and `gTileset_Repo` (the REPO), each generated from the approved concept** — *our own floor, wallpaper, mat and plants; the CHECKPOINT's lounge, escalator, terminal, wall map and link-spot marks; THE REPO's poster, L counter, bench and island.* **Every reserved block id kept** (*the escalator's 21 frames, the cable club door, barrier and shade, Viridian's counter pieces*). ***One Island's and Indigo Plateau's downstairs followed the same day, on tilesets of their own (`gTileset_CheckpointOneIsland`, `gTileset_CheckpointIndigo`)*** — *the theme and the new floor, Kanto's new furniture wherever the same vanilla block stands, our plant for Indigo's three; their own furniture (One Island's network machine, Indigo's statues, the glass tables) recoloured; the network screens One Island's script switches on keep their ids.* ***Still to note: the Kanto CHECKPOINT tileset is at 381 of 384 tiles, so any addition costs a detail; `tools/gbacheckpoint.py` and `tools/gbarepo.py` now only matter for those older layouts.*** **Original ticket:** **CHECKPOINT and REPO, the second pass** (*playtest 2026-09-15*). ***A new floor plan in each, so they read fresh***, *propose it with a concept first.* **Both:** *our own floor tiles, our own entrance mat (the common one), our own wallpaper, our own potted plant.* **CHECKPOINT:** *the PC terminal loses its red cross; the wall map looks different; the stairs up are tweaked; the table on the right is redesigned; the two chairs at the far right become a couch; the two chairs above the table may go.* **REPO:** *its own colour scheme; the upper counter and the items on it change; the shop counter is one solid L-shaped counter, not glass with a questionnaire and blue; the upper-left poster changes and carries THE REPO's branch emblem; the second glass counter at the bottom left may go.* ***Every warp, clerk, nurse, sign and script position across every map built on the two layouts has to survive the new plan*** (*17 maps on the CHECKPOINT's, 12 on the REPO's, 18 upstairs*). ***Chosen 2026-09-15: other buildings that share these tilesets may inherit the new textures*** — **anything that looks wrong there is fixed later** — ***and the CHECKPOINT's upstairs (`LAYOUT_POKEMON_CENTER_2F`) gets the same theme*** — **its two floor link spots lose their Poke Balls for a mark of our own.** ***Concept approved 2026-09-15: remove the two chairs above the table; keep the questionnaire hidden in the L counter; build as drawn.*** *Built as a fresh tileset per building from the concept art, keeping every block id the code or a script names — the escalator's frames, the cable club door, barrier and shade, Viridian's two counter blocks — with their art recoloured into the theme; One Island's and Indigo Plateau's downstairs stay on the old tileset for now* | `LAYOUT_POKEMON_CENTER_1F`, `_2F`, `LAYOUT_MART`, the `pokemon_center` and `mart` tilesets | T-65 |
| ~~**T-65**~~ | ***Done 2026-09-15.*** **The CHECKPOINT's capsule and red cross are a steel rack: a dark bay of six slots where the heal places its sprites, green progress lights either side, a plain plinth** (`tools/gbacheckpoint.py`); *the heal's ball is a BOX cartridge and its wall monitor a status panel whose three lights sweep.* **THE REPO's wall unit and aisle gondolas hold labelled amber package bins** (`tools/gbarepo.py`), *in the Mart tileset's empty row 10.* ***Both re-point their blocks in place: every CHECKPOINT and every REPO changes, no map does, and every counter, clerk and script keeps its tile.*** *Left: the PC terminal beside the rack still wears a small red cross.* **Original ticket:** **CHECKPOINT and REPO interiors** (*9.23*). ***The healing machine becomes a rack of BOX slots with progress lights***, *and its healing animation follows*; ***the REPO's shelves hold labelled package bins***. **Every counter, clerk and script keeps its tile** | `data/tilesets/secondary/pokemon_center/`, `data/tilesets/secondary/mart/`, `graphics/field_effects/pics/` | **9.23** |
| ~~**T-79**~~ | ~~**A BENCHMARK's two words**~~ — ***Verified closed 2026-09-17*** (closed in `6f398f27`) | this commit — ***the board reads MARK***, **its face moved down into the cell every town has** (*Quicksilver's board has no top cell*); ***the frieze right of the door carries the mark's name on two blocks of each town's own*** — *SLATE, SLOPE, SENSE, FIT, SKEW, FRAME, HEAT, TRUE.* **The 3x5 K no longer reads as an E** |
| ~~**T-83**~~ | ~~**The sandbar, again**~~ — ***Verified closed 2026-09-17*** (closed in `6f398f27`) | `3c0444d84` — ***the glitch was the engine, not the ids***: **the screen is not redrawn when you cross a connection, so any secondary cell on screen at the crossing reads the other tileset** (*engine.md trap 14, corrected*). ***Now three rows deep across the sea, with two sand arms down both sides to the join and a channel x 7..16***, **so the border is never on screen when you cross**; *the arms' last row is solid, so nobody surfs round.* **Quicksilver's tileset is as it was** |
| ~~**T-84**~~ | ~~**A sketch on the station table**~~ — ***Verified closed 2026-09-17*** (closed in `6f398f27`) | `3c0444d84` — ***one rabbit drawn three times, faceted purple, split, white*** **on a sheet under the visitor log** (`tools/gbastation.py`, *the Mansion tileset's unused row 9*); **the log reads on: *"In the first it is all corners. In the last it is not. Each one is drawn from a different side of the table."*** |
| ~~**T-85**~~ | ~~**Our buildings are simpler than vanilla's**~~ — ***Verified closed 2026-09-17*** (closed in `6f398f27`) | this commit — ***detail where the budget allows***: **`tools/gbadedupe.py` freed 45 of the General tileset's duplicate tiles** (*1,809 entries repointed, each checked to draw the same pixels*), *and T-77's drawings spend them on a cornice, a canopy, a plinth and container castings, with mirrored tiles shared.* ***The ceiling is real: five slots are left.*** **More detail than this means drawing on each town's own tiles**; ***then drawn on town tiles*** — **`tools/gbacivictown.py` redraws 35 of the 37 buildings in full detail on their own town's tileset** (*unreferenced slots and each town's duplicates, `gbadedupe.py --secondary`*). **Lurid's BENCHMARK and Brazen's REPO keep the shared drawing** |
| ~~**T-86**~~ | ~~**Vanilla signs still in towns**~~ — ***Verified closed 2026-09-17*** (closed in `6320b58a`) | this commit — ***Quicksilver's lab board is peeling gold leaf over older letters; One Island's reads NET*** (`366fe4e8c`); ***Brazen's two red GYM roundels are a MARK board before the BENCHMARK and a QED board before the PROOF HALL*** (`tools/gbaislandsigns.py`). **The "Sevii" overlap was Quicksilver's CHECKPOINT and REPO, which stand wall to wall**: *under the shared drawing one building's blocks showed inside the other's; T-85's town-tile redraw gives each its own blocks and they read cleanly.* ***No Sevii island has the two touching.*** ***Found later: Slate has one more red squiggle board, by the fence in the south-east.*** ***Closed 2026-09-15: it was not one board but the ordinary signpost itself*** — **General block 2's face (tiles 304, 305, 320, 321) carried the red lettering on every outdoor sign in the game; redrawn once in place with two lines of grey writing** (`tools/gbasignface.py`) |
| ~~**T-87**~~ | ~~**AL on the naming screen is still vanilla's rival**~~ — ***Verified closed 2026-09-17*** (closed in `5ece3749`) | this commit — ***`tools/genclears.py` writes Al's nine frames, stacked, to the naming screen's own sheet with the Clears' palette*** |
| ~~**T-88**~~ | ~~**The agent named its rival ABABAB**~~ — ***Verified closed 2026-09-17*** (closed in `5ece3749`) | `engineAi` 247d57c — ***the prompt now asks for the rival's name to be decided before the grid, like a nickname***: **3..7 characters, pronounceable, no character more than twice, no alternating pattern, not the agent's own name or a default** |
| ~~**T-89**~~ | ~~**The player's house, inside, both floors.**~~ ***Done — skins `756fecfa`, layout engine `9512393c1`.*** **A timber cottage with depth** — *its own tileset `gTileset_PlayersHouse` (`tools/gbahouse.py`), top and front faces lit from the upper left, contact shadows, a shadowed side wall* — ***on a new layout, chosen from a concept and redrawn with four fixes:*** *the dining table clear of the mat, a real counter peninsula (top and front at the back counter's height), bigger armchairs, a roomier console corner.* **MOM stands in her kitchen at (3,3); the console moved to (9,6); `PLAN` in the tool sets every cell's collision and behaviour.** *Exactly 384 tiles — the boot tray was cut to fit* | `PalletTown_PlayersHouse_1F`, `_2F`, `tools/gbahouse.py` | playtest |
| ~~**T-90**~~ | ~~**The console takes codes**~~ — ***Verified closed 2026-09-17*** (closed in `3f9aa64c`) | `395476874` — ***a CODE? keyboard from the old console***; **HELP gives one of five riddles whose answer is a code**: *TUNE plays a town theme, TIME shows play time, MOM calls up the stairs, STARS fills the screen with stars, LOG prints the console's log.* **Lore and cosmetic only** |
| ~~**T-91**~~ | ~~**Blanche's mailboxes are vanilla's shape**~~ — ***Verified closed 2026-09-17*** (closed in `3f9aa64c`) | `d07754ae1` — ***every town's mailbox is ours***: **a rounded rural mailbox with a dark slot and an ink flag on a post**, *redrawn once in the General tileset* (`tools/gbamailbox.py`); **Blanche's is pale through its row 10, with no warm colour** |
| ~~**T-92**~~ | ~~**The Index looks like a Pokédex**~~ — ***Verified closed 2026-09-17*** (closed in `3f9aa64c`) | `d07754ae1` — ***the Index screens are slate, pale slate, navy and steel cyan***, *the category cursor pulses slate to cyan*, **the area map's markers are cyan**. ***The lab's Index units are a closed device with a lens and two lights***, **but in indigo and lavender**: *their sprite must use the NPC blue palette, which has no true slate or cyan* |
| ~~**T-93**~~ | ~~**A new game starts at FAST text**~~ — ***Verified closed 2026-09-17*** (closed in `8250412c`) | `35e6ee739` — ***`SetDefaultOptions` sets FAST***; **the option menu can still change it** |
| ~~**T-94**~~ | ~~**The debug ADD list is one long list, and the banner clips**~~ — ***Verified closed 2026-09-17*** (closed in `fdfc3554`) | this commit — ***ADD lists one pocket at a time***: **LEFT and RIGHT step through ITEMS, KEY ITEMS, BOXES, TM CASE and TRAP TABLE**, *the pocket named on the help banner*. ***The banner no longer shows the map through its corner***: *a taller page's cleared frame took the banner's tiles with it, so `DbgRedraw` puts them back before drawing* |
| ~~**T-95**~~ | ~~**Mailboxes have an echo above them**~~ — ***Verified closed 2026-09-17*** (closed in `fdfc3554`) | this commit — ***vanilla's lid, in the cell above every mailbox, still drew***: **General tiles 336 and 337 are cleared** (`tools/gbamailbox.py`), *used outdoors by nothing else* |
| ~~**T-96**~~ | ~~**You can walk onto a BENCHMARK's roof**~~ — ***Verified closed 2026-09-17*** (closed in `fdfc3554`) | this commit — ***the colonnade's roof row stood on vanilla's walkable overhang***: **38 cells blocked in Celadon, Cerulean, Cinnabar, Pewter, Vermilion and Viridian** (`tools/gbaroofs.py`); *the row behind stays open everywhere, no event stands on any of them* |
| ~~**T-97**~~ | ~~**The town flower is vanilla's**~~ — ***Verified closed 2026-09-17*** (closed in `5cdb5e8d`) | this commit — ***two tulips, cupped heads on straight stems, long pointed leaves***, *swaying a pixel either way over vanilla's five frames* (`tools/gbaflower.py`); **in General row 0's own reds and greens, no hue added**. *Blanche keeps its own white flower* |
| ~~**T-98**~~ | ~~**TEACHY TV becomes STREAM**~~ — ***Verified closed 2026-09-17*** (closed in `5cdb5e8d`) | this commit — ***the item was STREAM already; the title card now reads STREAM***, *a pink play mark and a blue wave where the T and V were, the badge and its style kept* (`tools/gbastream.py`); ***the presenter is CRYSTAL CLEAR's sprite***. **Open: the show's lines are still the old host's voice** — *"HELLO, USERS! It's me, the TUTOR!"* — **which is not how CRYSTAL speaks** | ***Checked 2026-09-17: the "Open" is STALE*** — **the show speaks as CRYSTAL CLEAR now** (*"This is a recording. CRYSTAL CLEAR. A new lecture has been added to the list."*) **and every lecture is rewritten**, engine `ced272b5d`, *the same day, under T-99's commit — which is why this row never heard about it.*
| ~~**T-99**~~ | ~~**HEARSAY still carries vanilla's Fame Checker lines**~~ — ***Verified closed 2026-09-17*** (closed in `b290255c`) ***Done — this commit*** (`tools/port_hearsay.py`): **all 96 entries, 16 letters and 13 journals, and the 53 world lines they quote**; *each person has an admirer and a doubter, both right about something* — **CRYSTAL's rival: "Wanted it more than she wanted any of us. Mind you, she was usually right."** *SCORN's "criminal enterprise" and his "kid" are gone.* **Original ticket:** **HEARSAY still carries vanilla's Fame Checker lines.** *SCORN's "our criminal enterprise" is not his voice.* ***Every entry is what someone else says of a character, so each character's set must hold more than one view*** — **the case against them and the case for them, with trust in all of them** (*even CRYSTAL's projects read to someone as blind ambition*). **Check the bible's characters before writing; never state the thesis.** ***Scope, chosen 2026-09-14: the 96 entries, the 16 letters, the DAEMON JOURNAL copies, and the world lines each entry quotes (about 40 NPC and sign lines)***, *so HEARSAY always matches what someone in the world says*; **the four REVIEW BOARD room intros stay vanilla-flavoured for now** | `data/text/fame_checker.inc`, the quoted maps' `text.inc` | playtest 2026-09-14 |
| ~~**T-101**~~ | ***Done 2026-09-15***: **the Brazen employee is "on the growth track! Every one of my objectives is green. I don't know what they are!"**; *the ARDOR MART shopper: CORPUS lets the DAEMON that don't test well go out the back, "very polite about it"*; ***PHLEGMATIC in STILLFALL CAVE: "They were not cruel to the DAEMON. They were counting them. I am not sure yet which is worse."*** **And the REVIEW BOARD's four rooms are in their humours now, through `tools/port_hearsay.py`.** **Original ticket:** **CORPUS staff still call themselves criminals.** *Three vanilla lines survived the HEARSAY pass because no entry quotes them:* **BRAZEN CITY's "My life as a criminal makes me feel alive", the ARDOR MART's "DAEMON for criminal acts", STILLFALL CAVE's "criminals were loose here"** — ***craft rule 6: CORPUS employees are cheerful and absurd, and they do not think they are criminals*** | `SaffronCity`, `VermilionCity_Mart`, `FourIsland_IcefallCave_Back` text | T-99 |
| ~~**T-102**~~ | ~~**THE SCHOLAR'S HOUSE looks like every house in Brazen**~~ | this commit — ***the stone study, chosen from three concepts (study, library, observatory) 2026-09-14***: **grey fieldstone and a slate roof, a lamp in a round window, book-lined windows, a stone arch and brass plaque, ivy, a stone owl at the ridge** — *the one ungilded house in the bought city, and nobody says why* (`tools/gbascholar.py`). **Its own 22 blocks in both Brazen layouts; the door keeps block 644 for its animation; drawn for the 8-pixel grid so 31 tiles cover it** |
| ~~**T-100**~~ | ***Done — this commit***: **every lesson, the battle demos' narration (`data/text/pokedude.inc`) and the item description** are CRYSTAL's recorded lectures, *each ending "That is the lecture." and the last "That was the last lecture. There were to be more."* **Original ticket:** **STREAM's lessons in CRYSTAL's own voice.** *Chosen 2026-09-14:* ***the show is recordings of the lecture series that was announced twice and never held*** — **her dry, precise register, not the old host's "HELLO, USERS! It's me, the TUTOR!"** | `src/data/text/teachy_tv.h` | T-98 |
| ~~**T-80**~~ | ~~**The RUNNING SHOES, told and everywhere**~~ — ***Verified closed 2026-09-17*** (closed in `ad317337`) | this commit — ***MOM: "Try them out. Hold B while you walk. You will need the room."***, *in her voice rather than a narrator's*; **running allowed on 180 more maps**, *every one but the five link rooms* |
| ~~**T-81**~~ | ~~**Al calls her CRYSTAL**~~ — ***Verified closed 2026-09-17*** (closed in `ad317337`) | this commit — ***eleven lines***, **three rewrapped** (*"All right, CRYSTAL! / Leave it to me!"*, *Vera's LAB line and her instrument line*); **4.3 rewritten and the old word logged in Reversed** |
| ~~**T-82**~~ | ~~**The eight meetable daemons with vanilla names**~~ — ***Verified closed 2026-09-17*** (closed in `ad317337`) | this commit — ***TRUST → HOPE, VALENCE, RANKLE*** *(islands)*, ***MAGTAPE → MAINFRAME, DRUM → PUNCHCARD*** *(Kanto)*, **each with a new Index entry and category in both editions**, *and the two Fuchsia zoo signs.* **`SPOOL` was refused by `check_lexicon`: it is a routine** |
| ~~**T-72**~~ | ~~**No seams on Route 1**~~ — ***Verified closed 2026-09-17*** (closed in `6e1d9be5`) | this commit — ***the trees follow the grass***: **152 cells of birch back to vanilla's broadleaf above row 30, and the border**, *which also ends the Callow glitch.* ***Blanche's chalk path runs unbroken to the ledge at row 26*** **through coloured grass** — *vanilla's path tiles pointed at four spare slots of row 10* — **and the grass changes at the first pale ledge's lip, split by quadrant** (`tools/gbaroute1.py`). *8 tiles, 23 blocks* |
| ~~**T-73**~~ | ~~**A connection draws the neighbour with this map's tileset**~~ — ***Verified closed 2026-09-17*** (closed in `6e1d9be5`) | this commit — ***`tools/gbaseams.py`***, **engine.md trap 14.** *Route 1 and Route 21 now report nothing; the 541 cells it still lists are in maps we have not drawn (Fuchsia/Route 18, Routes 22 and 23, Saffron's gate approaches) and are left as found.* ***Dual ids*** — **one id, a different drawing in each tileset** — *are declared in the tool* |
| ~~**T-74**~~ | ~~**A sandbar across Route 21, and the room under it**~~ — ***Verified closed 2026-09-17*** (closed in `6e1d9be5`) | this commit — ***gold sand the full width from North row 41 to South row 5***, **the hatch at (11,42), a caretaker (a lab mole) beside it** (`tools/gbasandbar.py`). *Its side and bottom edges and the row of water under it are dual ids, pale in Blanche's tileset and blue in Quicksilver's, which is padded to 312 blocks for them.* ***`Route21_North_Station`***: **an 11x11 room cropped from the Mansion's basement, statue removed, two lit terminals with their input cables out, and the visitor log.** *Its stairs are a copy of the Mansion's block with a north warp, so the Mansion is unchanged* |
| ~~**T-75**~~ | ~~**S.T.A.R.R. knows GOTO**~~ — ***Verified closed 2026-09-17*** (closed in `6e1d9be5`) | this commit — ***GOTO at 70 after RECURSION, so a wild S.T.A.R.R. has SAFEGUARD, PSYCHIC, RECURSION and GOTO***; **HM02 compatible**; *the debug party gives it GOTO and SEEKMUSAI's slot takes ADVERTISE* |
| ~~**T-76**~~ | ~~**No GYM left in anything the player reads**~~ — ***Verified closed 2026-09-17*** (closed in `6e1d9be5`) | this commit — ***101 edits in 33 files***, **measured against the widest line each box already ships.** *The FAME CHECKER's eight VERDIGRIS and QUICKSILVER labels read BENCHMARK alone (88px box).* ***The one GYM left in the ROM is Hoenn's mystery-gift PETALBURG line, which FireRed cannot show.*** **Title stays LEADER** |
| ~~**T-77**~~ | ~~**CHECKPOINT, THE REPO and BENCHMARK stop looking like houses**~~ — ***Verified closed 2026-09-17*** (closed in `6f398f27`) | this commit — ***the BENCHMARK a colonnade*** (*flat roof, a dentilled cornice, a frieze with a relief of bars, the gauge over the door, columns, one step*), ***the CHECKPOINT a rotunda*** (*a teal dome with the restore arrow over a stone drum, tall windows, a canopy over the door, on a terrace*), ***THE REPO stacked containers*** (*corrugated, corner castings, the branch stencilled on the top one, REPO on the left*) — **all 37 by `tools/gbacivic.py`, 127 of 132 free slots.** *The CHECKPOINT's checkmark plate is gone: the dome is the sign* |
| ~~**T-78**~~ | ~~**A BENCHMARK's pillars wear its leader's type**~~ — ***Verified closed 2026-09-17*** (closed in `6f398f27`) | this commit — ***the two pillars at the door are drawn on the top layer in palette row 7***, **which all eight towns leave unused, and each town's row 7 holds its leader's type colour** (*LEGACY grey at Slate, FLOW blue at Doldrum, SIGNAL teal at Ardor, GROWTH green at Verdigris, CORRUPT mould at Lurid, CONTEXT magenta at Brazen, ENTROPY gold at Quicksilver, STRATUM earth at Callow*) |
| ~~**T-61**~~ | ~~**The world's own trees, water and grass**~~ — ***Verified closed 2026-09-17*** (closed in `93cf3013`) | e976bb455, 68fc4c046 and this commit — ***water*** *(ripple dashes in both animated groups, `tools/gbawater.py`)*, ***grass*** *(our greens across 58 outdoor palettes, our sprig, tall grass as blades, the walking effect, `tools/gbagrass.py`)* ***and trees*** *(a broadleaf traced into 36 pieces from standalone, forest and forest-edge scenes, `tools/gbatrees.py`)*. **Left vanilla:** *seven rare edge pieces on about 20 cells (Four Island, the unused R/S Safari maps, one cell of Route 24), bushes and hedges, the flowers, and the secondary tilesets' own trees (Viridian Forest, Berry Forest, the towns). Blanche's pale tall grass still has vanilla's shape until `gbaroutes.py` re-derives it* | ***Checked 2026-09-17***: *the "until `gbaroutes.py` re-derives it" is settled by design, not pending* — **`gbaroutes.py` gives every ground colour a job and keeps tall grass, paths and ledges in vanilla's SHAPE on purpose**, *so the grass is re-coloured and its shape is the intended one.*
| ~~**T-60**~~ | ~~**Where the pale ends**~~ — ***Verified closed 2026-09-17*** (closed in `f5be2440`) | this commit — ***Route 1's last ten rows, its sides, its top row and its border are Blanche's birches and pale ground; all of Route 21 North is pale, the sea in shallow and deep pale water that both animate, ending behind a line of pale rocks with a gap at x 10..13***, *by `tools/gbaroutes.py`.* **Signs read BLANCHE, LAB, ? and RT1** (*`tools/gbasigns.py`*). ***Also fixed on the way:*** *map changes never reached the ROM (engine.md trap 13); the water animation painted birch crowns (`tools/gbacompact.py` now guards the animated slots); the lab door animates two cells tall; the lab plants.* **Blanche's tileset is 339 of 384 tiles** |
| ~~**T-59**~~ | ~~**Blanche's two houses, two buildings**~~ — ***Verified closed 2026-09-16*** (engine `6c6f734b2`) | 6c6f734b2 — ***the player's house a timber cottage*** *(weatherboard, a shingle roof, a chimney, a dormer, a porch, window boxes)* ***and the Clears' house the lab's pale stone*** *(a hipped slate roof, tall windows, a stone door surround, a glasshouse)*, **by `tools/gbahouses.py` on vanilla's footprints.** *The cottage is row 11; the stone house is the lab's row 9, its unused indices 11 and 15 now the glasshouse's greens.* **Both doors are block 675, which the door animation needs, so they share one door and each house frames it.** ***Blanche's tileset is now 342 of 384 tiles*** — *the next redraw there should reclaim the orphaned vanilla ground and house tiles first* ***Checked 2026-09-16***: **the budget moved — the build declares `-num_tiles 356` for `pallet_town` now, not the 342 this row recorded, so the headroom is 28 tiles rather than 42.** *The note about reclaiming the orphaned vanilla ground and house tiles before the next redraw there still stands, and is worth more than when it was written.* |
| ~~**T-58**~~ | ~~**Blanche's ground, drawn**~~ — ***Verified closed 2026-09-16*** (engine `38f41c9d3`) | 38f41c9d3 — ***silver-green grass, white chalk paths on vanilla's own shapes, 33 white-barked birches, white daisies and a clear pond with a pale stone rim***, *by `tools/gbaground.py`, from B's shapes and A's colours.* **The ground is derived pixel by pixel: each vanilla colour is given a job, so every path curve survives; the trees, daisies and pond are drawn new.** ***Rows 7 and 12 share their eight ground colours***; *Blanche's own blocks are rewritten in place so the door ids hold, 19 common ones are copied, and the border points at the tree copies.* **`TilesetAnim_PalletTown` animates the daisies (800) and the water (804); the wash now touches only the primary rows.** ***Seams left:*** *Route 1's conifers across the north exit and Route 21's water past the pond are vanilla, washed* ***Checked 2026-09-16***: **the "seams left" note is STALE** — *T-72 took the birches back to vanilla's broadleaf above row 30, and T-73's `gbaseams.py` reports Route 1 and Route 21 clean.* **The seams this row leaves open were closed by the two tickets after it, which is what a read-and-check is for.** |
| ~~**T-57**~~ | ~~**CRYSTAL CLEAR's lab, the room inside**~~ — ***Verified closed 2026-09-16*** (engine `53f498499`) | 53f498499 — ***pale plaster and stone, the computer, a desk of loose papers and an open notebook, the two posters, the INDEX unit with its gold trim and her note, tall shelves and two low shelves of journals, the PORT with two BOXES docked and two slots empty, the bench, two plants and a mat***, *by `tools/gbalabroom.py`.* **The tileset is rebuilt, not appended to (213 tiles, 69 blocks)**; *every cell keeps its attributes and collision, so the computer, the signposts, the shelves and the exit keep their behaviours.* ***One tile, one palette row***: **walls and floor use the six neutrals rows 7..11 share; each object family has its own row, and a tile where two meet is refused.** *Objects draw over the player only where vanilla's layer type did* |
| ~~**T-41**~~ | ~~**The 41 thin openings.**~~ ***Closed `7f2df046d` — and the list was wrong.*** **Twenty-two of the forty-one were daemons opening with `WRITE`, `FLIP` or `PUSH`, which 2.8 SETTLED ON PURPOSE.** *A check that fires on a thing the design decided is arguing with it, not measuring it.* `THIN OPENING` is retired and replaced by two flags that mean separate things — **`MIXED SIGNAL`** (*before 10 it does something typed, it is the wrong type, and it never does its own*) and **`LATE TYPE`** (*first on-type damaging routine after 25*) — **which between them caught four the old flag missed.** Eighteen substitutions, nineteen daemons, all four flags now none, off-type 41% | `level_up_learnsets.h` · `tools/gbacoherence.py` | **2.7d**, 2.8 |
| ~~**T-49**~~ | ~~**Nothing has ever checked a name against the DATA it sits on.**~~ ***Closed `adaa532a8` — `tools/check_names.py`, and it fires on CONTRADICTION only.*** **Five moved** — *`REINFORCE`→`FATIGUE`, `DEPRECATE`→`LONG SUPPORT`, `DISSOLVE`→`PASSIVATE`, `NOISE`→`RESIDUAL`, and `CHARM`→`TRANSPOSE`* — **three are rulings printed by `--rulings`**, and *`FAULT` is a word the check deliberately does not list, because a ruling that can never fire is worse than no entry.* ***`DISPLACED` is the class the original count missed***: **a name that is vanilla's name for something else is invisible from either table alone.** *`IRON TAIL`'s description had been arguing for the wrong name.* **Verified by putting all five back** | `tools/check_names.py` | **2.6**, 2.8, **2.13** |
| **T-50** | **Read `playtest.json` and act on it.** *8.10 gave the agent the ask it never had; this is the other half.* ***The run before the fix produced 200 tactical asides and zero playtest entries***, so there is nothing to read yet — **run it, then read the file once and triage: `CONFUSED` first, because that is a fact about the writing rather than about the reader.** *It is the only class of finding none of the five checks can produce* — **every check this project owns reads a TABLE and nothing reads a SESSION.** *Findings that turn out to be jobs get ticket ids; findings that turn out to be questions go to the Open log* | `engineAi/server/gpt_data/playtest.json` | **8.10** |
| ~~**T-54**~~ | ~~**Blanche's lab gets its own interior tileset**~~ — ***Verified closed 2026-09-16*** (engine `2525bcc42`) | 2525bcc42 — ***`gTileset_BlancheLab`***, **a byte-identical copy of `gTileset_Lab`'s tiles, metatiles, attributes and sixteen palettes, with its own `-num_tiles` rule**, *drawn by `PalletTown_ProfessorOaksLab` alone.* **The Quicksilver lab's four rooms and the Three Island house keep the original**, *and so does the founder's photo.* ***No metatile of the Lab was named anywhere, so every block id stays valid in both*** |
| ~~**T-56**~~ | ~~**Blanche's ground and trees, washed at runtime**~~ — ***Verified closed 2026-09-16*** (engine `673fd7db2`) | 673fd7db2 — ***grass, trees, flowers and the pond*** **washed as the palettes load**: *common rows 0..6 and Blanche's 11 and 12, on the Blanche map only, tiles only, never rows 8..10.* **Five-bit, so both grass rows match exactly.** ***And the step across the Route 1 connection reloads the primary rows***, *which vanilla never does, or the wash would have followed the player out* |
| ~~**T-55**~~ | ~~**Blanche's houses and fences go pale**~~ — ***Verified closed 2026-09-16*** (engine `2b300e14d`) | 2b300e14d — ***roofs, walls, windows, doors, fences, mailboxes and the two signs***, *by `tools/gbablanche.py`.* **Row 10 (free since T-53) is pale row 2 index for index; row 3's 13 tiles are copied with its four colliding indices moved; row 8 is paled in place for the doors and their animation; the common sign block is copied to Blanche's 763.** ***Not row 12*** — *the pond draws it, and the pond is T-56's* |
| ~~**T-53**~~ | ~~**CRYSTAL CLEAR's lab, the exterior**~~ — ***Verified closed 2026-09-16*** (engine `4bda2ed77`) | 4bda2ed77 — ***pale stone, a flat slate roof with a parapet and a skylight, four tall two-pane windows on sills, and one line of gold at the door and its blank plate***, *drawn by `tools/gbalab.py` in row 9 on the existing footprint.* **Every cell got its own block** (*vanilla reused one across four roof cells*), **71 tiles appended after slot 76, the door cell kept block 684 for its warp and animation, and the door animation redrawn in row 9** |
| ~~**T-52**~~ | ~~**The player's bike, surf and fishing sheets**~~ — ***Verified closed 2026-09-17*** (closed in `c5233a44`) | this commit — ***Every player sheet is now drawn by `tools/gbaplayer.py`***: **the bike (9) and the VS SEEKER on it (6), fishing in three facings (12), and the three frames of sitting on the water**, *for REASON and INSTINCT, the tail difference kept on all of them.* ***On the bike the wheels turn and the rider does not lurch*** — *the GB build's call, carried over.* **The rod passes behind the body when cocked and in front when cast**, *and facing you it casts down and out so it does not vanish into the trousers.* ***The surf mount is provisional:*** **vanilla drew its body in the jeans' blues, which are this figure's blacks, so it moved onto the BOX's greys** — *you cross the water on something the colour of what you carry daemons in.* **`red_surf.png` is not drawn: nothing in the engine reads it.** ***Still open: `player_reflection.pal` is vanilla's colours***, *so water reflections show the old figure* | ***Checked 2026-09-17: STILL OPEN, and filed as T-129*** — **`player_reflection.pal` is byte-identical to `upstream/master` while `player.pal` is ours**, *so the reflection in water is still drawn in vanilla's colours. Its only commit is 2021's palette rename.*
| ~~**T-47**~~ | ~~**The Index gloss read — 0.6's second half.**~~ — ***Read 2026-09-17: every entry passes, nothing changed*** *The SUBJECT half is done: Kanto's creatures are machinery, the islands' are mind.* **What is unread is which half each entry leaves unsaid.** ***Walk the Index entries and change only the ones that leave neither*** — **a read, the shape of T-07 and T-08, not a rewrite**: *`NIBBLE` and `RUMINATE` already pass and are the worked examples in 0.6.* **Nothing is inserted; craft rule 1 says an entry that names both halves has stated the connection** ***The read***: **all 300 texts — 151 GAMUT species and the 75 island species with names of ours, both editions where they differ — were read against 0.6's one question, *which half does it leave unsaid?*** *GAMUT entries describe machinery and leave the mind to the reader; island entries describe a mind and leave the machine.* **Not one names both.** ***One was flagged and cleared against the bible***: *SUBSTRATE's CONTEXT line — "Nobody has shown that being material is different from running" — reads at first like the connection stated, and 4.25 quotes and defends it as substrate independence.* **Under 0.6's own test it passes: MATERIAL and RUNNING are both on the machine's side — what a thing is made of against what it does — and the mind is never named.** *The misreading was mine, taking "material" for "mind".* ***Two notes, neither a change under this ticket***: *WILDFIRE's LG line "It is said to remember every slight" names a grudge and leaves the machine unsaid, which is the ISLAND direction on a mainland species — it does not name both, so it stays; and the island names HOARDING and OBSESSION sit close to their clinical neighbours, which is T-45's register and craft rule 3's question, not this read's.* ***And the read found what it was not looking for***: **two entries were never written at all — filed as T-130.** | `src/data/pokemon/pokedex_text_fr.h`, `_lg.h` | **0.6** |
| ~~**T-48**~~ | ~~**The two regions are the last vanilla place names in the game.**~~ ***Closed `d71276908` — `GAMUT` and `THE MARGINS`.*** **27 substitutions in 14 files, 7 mapsecs, one item description and one block that is a rewrite rather than a substitution** — *SevenIsland's naming joke is a folk etymology for `seven`, twice.* ***Two of its own bugs are the lasting part***: **`THE MARGINS` carries its own article and the first sweep wrote `the THE MARGINS` seven times**, *and absorbing the article then ate a `.string` boundary* — **caught by the tool's re-split guard before anything was written.** *`THE HOLDOUTS` was retired first:* **`LOOKOUT` is distance 3, the same `<word>`+`OUT` shape, and island-only** | `tools/port_regions.py` | **0.6** |
| ~~**T-16**~~ | ~~**Per-gym battle backdrops.**~~ — ***Done 2026-09-17*** (`tools/genbackdrops.py`; engine `12a7d4169`) Eight benchmarks, eight rooms, one backdrop, and it is the one place decoration IS the argument: 5.3 gives each gym a lever and the backdrop can be the room you met it in. ***Costed wrong when it was proposed** — "ROM-only, cheap" is true of the BYTES and false of the work.* **Gen 3 keys the backdrop off ten TERRAIN types, each a full tileset, tilemap and palette in `graphics/battle_terrain/`** — so reassigning an existing one per gym is a data change, and eight gym-specific ones is **eight new tilesets**. *Decide which of the two before starting* ***Decided and built***: **neither of the two costed options — the Elite Four's.** *Lorelei, Bruno, Agatha, Lance and the Champion share ONE tileset and ONE tilemap and differ only in palette, so a room of its own costs forty-eight colours, not a drawing.* **Each BENCHMARK now battles in the shared room coloured as its town is — SLATE stone, DOLDRUM sea, ARDOR rust, VERDIGRIS patina, LURID pink, BRAZEN brass, QUICKSILVER silver, CALLOW green — for its trainers and its leader alike, chosen by the MAP the battle is on rather than the trainer's class.** ***The colour is read, not picked***: *each town's key is the most saturated of the first three colours of its palette in `gentowns.py`, and the tint is Lorelei's, index by index, with the town's hue and her saturation SCALED by the town's.* **The scale is what the first mock lacked — it kept every town at Lorelei's saturation and put SLATE and DOLDRUM in the same blue, because a stone and a sea differ in how much colour they have, not only in which.** *Verified by build and by bytes in all four ROMs; not yet seen in a battle. Drawing each room as its 5.3 lever remains possible later on the same map-keyed selection, which would not change.* | `graphics/battle_terrain/`, `src/battle_bg.c` | 5.3, 9.4 |
| ~~**T-39**~~ | ~~**The cross-gen relatives of our Kanto lines are HALF renamed.**~~ ***Closed `e9dcc84c4` — and there were FOURTEEN, not thirteen.*** *`HITMONTOP` was missing from the ticket's own list.* **Each named from its own line** (2.7f): the sleep ladder gained `STANDBY` below SUSPEND, the argument triad gained `PREMISE` and `CIRCULAR`, and `GOLDSET` is the clean twin of BADSEED off the same BLIGHT. **All four ROMs build and `gbastr` confirms the names through the charmap.** *The tool gap it exposed is the lasting part* — **every check defines "ours" by diffing against upstream, so a daemon we forgot to rename is invisible to all of them** | `engineGba`, species | T-36, 8.2, **2.7f** |
| ~~**T-42**~~ | ~~**`TAINT` was withdrawn in 5.2 and was still in the game.**~~ ***Closed `b8e509ed6`.*** The routine is **`TAMPER`** (CORRUPT's clause as a verb) and the Viridian line is rewritten. **Root cause: `é` is a lowercase cased character**, so `.isupper()` was dropping every accented vanilla name from `port_vocab`'s rename map — *the exact set the pass exists to remove.* **`VETOED` in `check_lexicon` now fails on any withdrawn word in player-visible text**, and `port_vocab` grew an **a/an agreement pass** (17 pre-existing errors) | `tools/`, `engineGba` | 5.2, craft rule 3, **2.7e** |

---

## Blocked — decided, but something has to happen first

| | what | blocked on | from |
|---|---|---|---|
| **T-05** | **Callow and The Bleed share their music.** *8.1's second theme cannot be given to either until they have a slot of their own* | **A track that does not exist yet.** *Re-read 2026-09-10 and the ticket was aimed at the wrong repo:* `data/maps/songs.asm` is the **Game Boy** build, which CLAUDE.md says is a reference and not updated further. **On the GBA it is one field in a map's `.json`** — so the plumbing is not the blocker at all. `song-status.md` lists eleven compositions still unassigned, and this wants one of them | 7.3, 8.1 |
| **T-10** | **The missing key signature as a puzzle** — notes reading flat because the key is absent, and giving them the key lifts them. **2.6's one-clause test for CONTEXT as a room rather than a definition**, and 5.3 names *the room* as one of four levers a grinder cannot grind | Wants **Brazen gym**, which is T-03's territory. Sequence it after | lineage 3b, 5.3 |


---

## Wants a decision first — these are Open log items, listed only so the trail is here

*Do not work these. They are questions, and the answer belongs in `vision.md`.*

- **THRASHING's ownership** — is *"the state no type owns"* the point, or does it want LOGIC? (2.7)
- **THROTTLED's five strays** — SIGNAL owns it six to five (2.7)
- **SUSPENDED's six sleep moves** — CONTEXT owns the concept; retyping the moves is a balance question (2.7)
- **`PKRS` still says PKRS** — the condition has no place in the lexicon yet (9.15)
- ~~`LEGACY` → `RUST` and `VECTOR` → `FLOAT`~~ **Declined 2026-09-11 (2.6).** *The clause was the fault, not the name — both re-clause and pass with the words untouched*
- **Whether the doctrine has a name, and whether he has one** — *placement settled by T-22: SIX ISLAND's GREEN PATH, somewhere with nobody to check him.* **Both names were left off on purpose and that may be the answer**: naming the doctrine gives the game a thing to hold, and 4.33's whole rule is that it holds nothing (4.33)
- ~~Re-score the one-clause test at 17 types~~ **Done 2026-09-11 (2.6).** *CONTEXT went up to 11/11; HARDENED and OPAQUE had never been scored and both pass*

---

## Deferred — decided we want it, decided not yet

*Not blocked and not a question. **Someone looked at the cost and said later.***
An item here needs the reason recorded, or it comes back as a fresh idea in a
month and gets re-argued from nothing.

| | what | why not yet |
|---|---|---|
| **T-18** | **A follower daemon.** One walking behind you, Yellow/HGSS style. *"You bind a daemon and you host it" is an abstraction everywhere except here* — a follower is the one place 1.1's argument would be visible rather than stated | ***The only one of the five ideas with a real RAM cost.*** It wants an object-event slot and sprite tables, and `engine.md` puts the free static space at **1.1 KB of EWRAM and 2.9 KB of IWRAM**. **Measure it before promising it** — every other job on this list is ROM-only and free. *Deferred 2026-09-10, by choice rather than by blocker* |

---

## Closed

*Struck through, never deleted. The record of what was decided is worth more
than a short list.*

| | what | closed by |
|---|---|---|
| **T-51** | ~~**The Owl signs something, S.T.A.R.R. is seen arguing, and he is an owl**~~ | this commit — ***4.34 ruling 1 reversed on playtest:*** **a sealed PEER REVIEW** (`ITEM_DEVON_GOODS`) *addressed to the people who signed the first one*, **given to Crystal with the package at THE ANNEX or in the lab, contents never shown.** ***S.T.A.R.R. is narrated three times and never quoted***, *turns toward the door after the concession, and says it is waiting while the package is missing.* **MR. CONSTRUE is `OBJ_EVENT_GFX_OWL`** — *his own slot, because BALDING_MAN is on 29 maps* — drawn by `tools/genowl.py` from an n8n draft |
| **T-11** | ~~**The 87 unreachable routines**~~ | this commit — ***they stopped being unreachable and nothing about the routines changed.*** **T-45 named 52 island daemons, and naming a daemon brings its learnset into scope**, so 2.10's own rule applied to 50 of them. **Forty renamed; 309 of 356 are ours.** *The KICK family took LOGIC's argument register — `LEAP`, `GREAT LEAP`, `LOOP BACK`, **`CHAIN RULE`** — and `STOCKPILE`/`SWALLOW`/`SPIT UP` became one metaphor: **`ENQUEUE` · `DEQUEUE` · `EVICT ALL`**.* ***Nine collisions refused before writing***, and `SNAPSHOT` caught afterwards by `check_lexicon` **because my own check read four surfaces and not items.** *Ten remain and all ten are ruled* |
| **T-46** | ~~**The 52 island learnsets**~~ | this commit — ***23 daemons, not 32***: *the ticket counted flag lines and `SILENT TYPE`/`NO STAB` name the same nine twice.* **T-36's finding held a second time — the dominant fault is a SECOND TYPE WHOSE VOCABULARY NEVER APPEARS**, *five with a silent SWARM half and six with a silent VECTOR or STRATUM one* — **so most of it was restoration again: 18 substitutions, one routine out and one in.** *Twelve more for MIXED SIGNAL and LATE TYPE, mostly two routines trading places.* ***`PROTEUS` needed fixing twice, because vanilla splits that learnset on `#if defined(FIRERED)`.*** **And three are not fixed on purpose** — `SYMBOL`, `REPAY` and `PREMISE` know one routine each and *the routine IS the creature*, each with a ruling in the tool. **All four flags report none; off-type 42%** |
| **T-45** | ~~**The 30 island daemons in the psychology register**~~ | this commit — ***fifty-two, not thirty.*** *33 have wild encounters; the evolution check found sixteen more and a second pass three after that* — **a base named with its evolution left vanilla is T-39's exact bug**, so the lines are named whole: *`RUMINATE` → `BROODING` → `GRIEVANCE`, `WHIM` → `CAPRICE` → `ABANDON`.* **It also caught `SUBSTRATE` → `PORYGON2`, which predates today.** *98 Index entries across both editions;* `TYRANITAR`'s needed its own pass because vanilla splits it on `#if REVISION`. **`HERACROSS` is `DRIVE`, which improves the measuring house** — *a woman who measures DRIVES in inches.* **The learnsets are T-46** |
| **T-38** | ~~**Both story documents go stale silently**~~ | this commit — ***split into the half a machine can judge and the half it cannot.*** **`check_lexicon` now refuses a name in either document that the game no longer uses**, *which caught three on its first run — `KINDLE ROAD`, `TREASURE BEACH` and `BOND BRIDGE`, a day after the islands were renamed.* ***And `BOND BRIDGE` was missed by the first scan because the paragraph WRAPS it*** — **prose is flattened before matching now**, the fourth time that seam has hidden something. *The other half is a **reconciled against v** line in each document, **reported and not failed**: a story doc legitimately trails the bible, and failing on that is the crying-wolf failure this project has had twice already. The number prints every run whether it is zero or forty* |
| **T-33** | ~~**`MOON STONE` wants a name**~~ | this commit — ***it already had one.*** `REFLECTION`, and the whole set with it — **`EXPOSURE` · `AXIOM` · `EMBEDDING` · `AFFECT` · `REWARD`**, *the six things a mind is built from* — **and 4.30's room already says "A REFLECTION"**, which was the one worry the ticket carried. ***What was real is the class of bug it implied***: **five lines still offered a `POKé DOLL` and an `EXP. SHARE`**, *and the fifth spanned a line break.* **The stale-name check covered places, moves and species and not ITEMS** — *they live in JSON and needed their own reader, which is why they were skipped* — ***and its filter used `o.isupper()`, which is False for `POKé DOLL`***, so the one item name with an accent stayed invisible on the very run that added items |
| **T-43** | ~~**Two words carrying a second reading nobody chose**~~ | this commit — ***both stay, and the test that cleared the first found a third nobody had decided.*** **`KILL` is not a tonal break: the lexicon already held the whole event** — *`KILL` the routine, `SIGKILL` a daemon, `DETACH` what vanilla calls fainting.* ***You kill a process and it detaches.*** **Its three siblings agree** — `TERMINATE`, `WIPE`, and ***`FISSURE`, which had never been renamed at all***: a vanilla geological word among three process words, learnable at 41. **`FISSURE` → `SEGFAULT`**, *a fatal fault in the layer everything else runs on, which is 2.6's STRATUM.* **`WEED` is settled by `BLIGHT` → `BADSEED` → `GOLDSET`.** *Recorded as 2.10a* |
| **T-40** | ~~**Vanilla Kanto Index entries under our names**~~ | this commit — ***the ticket said ten and the number is thirty*** (25 in FR, 29 in LG). **And the first measurement said ten too, and was wrong**: *comparing our entries to upstream byte-for-byte reports almost everything as rewritten, because the sweep turned POKéMON into DAEMON inside every one.* ***The comparison has to apply our vocabulary to vanilla first*** — **PULSAR, the daemon that raised the ticket, did not appear in my own first list, which is how the error surfaced.** *Thirty written to both editions in the authored voice; same text in both, which holds the divergence reduction rather than spending it.* **`check_lexicon` now refuses a renamed daemon whose entry is vanilla's** |
| **T-34** | ~~**The starter names are placeholders**~~ | this commit — ***re-cut after all, and the git log is what decided it.*** *First reading: seven of nine were load-bearing and the two odd ones were a device worth promoting.* **The history says otherwise** — `LABL` and `CLUSTR` landed **2026-08-31**, `CLUSTER` landed **2026-09-10** and had never existed on the Game Boy at all, ***so EXEGGCUTE walked into a ten-day-old collision and the newcomer moved.*** **`LABEL` · `CLUSTER` · and EXEGGCUTE to `BOOTSTRAP`**, *which sharpens its own joke — bootstrap aggregating is how an `ENSEMBLE` is built.* **8.2b's register was the argument all along: the technical surface is the undisguised word.** *`check_lexicon` now refuses a name that is another with a vowel dropped — three narrowings, and the rejected ones are in 2.7a* |
| **T-26** | ~~**Sevii tier 3b — the ~9,500 words**~~ | this commit — ***18 blocks of ~750, across all seven islands.*** **The rule held exactly as 8.2a wrote it; the SIZE was wrong by an order of magnitude.** *And the shape of the eighteen is the finding:* **eight island SIGNS, three public NOTICES, four SHOPKEEPERS and three lines from the REVIEW BOARD's own leader** — ***not one ordinary islander***, because vanilla already wrote them as people who happen to be somewhere. **The rule did not need applying to the islands; it needed applying to the institutions standing on them.** *Every remaining sign also asserted a **second vanilla name** — Chrono, Boon, Kin, Quest, Knot — for an island the map calls a number.* **Found on the way: `MT. JITTER`, a mountain in neither column, on five signposts** |
| **T-44** | ~~**`check_agent_vocab` cried wolf twice**~~ | this commit — *both false positives gone: **`PRIORITY/Paralyze Heal` is the glossary working**, and `backtracking confusion` is English.* ***Then testing the checker against sentences it should catch found three holes it had been silent about***: **the town patterns were UPPER-ONLY**, **species were not checked at all** (a hand list cannot hold 151, so nobody wrote one) and **abilities were the same hole from both sides** — `port_gamedata` was missing `ABILITY_NAME`, so the bridge reported `TORRENT` where the ROM says `SPILLOVER`, **77 of 78 stale.** *The live prompt stays scanned, but a hit whose word is no longer in anything we author is reported as history rather than counted as a fault* |
| **T-35** | ~~**Re-port the harness after every name pass**~~ | this commit — *closed by T-44 rather than on its own.* **`port_gamedata.py` now covers `ABILITY_NAME` as well as species, moves, items, maps and objects**, and reports **nothing to do on every table.** ***The ticket's premise was that the tables go stale; the finding was that one table was never in the tool at all***, so re-running it faithfully would never have fixed abilities |
| **T-36** | ~~**The daemon × type × routine coherence sweep**~~ | `546c3394` and its two predecessors — ***the structural half.*** **`SILENT TYPE` reports none and `NO STAB` reports none**, and off-type overall sits at **42% against 2.7a's 45% baseline**. *The tool's first finding was that the premise had moved: the bestiary was already on coverage, so ranking on off-type percentage would have produced a list of nothing.* ***Eight of the thirteen were one fault*** — a two-type line whose last evolution drops the second type's whole vocabulary — **so most of the pass was restoration, not invention.** *The 41 thin openings are **T-41**, split out because the ticket itself asked for that* |
| **T-37** | ~~**`INVOKE` was only on two surfaces**~~ | this commit — *1.6's rule reached the battle log and the menu and nothing else.* **Now the map dialogue, the help system, the battle prompts and the TM descriptions.** ***Most `use` survived and that is the rule working***: a TM is an item, so *"I used TMs to teach good ROUTINES"* was already right. **Ten map blocks and three help blocks are frozen** because the word is the verb or the PC's own command. *Three false positives caught before writing — **"too scared to move"**, **"mimic my every move"** and **"just move to a new town"** — each one a phrase split across a line break, which is why the guard had to become per-BLOCK.* **A pre-existing 291px line in the matchup pane turned up on the way**, 43px past vanilla's own widest, shipped and never measured |
| **T-31** | ~~**`PC` → `PORT`**~~ | this commit — ***it was already done.*** *Of 23 remaining occurrences, 22 are comments, macro docs, C identifiers and asm labels.* **The one player-visible survivor was the EASY CHAT word**, which lived because `port_vocab` is deliberately fenced out of easy chat — *a name table it also learns from* |
| **T-32** | ~~**Nineteen `type word + DAEMON` lines**~~ | this commit — *124 candidates, not nineteen, and **36 of them were the type**: the gyms first, then SWARM, FLOW, FROZEN, CORRUPT, CONTEXT, ENTROPY and EMERGENT elsewhere.* **The chart claims were checked before the words changed** — ours is vanilla's plus one line, so every matchup taught was still true. ***~70 are the ENGLISH word and were left***, which is 1.2's actual point. **Route 9's pun was rebuilt on SWARM rather than left as a joke with nothing under it** |
| **T-29** | ~~**The flight, and the reading**~~ | this commit — ***`BIRTH ISLAND` → `THE ANNEX`***, *the only one of the twenty-two not named by a person who arrived: nobody arrives here, so the name came off a form.* **The flight has two entry points** so a player who reaches the Owl without the package is not stranded. ***The last scene moves here from the lab*** — 8.2a had her read it in a room she had been thrown out of. *She does not ask how you got here. She turns it over, asks one word, walks to the water and opens it facing away, and the script ends with her still facing away.* **Deoxys is turned off** |
| **T-30** | ~~**The gold is not holding**~~ | this commit — *the inversion is done by **order and nothing else**: the buried name is what you read first now and the gold one is last, and no line remarks on it.* **4.4 calls the leaf "not a gesture, a fact with a date", so repainting it would be a gesture** — the game taking a victory lap over a man it has spent forty hours refusing to sneer at. *Nobody touches the sign. It simply stops holding* |
| **T-28** | ~~**Brazen, the Owl — 4.24 staged**~~ | this commit — ***the player only ever hears one side***, which is not economy but the safest way to write it: **the machine's argument never has to be written down, so it can never say the thesis**, and the player infers it from what a scholar answers. *That is 4.24's own structure with Act 1 inverted.* **He is not a fool and he is not beaten** — the turn is not agreement, it is him noticing his own objection applies to him. ***The concession refuses the word twice before making it.*** Gate is a new `DaemonsPartyHasStarr` special, **party rather than caught**, because 4.34 says you *bring* the machine |
| **T-27** | ~~**The lab, post–Review Board: she is not there**~~ | this commit — ***the silence version.*** *Vanilla walks her up to your door and escorts you in; none of it happens.* **You come out of your house after the biggest thing you have done and for the first time nobody is waiting, and nothing says so** — done by leaving one scene var at a value the on-frame table has no entry for, so **no script was deleted and two of them simply have no way to run.** The GLOBAL INDEX is handed over by a man at a desk on being spoken to. ***The note is one bg_event with two states*** — the INDEX unit before, the note after — *so the wall it appears on is one the player has already read once, and the note itself never changes again.* **The 60-species gate went with the escort**, deliberately: an aide saying *you have not earned it yet* reinstates assessment at the moment 8.2a removes it |
| **T-23** | ~~**Sevii tier 3 — the place names**~~ | this commit — *the ticket said 35 places and the reading said three kinds. **A place carrying its island's number is one nobody named**, which is 8.2a's grace note, so those keep the number; **TANOBY and the seven chambers are transliterations of an alphabet 4.24 says nobody reads**, so translating them would translate the one thing whose point is that you cannot; and 2.10 leaves the two islands no player reaches. **That left 21 to name.** `tools/port_sevii.py` owns them and caught both breakages: **seven hand-written `sMapsecName_*` symbols**, and one swept line pushed past the box because `STILLFALL` is two characters longer than `ICEFALL`. **The prose half is T-26** |
| **T-09** | ~~**Two ERRATA cards from *The Painted Mirror*.**~~ | this commit — *the `DISPLAY UNIT` and the `SORTING FRAME`, on the one exhibit case on that floor that had no card at all. **It is wide enough for two and they are read from opposite sides**, so one is met coming in and one going out. The ticket said what was left was "choosing which display case" — **the choice was made by reading which blocks already had a card and finding the one that did not**, and the approach tiles came out of the layout's own `map.bin`. *The words were right the first time; only the line breaks were wrong, and three of nine ran past the box by six pixels* |
| **T-21** | ~~**The Tanoby chambers as 4.24's translations**~~ | this commit — *seven chambers, seven accounts of one event, and **the event is never named**. Nobody in them is lying and nobody is wrong, and it happened anyway. Placed by reading each chamber's own collision — a wall tile with a walkable one under it* |
| **T-19** | ~~**The Warehouse, both ends**~~ | this commit — *the package is `PAYLOAD`, out of `ITEM_LETTER`, which nothing in this game referenced and which was already a letter. **Both men are in the room and SCORN is facing the right way.** TY crosses to you; the walk is the characterisation. CRYSTAL turns it over, asks one word, crosses the room and opens it facing away — **and the script ends with her still facing away*** |
| **T-22** | ~~**The doctrine's room**~~ | this commit — *the scientist on SIX ISLAND's GREEN PATH, eight steps, one per visit, and the ninth clears the flags so he goes round again. **Every prohibition is an absence**, so the script carries the list of what is deliberately not there. `tools/gbamaptiles.py` was written to place him and unblocks T-09 on the way* |
| **T-02** | ~~**The MUSAI branch was 78% off-type**~~ | this commit — *100% in fact, all nineteen. `tools/port_musai.py` takes it to **37%**, under 2.7's 45% baseline, with two deliberate residuals a line. **`FALSIFY` on CODEMUSAI is the pass**: it breaks STEELMAN and PARAPHRASE, and CODEMUSAI exists to be LOGIC against CONTEXT. **S.T.A.R.R. was already at 25%** — the ticket's 78% was the thirteen's average carried onto a name that did not deserve it* |
| **T-08** | ~~**~154 `#MON` occurrences read singular**~~ | this commit — *POKéMON is its own plural; DAEMON is an ordinary noun and is not. **25 sentences force it** and were read one at a time; the rest are genuinely singular or attributive — "rare DAEMON fossils" is a compound noun the way "sheep dog" is* |
| **T-25** | ~~**`FIGHTING DOJO` carried a vanilla type word**~~ | this commit — *`PROOF HALL`. A dojo is where a discipline is practised and a **proof** is what LOGIC produces, so it is the same building said in our words — and narrower than what it replaced, which nothing else in this pass has been* |
| **T-07** | ~~**Forty-two `fight` occurrences**~~ | this commit — *three buckets, and the read was the job. Four were the TYPE said in lower case, eleven were 1.2's replaced verb, and the rest are people: an artist who is not a fighter, a fighting game in a shop, a pro fighter. It also turned up the help system's own copy of the chart* |
| **T-13** | ~~**The BERRY register**~~ | `c4786bd15` — *a berry is a HANDLER, so it is a TRAP, so the pouch is a TRAP TABLE. `src/berry.c` IS the second table the ticket warned about and is left alone: `BERRY_NAME_LENGTH` is six, and its only readers are ENIGMA (unobtainable) and BERRY CRUSH (link-only)* |
| **T-12** | ~~**The STREAM has no overworld notification**~~ | this commit — *nine told-flags out of T-17's block, and the same per-step hook the VS SEEKER and the egg use. Told, not seen: a show you skipped does not nag, and a new MARK still announces* |
| **T-24** | ~~**The A-to-Z picker sorted by vanilla's spellings**~~ | this commit — *`tools/port_ecsort.py` resolves every entry to the string the game will actually draw and re-buckets it. 26 of the 27 lists moved; 1,949 entries in and 1,949 out, and every `numWords` still matches its array — that count is elements, not words, so a marker counts two and a wrong one is an out-of-bounds read* |
| **T-14** | ~~**The USABLE items**~~ | `cddccf293` — *41 named, and 2.8's rule takes out more than half the bag: the drinks, the treasure, the mail, the tickets and the keys are things people own. ITEMFINDER is `GREP`, TOWN MAP is `SITEMAP`, VS SEEKER is `ROLL CALL`* |
| **T-20** | ~~**`RUBY` and `SAPPHIRE`**~~ | `12b5a2e5b` — *`PUBLIC KEY` and `PRIVATE KEY`. Which is which was decided by the plot: CORPUS steals the SAPPHIRE, and stealing a private key is a crime. Exposed `gExpandedPlaceholder_*`, the table behind the `{RUBY}` control code* |
| **T-04** | ~~**Dialogue naming a vanilla type**~~ | `f7f9c1abb` — *28 lines. The type names cannot be swept as WORDS — GRASS is grass almost everywhere — but "X-type" is unambiguous, so the CONSTRUCTION is the unit* |
| **T-15** | ~~**easy_chat's second copy of the vocabulary**~~ | this commit — *smaller than the directory looked: species and moves are stored as CONSTANTS and already read as ours, so only the 70 duplicated ability strings needed moving. Derived by `tools/port_easychat.py`, not listed* |
| **T-01** | ~~**`PC` → `PORT`**~~ | `087665ed5` — *66 places, not the 25 1.7 estimated. CRYSTAL CLEAR's row went 101px → 113 and clipped a 112px menu; the window went to sixteen tiles. Three help lines rewrapped* |
| **T-06** | ~~**`check_lexicon` passes on an empty file.**~~ **An absent README row is a fault now, not agreement.** *It had been reporting "the version agrees in all four places" while testing two of them, and it went quiet at exactly the moment the surface it watches had been destroyed* | this commit |
| **T-17** | ~~**Extend the flag space**~~ | `1c83e6787` — *1,024 flags out of `SaveBlock2`'s filler, so `encryptionKey` never moved and an old save still loads. Two `STATIC_ASSERT`s, both checked by breaking them* |
| **T-03** | ~~**The seven leaders' mechanics** — the parties, Doldrum's healing trainers and Scorn's fixed roll~~ | `4f94d3914` — *six parties rewritten, two mechanics in C, one gym script. GROWTH had no coverage and the design had to change shape rather than be dropped* (9.x, 5.3b) |
